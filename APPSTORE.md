# نشر «تحدي رجا» على App Store

> دليل عملي لصاحب المشروع. ما جُهِّز في المستودع صار جاهزاً؛ الباقي خطوات
> يدوية في حسابك لا يستطيع أحد تنفيذها نيابةً عنك.

---

## الحقائق قبل أن تبدأ

| | |
|---|---|
| **التكلفة** | **99 دولاراً سنوياً** (~370 ريالاً) وتتجدّد. لو توقّفت عن الدفع يُحذف التطبيق من المتجر. |
| **Xcode** | حصري لـ macOS. أنت على ويندوز، لذلك نبني عبر **GitHub Actions** — أجهزة ماك سحابية مجانية للمستودعات العامة. |
| **مدة المراجعة** | من يوم إلى أسبوع عادةً لأول إصدار. |
| **العمر** | يجب أن تكون 18 سنة فأكثر لفتح حساب مطوّر. |

---

## ⛔ أوقفْ كل شيء حتى تنتهي من هذه الثلاثة

هذه ليست تحسينات. كل واحدة منها **رفض شبه مؤكَّد** لو أُرسل التطبيق بدونها.

### ١. شغّل `supabase-rooms-secure.sql`

الرومات كانت مفتوحة للقراءة والكتابة للجميع. تفاصيل السبب في رأس الملف
نفسه. شغّله **دفعةً دفعةً** ووقت لا أحد يلعب.

بدونه لن يعمل الأونلاين إطلاقاً في هذه النسخة من الكود — الواجهة صارت
تنادي دوال `mr_*` وهي غير موجودة في قاعدتك بعد.

### ٢. شغّل الدفعة 9 من `supabase-accounts.sql` (حذف الحساب)

**بند 5.1.1(v):** من أنشأ حساباً داخل التطبيق يجب أن يحذفه من داخل
التطبيق. الزر موجود في «ملفي والإحصائيات»، لكنه ينادي `delete_my_account`
وهي في آخر `supabase-accounts.sql` ولم تُشغَّل بعد.

### ٣. أنشئ حساب المراجعة `apple_review`

آبل لن تراجع تطبيقاً لا تستطيع الدخول إليه. سجّل حساباً بهذا الاسم من
شاشة اللعبة، واكتب كلمة مروره في `store-listing.md` ثم في خانة
App Review Notes.

---

## ما جُهِّز في المستودع ✅

| الملف | الغرض |
|---|---|
| `capacitor.config.json` | هوية التطبيق: `com.rajaxdx.tahadiraja` |
| `ios/` | مشروع Xcode كامل |
| `tools/build-www.py` | يجمع ملفات اللعبة في `www/` (~2.1 ميغابايت) |
| `tools/make-icons.py` | يولّد الأيقونات وشاشة البداية، ومنها أيقونة آبل 1024 بلا شفافية |
| `.github/workflows/ios.yml` | يبني ويوقّع ويرفع من ماك سحابي |
| `.github/workflows/verify-secrets.yml` | يتأكد أن الأسرار الثمانية موجودة وصالحة قبل أن تهدر عشر دقائق بناء |
| `privacy.html` | سياسة الخصوصية — **آبل تشترطها** |
| `store-listing.md` | كل نصوص صفحة المتجر وملصق الخصوصية جاهزة للنسخ |
| `assets/vendor/supabase.js` | المكتبة محزومة محلياً — **بند 2.5.2** |
| `ios/App/App/Info.plist` | لغة عربية + إقرار التشفير + arm64 |

### تفاصيل صُحِّحت في قالب Capacitor الافتراضي

القالب يخرج بإعدادات لا تصلح للنشر، وصُحِّحت مسبقاً:

- **`CFBundleDevelopmentRegion`**: كان `en`. يجعل المتجر يعرض التطبيق
  كأنه إنجليزي ويقترح ترجمته للعربية.
- **`UIRequiredDeviceCapabilities`**: كان `armv7` — معمارية 32‑بت ماتت مع
  iOS 11، والهدف هنا iOS 14 (arm64 حصراً). التناقض يربك حساب توافق الأجهزة.
- **`ITSAppUsesNonExemptEncryption = false`**: كان غائباً. بدونه يسألك
  App Store Connect عن امتثال التصدير في **كل رفعة** ويوقف المعالجة.
- **`TARGETED_DEVICE_FAMILY = "1"`**: كانت `"1,2"` أي دعم iPad. وآبل حينها
  تشترط **لقطات iPad** وإلا حجبت الإرسال — بعد رفع البناء ومعالجته، أي
  تكتشفها في آخر خطوة. ولدينا لقطات آيفون فقط.

---

## الخطر الحقيقي: بند 4.2

آبل ترفض التطبيقات التي هي «موقع مغلَّف» فقط. لعبتنا مكتوبة بـHTML/JS،
فالبند يخصّنا مباشرة.

**وضعنا جيد لأن:**
- كل ملفات اللعبة وأكثر من ٢٨٠٠ سؤال محزومة داخل التطبيق
- تعمل بلا إنترنت في وضع الجهاز الواحد
- **لا تجلب أي كود من الشبكة** — مكتبة Supabase محزومة محلياً (بند 2.5.2)
- لعبة حقيقية بمنطق كامل، لا صفحة عرض

**ما يزيد فرص القبول:** ألا تذكر في وصف المتجر أنها «نسخة من الموقع»،
وأن تُظهر في اللقطات لعباً فعلياً لا شاشات إعدادات ولا تسجيل دخول.

---

## الخطوات

### ١. حساب المطوّر
سجّل في [developer.apple.com/programs](https://developer.apple.com/programs/)
وادفع الـ99 دولاراً. التفعيل من ساعات إلى يومين.

### ٢. أنشئ معرّف التطبيق
App Store Connect ← My Apps ← **+** ← New App

- **Bundle ID:** `com.rajaxdx.tahadiraja` (يجب أن يطابق `capacitor.config.json`)
- **SKU:** `tahadiraja`
- **اللغة الأساسية:** العربية

### ٣. الشهادة وملف التزويد
- شهادة **Apple Distribution** → صدّرها `.p12` بكلمة مرور
- ملف تزويد **App Store** لـ`com.rajaxdx.tahadiraja`
- مفتاح **App Store Connect API** (ملف `.p8`) من Users and Access ← Keys

### ٤. أضف الأسرار الثمانية في GitHub
Settings ← Secrets and variables ← Actions:

| السرّ | من أين |
|---|---|
| `BUILD_CERTIFICATE_BASE64` | الشهادة `.p12` مُرمّزة base64 |
| `P12_PASSWORD` | كلمة مرور ملف `.p12` |
| `PROVISIONING_PROFILE_BASE64` | `.mobileprovision` مُرمّزاً base64 |
| `KEYCHAIN_PASSWORD` | أي كلمة تخترعها |
| `APPSTORE_ISSUER_ID` | App Store Connect ← Keys |
| `APPSTORE_KEY_ID` | نفس المكان |
| `APPSTORE_PRIVATE_KEY` | محتوى ملف `.p8` كاملاً |
| `TEAM_ID` | developer.apple.com ← Membership |

للترميز على ويندوز (PowerShell):

```bash
[Convert]::ToBase64String([IO.File]::ReadAllBytes("cert.p12")) | Set-Clipboard
```

### ٥. تحقّق من الأسرار قبل أن تبني
Actions ← **Verify secrets** ← Run workflow.
لا يطبع أي سرّ، ويكشف الناقص أو التالف في دقيقة بدل عشر دقائق بناء فاشل.

### ٦. بناء تجريبي بلا توقيع
Actions ← **iOS** ← Run workflow مع `upload = false`.
يثبت أن الكود يُترجم — وهذا يعمل قبل أن تدفع الـ99 دولاراً أصلاً.

### ٧. البناء والرفع
نفس السير مع `upload = true`. بعد النجاح تجد البناء في
App Store Connect ← TestFlight خلال ١٥–٦٠ دقيقة.

### ٨. املأ صفحة المتجر
كل النصوص جاهزة في [`store-listing.md`](store-listing.md) — انسخها كما هي.

> ⚠️ **انتبه لملصق الخصوصية.** «تحدي رجا» **تجمع** بيانات (بريد، اسم
> مستخدم، إحصاءات، رسائل شات)، بعكس «صراع الحروف». اختيار
> *No, we do not collect data* هنا **إقرار كاذب** — وعقوبته إزالة من
> المتجر لا رفض يُصحَّح. الجدول الصحيح في `store-listing.md`.

> ⚠️ **التصنيف العمري 12+ لا 4+**، بسبب الشات بين اللاعبين.

### ٩. اللقطات
مقاسان على الأقل: 6.9 بوصة (1320×2868) و6.5 بوصة (1242×2688)، ٣–٤ لكل مقاس.
التفاصيل وترتيب اللقطات في `store-listing.md`.

### ١٠. أرسل للمراجعة
Add for Review ← Submit. تصلك النتيجة على بريد الحساب.

---

## بعد النشر

**تحديث اللعبة:** عدّل الملفات، ارفع `MARKETING_VERSION` في
`ios/App/App.xcodeproj/project.pbxproj`، ثم شغّل السير برفع.
رقم البناء يتولّد من رقم تشغيل السير تلقائياً، ولا يتكرر أبداً.

**تحديث الأسئلة فقط:** لا يحتاج نسخة جديدة من التطبيق — لوحة الإدارة
تكتب في `game_settings` والتطبيق يقرأها من السحابة عند التشغيل.

---

## أخطاء شائعة وحلولها

| العَرَض | السبب |
|---|---|
| `No signing certificate found` | `BUILD_CERTIFICATE_BASE64` ناقص أو رُمّز بسطور جديدة داخله |
| `Provisioning profile doesn't match` | اسم الملف في حساب آبل يخالف `PROVISIONING_PROFILE_SPECIFIER` في `project.pbxproj` |
| رفض 409 عند الرفع | بُني بـSDK أقدم مما تشترطه آبل — السير يفحص هذا مسبقاً ويفشل مبكراً |
| `Missing screenshots for iPad` | عاد أحدهم بـ`TARGETED_DEVICE_FAMILY` إلى `"1,2"` |
| الأونلاين لا يعمل داخل التطبيق | `supabase-rooms-secure.sql` لم يُشغَّل |
| شاشة دخول لا تُتجاوز بلا إنترنت | تأكّد أن زر «العب بلا حساب» ظاهر — هو المخرج المطلوب في بند 5.1.1(v) |

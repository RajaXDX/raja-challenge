# تحدي رجا — خطة التطوير وحالة المشروع

> **لأي مساعد ذكاء اصطناعي يقرأ هذا الملف:** هذا هو المرجع الرسمي لحالة المشروع.
> اقرأه كاملاً قبل البدء، ثم **حدّث قائمة المهام في الأسفل** كلما أُنجز بند
> (`[ ]` ← `[x]` مع تاريخ ورقم الـ commit). آخر تحديث: **2026-09-10**.

---

## 1. ما هو المشروع

لعبة أسئلة جماعية عربية بأسلوب «الفئات والنقاط»، فريقان (أ / ب) يتنافسان على لوحة
من 6 فئات × 3 مستويات (100 / 250 / 400 نقطة).

| | |
|---|---|
| **التقنية** | HTML + CSS + JavaScript خام — **بدون أي مكتبات أو أدوات بناء** |
| **الخادم** | Supabase (قاعدة بيانات + Realtime) |
| **النشر** | GitHub Pages → `rajaxdx.github.io/raja-challenge` |
| **المستودع** | `github.com/RajaXDX/raja-challenge` (فرع `main`) |
| **اللغة** | عربي RTL بالكامل |

### وضعا اللعب
- **محلي**: جهاز واحد، فريقان يتناوبان عليه
- **أونلاين**: المضيف ينشئ روم بكود من 6 خانات، والبقية يدخلون بالكود من أجهزتهم،
  والحالة تُزامَن لحظياً عبر Supabase Realtime

---

## 2. خريطة الملفات

```
index.html              كل الشاشات (home / mode / rooms / room-setup / setup /
                        categories / game / bet / end / admin) + نافذة السؤال
css/style.css           التنسيق الرئيسي (~1850 سطر)
css/responsive.css      تنسيقات إضافية للجوال
js/utils.js             Sound، loadJSON/saveJSON، createElement، showScreen، log
js/game.js              قلب اللعبة: الحالة، اللوحة، الأسئلة، النقاط، الأدوار،
                        شاشة النهاية، تحميل بنك الأسئلة، تنقّل الأونلاين
js/rooms.js             إنشاء/دخول/خروج الرومات، اللاعبون، اشتراكات Realtime
js/chat.js              الشات (إرسال، تفاعلات، اللوحة السفلية، الزر العائم)
js/about.js             شاشة «عن اللعبة»: الشرح بأقسامه وأرقام البنك الحيّة
js/feedback.js           بلاغات الأسئلة وتقييم الجولة — والطابور المحلي لهما
js/sync.js              مزامنة الإعدادات وبنك الأسئلة مع السحابة
js/admin.js             لوحة الإدارة (أسئلة، فئات، نقاط، مزامنة، تنظيف)
js/auth.js              حسابات اللاعبين، البوابة، الإحصاءات، استعادة كلمة المرور
js/bans.js              الحظر: بصمة الجهاز، بوابة المحظور، أزرار اللوحة
js/friends.js           الأصدقاء: إضافة بالاسم، قبول، حذف
js/profile.js           الخصوصية، بطاقة اللاعب، لوحة الصدارة
js/supabase-config.js   مفاتيح الاتصال
data/questions*.json    بنك الأسئلة — 22 ملفاً
assets/logo.png         الشعار (خلفية شفافة) — logo.jpg هو الأصل
assets/help/*.webp      سبع لقطات حقيقية من اللعبة تُعرض في شاشة «عن اللعبة»
supabase-setup.sql          الجداول الأساسية
supabase-admin-security.sql سياسات game_settings
supabase-analytics.sql      جدول app_events
supabase-accounts.sql       profiles + admins + دوال الإدارة
supabase-friends.sql        جدول friendships + request_friend
supabase-privacy.sql        عمود privacy + دوال البطاقة والصدارة
supabase-invites.sql        جدول room_invites + invite_friend + my_invites
supabase-bans.sql           جدول bans + device_accounts + دوال الحظر
supabase-feedback.sql       question_reports + game_ratings  ⚠️ لم يُشغَّل بعد
```

---

## 3. الحالة الحالية — ما أُنجز ✅

### سكربتات Supabase — كلها مُشغَّلة ✅ (2026-07-29)

| الملف | ما يضيفه | الحالة |
|---|---|---|
| `supabase-setup.sql` | الجداول الأساسية والرومات | ✅ مُشغَّل |
| `supabase-admin-security.sql` | سياسات `game_settings` | ✅ مُشغَّل |
| `supabase-analytics.sql` | `app_events` | ✅ مُشغَّل |
| `supabase-accounts.sql` | `profiles` + `admins` + دوال الإدارة | ✅ مُشغَّل |
| `supabase-friends.sql` | `friendships` + `request_friend` | ✅ مُشغَّل |
| `supabase-privacy.sql` | `privacy` + دوال البطاقة والصدارة | ✅ مُشغَّل |
| `supabase-invites.sql` | `room_invites` + دعوة الأصدقاء | ✅ مُشغَّل |
| `supabase-presence.sql` | `last_seen_at` لنبضة الحضور | ✅ مُشغَّل (2026-08-03) |
| `supabase-storage.sql` | دلو `question-media` لفيديو الأسئلة | ✅ مُشغَّل (2026-09-01) |
| `supabase-feedback.sql` | `question_reports` + `game_ratings` | ⚠️ **لم يُشغَّل** (2026-09-09) |

**سكربت معلّق واحد: `supabase-feedback.sql`.** أي سكربت جديد يُكتب مستقبلاً: شغّله على دفعات
(راجع الملاحظة 11 أدناه).

### إعدادات Supabase المضبوطة
- **Confirm email**: معطّل
- **SMTP**: Resend عبر `onboarding@resend.dev`
- **Site URL** و **Redirect URLs**: مضبوطان على رابط GitHub Pages
- **حساب الإدارة**: `vip` (عبر جدول `admins`)


### بنك الأسئلة — الأرقام الحيّة (قياس فعلي داخل اللعبة 2026-08-04)

| | |
|---|---|
| **`DEFAULT_CATEGORIES`** | **28** — آخر ما أُضيف «صور قديمة» (البند 60). ⚠️ الرقم المكتوب هنا قبل البند 58 («27») كان زائداً واحداً على العدّ الفعلي |
| **الفئات المعروضة فعلاً** | **24** (قياس 2026-08-31) — لا تساوي الافتراضية بعد الآن، انظر التنبيه تحت |
| **الأسئلة** | **1,486** (قياس 2026-08-31 داخل اللعبة، شامل 34 علماً) |
| **الفئات في البنك** | 24 — «ولا كلمة 🤫» بلا أسئلة إطلاقاً |
| **فئات مسحوبة** | 17 في `retired-categories.json` + `h` من السحابة = 18 |
| **نصوص مسحوبة** | 215 في `retired-questions.json` |

> ⚠️ **`DEFAULT_CATEGORIES` لم تعد هي المعروض** (رُصد 2026-08-31): المتصفح
> المُختبَر يعرض 24 فئة تنقصه **«مطاعم السعودية»** و**«الذكاء الاصطناعي»**
> رغم وجودهما في الكود. قائمة الفئات تُسحب من السحابة وتكتب فوق الافتراضية،
> فالحذف من لوحة الإدارة على جهاز واحد يسري على الجميع. الفئتان ليستا في
> `retired-categories.json`، أي أن الإسقاط جاء من بيانات السحابة لا من الكود
> — يُتحقق قبل أي عدّ يُبنى على الافتراضية.

> ⚠️ **الأرقام القديمة في هذا الملف («2,160 سؤال في 36 فئة»، «2,727») لم تعد
> صحيحة**: مصدرها عدّ ملفات `data/` كاملة، وفيها **1,020 سؤالاً** تخصّ فئات
> مسحوبة لا تصل اللعبة أبداً. الرقم الذي يهمّ هو ما يُقاس **داخل** اللعبة بعد
> الدمج والإسقاط.

خمس فئات أضافها المستخدم بنفسه ولم تأتِ من ملفات المشروع أصلاً:
«😀 خمن بالإيموجي» و«جغرافيا 🌎» و«الالغاز 🧩» و«الحساب 🧮» و«الانبياء 🌙»
— وكلها الآن معتمدة في الكود.

### النظام
- ✅ وضع محلي كامل
- ✅ رومات أونلاين بكود + توزيع اللاعبين على الفريقين (المضيف)
- ✅ **مزامنة اللعبة لحظياً**: بدء اللعبة، فتح سؤال، إعطاء نقاط، والنهاية تظهر
  على كل الأجهزة (مختبَرة بجهازين)
- ✅ المضيف وحده يتحكم، والبقية يشاهدون (`applyViewerRestrictions`)
- ✅ زر طرد لاعب من الروم
- ✅ شات بالتفاعلات + لوحة سفلية على الجوال + زر عائم
- ✅ **مؤشر الدور**: اختيار عشوائي لمن يبدأ + إعلان + تبديل الدور بعد كل سؤال
- ✅ **شاشة الفوز**: كأس، اسم الفائز، الفارق، نتيجة الفريقين، وحالة التعادل
- ✅ لوحة النتائج (كانت مفقودة تماماً وأُعيدت)
- ✅ خصوصية الرومات (لا تُعرض قائمة عامة)
- ✅ الشعار في الهيدر
- ✅ تحسين الأداء: الملفات تُجلب مرة واحدة بدل مرتين، واستعلامات السحابة بالتوازي

### حسابات اللاعبين (2026-07-28)
- ✅ بوابة الحساب للأونلاين وحده (`REQUIRE_ACCOUNT_FOR_ONLINE` في `js/auth.js`) —
  كانت إلزامية على كل لعب حتى البند 64
- ✅ التسجيل يتطلّب **بريداً حقيقياً**؛ الحسابات القديمة تبقى ببريد مشتقّ من بصمة الاسم
- ✅ الدخول باسم المستخدم أو البريد — يجرّب المسارين
- ✅ استعادة كلمة المرور برابط على البريد + شاشة تعيين كلمة جديدة
- ✅ لوحة الإدارة: قائمة الحسابات بإحصاءاتها، ترقية/إزالة إدمن، حذف حساب
- ✅ الصلاحية = عضوية في جدول `admins` تُفرض عبر `is_admin()` في قاعدة البيانات
- ⚠️ **كلمات المرور غير قابلة للعرض إطلاقاً** — تُحفظ مشفّرة في Supabase Auth. هذا مقصود.

### الأصدقاء والخصوصية (2026-07-29)
- ✅ **إضافة صديق باسم المستخدم** — طلب/قبول/حذف، والطلب المتبادل يُقبل تلقائياً
- ✅ **خصوصية الملف**: 🌍 عام · 🤝 للأصدقاء · 🔒 خاص (الافتراضي عام)
- ✅ **بطاقة لاعب** بالبحث عن اسمه — الأرقام تظهر حسب خصوصيته
- ✅ **لوحة صدارة** للحسابات العامة فقط
- ⚠️ **الخصوصية مفروضة في قاعدة البيانات**: `profiles` مغلق أمام القراءة المباشرة
  (عدا صفّك والإدارة)، والوصول عبر `find_player` و`get_player_card`
  و`list_my_friends` و`leaderboard`. **لا تُعِد فتح السياسة.**
  تم التحقق: استعلام مباشر بلا تسجيل دخول يرجع **صفر صفوف**.

### دعوة صديق للروم (2026-07-29)
- ✅ زر **«🤝 دعوة صديق»** في شاشة الروم يعرض قائمة الأصدقاء
- ✅ الدعوة تصل لحظياً كلافتة داخل اللعبة، وزر «دخول» يُدخله بلا كود
- ✅ ما وصل أثناء الغياب يظهر عند الدخول (خلال 3 ساعات)
- ⚠️ **الأصدقاء فقط، والشرط مفروض في قاعدة البيانات**: `room_invites` بلا
  سياسة INSERT إطلاقاً، والإدراج عبر `invite_friend()` التي تفحص الصداقة.
  تم التحقق: الإدراج المباشر مرفوض، والقراءة لا تسرّب دعوات الآخرين.

### البريد والاستعادة (2026-07-29)
- ✅ SMTP مربوط عبر **Resend** (Supabase → Authentication → Emails → SMTP)
- ✅ Site URL و Redirect URLs مضبوطان على رابط GitHub Pages
- ✅ استعادة كلمة المرور تعمل كاملة — الرسالة تصل والرابط يفتح شاشة التعيين
- ⏸️ **زر «نسيت كلمة المرور؟» مخفي مؤقتاً** عبر `ENABLE_PASSWORD_RECOVERY`
  في `js/auth.js`. السبب: `onboarding@resend.dev` لا يرسل إلا لصاحب حساب
  Resend، فأي لاعب آخر سيقرأ «وصلك رابط» ولا يصله شيء.
  **أعده إلى `true` بعد توثيق نطاق.**

### تعديلات الأونلاين (2026-07-28)
- ✅ بوابة البدء: لاعبان على الأقل مُوزَّعان على الفريقين، وزر معطّل بسبب مكتوب
- ✅ **دور لكل لاعب** بالتناوب بين الفريقين (`turnOrder` + `activePlayerId`)
- ✅ **أربعة خيارات لكل سؤال واحتساب تلقائي** — صاحب الدور يجيب من جهازه
- ✅ عدّاد الرسائل غير المقروءة على أيقونة الشات

---

## 4. أشياء مهمة يجب معرفتها قبل التعديل ⚠️

هذه اكتُشفت بصعوبة — لا تكررها:

1. **متغيرات الحالة `let` في نطاق السكربت**
   `currentRoom`, `activeTeam`, `QBANK` وغيرها معرّفة بـ `let` على مستوى الملف.
   `window.currentRoom = x` **لا يغيّرها**. للاختبار من الكونسول استخدم
   `eval` غير المباشر: `const geval = eval; geval("currentRoom = {...}")`.

2. **بنك الأسئلة يُدمج ولا يُستبدل**
   `syncBundledQuestionBank()` تُشغَّل كل تحميل وتدمج ملفات المشروع مع الموجود
   (مطابقة بنص السؤال). هذا مقصود: يمنع نسخة سحابية قديمة من محو الأسئلة الجديدة،
   ويحافظ على ما أضافه المستخدم يدوياً. **لا تحوّلها لاستبدال.**

3. **قائمة الفئات تُدمج ولا تُستبدل** (منذ 2026-07-27)
   `mergeCategories()` في `js/sync.js`. السبب: كان الاستبدال الكامل يسمح لأي شخص
   يصل للوحة الإدارة بمحو فئات كل اللاعبين. **لا تُرجعها استبدالاً.**

4. **`createElement` في utils.js** تضع المحتوى عبر `innerHTML` إذا لم يكن Node.
   مرّر نصاً أو رقماً بأمان، ولا تمرّر مدخلات مستخدم بدون `escapeHtml`.

5. **الاختبار**: لا يوجد أي إطار اختبارات. الطريقة المتّبعة:
   ```bash
   python -m http.server 8150 --bind 127.0.0.1 --directory .
   ```
   ثم التحقق من المتصفح بـ JavaScript (قياس العناصر، تشغيل جولة كاملة برمجياً).
   **لا تكتفِ بقراءة الكود — شغّل اللعبة وتحقق فعلياً.**

6. **رسائل الـ commit** بالإنجليزية، وتذكر ما جرى التحقق منه فعلياً.

7. **`css/responsive.css` يُحمَّل بعد `style.css`** فيتغلّب عليه عند نفس الأولوية.
   تعديلات الجوال يجب أن تكون في `responsive.css` نفسه.

8. **ارفع `?v=` في `index.html`** بعد أي تعديل CSS/JS وإلا بقي الكاش القديم.

9. **المشتّتات مولّدة وقت التشغيل** — البنك لا يحوي خيارات خاطئة مكتوبة.
   منذ 2026-08-02 يمرّ التوليد بثلاثة مسارات (مجموعة نوعية ← رقمية ← إجابات
   الفئة بمرشّحات شكل متدرّجة). **إن أضفت مجالاً جديداً كثير التكرار
   (مثل: عملات، لغات، رياضات شتوية) أضِف له مجموعة في `ANSWER_POOLS`** —
   هذا أرخص وأقوى تحسين متاح.
   ⚠️ `findAnswerPool` تطابق **بداية** الإجابة لا احتواءها في أي موضع:
   الاحتواء كان يجعل «الحوت الأزرق» يقع في مجموعة الألوان فتصير خياراته ألواناً.

10. **الأنيميشنات لا تتقدّم ولوحة المتصفح مخفية** أثناء الاختبار الآلي.
    قِس `offsetWidth` لا `getBoundingClientRect` إن كان العنصر متحرّكاً.

11. **محرر Supabase ينفّذ اللصقة كمعاملة واحدة** — خطأ في أمر واحد يُلغي كل
    شيء بصمت. شغّل السكربتات الطويلة على دفعات لتعرف أين تفشل.

12. **لا تستخدم `$$` كمحدّد دالة** في لصقة تحوي `DO $$` — يتصادمان.
    استخدم `$fn$`.

13. **plpgsql ترفض تسمية عمود إخراج باسم عمود جدول** تقرأه في نفس الدالة
    (`username`). سمِّه `out_username`.

14. **`switchAuthTab` لا تُستدعى تلقائياً** عند عرض شاشة الحساب — لا بد من
    ندائها في `initAuthGate`، وإلا ظهرت الشاشة بحالة HTML الافتراضية.

15. **إعادة صياغة سؤال تحتاج خطوتين.** الدمج يطابق بنص السؤال، فتغيير النص
    يُنتج سؤالاً «جديداً» ويبقى المعيب جنبه على أجهزة اللاعبين وفي Supabase.
    **أضف النص القديم حرفياً إلى `data/retired-questions.json`.**
    القائمة يفحصها الدمج نفسه، فالسؤال المسحوب لا يعود من أي مصدر — لا من
    الملفات ولا من نسخة السحابة ولا من بثّ لحظي.
    (تغيير **الإجابة** وحدها لا يحتاج ذلك — يكفي تعديل الملف.)

16. **ارفع `?v=` بعد أي تعديل على `data/*.json` أيضاً**، لا على CSS/JS فقط.
    البيانات صارت تُجلب بنفس رقم الإصدار عبر `versionedUrl()`.

17. **الصور والصوت تُخزَّن داخل بنك الأسئلة نفسه** كـ data URL، والبنك يُحفظ
    في localStorage (~5MB سقفاً). لذلك تُصغَّر كل صورة إلى 640px، والصوت محدود
    بـ 400KB، و`saveBankWithImages()` ترفض فوق 4MB. عند الحاجة لصور كثيرة:
    انقلها إلى **Supabase Storage** واحفظ الرابط بدل الـ data URL — كما جرى
    للفيديو في الملاحظة 17.5.

17.5 **الفيديو خرج من البنك إلى Supabase Storage** (2026-09-01). يُرفع إلى دلو
    `question-media` ويُحفظ في السؤال **رابطه فقط**، فارتفع الحدّ من 1MB إلى
    50MB بلا سقف على العدد. المدخل الوحيد `storeVideoFile()` في `js/utils.js` —
    لا تستدعِ `readVideoFile()` مباشرة، فهي مسار الطوارئ بلا شبكة (1MB داخل
    البنك). الأسئلة القديمة تحمل data URL في نفس الحقل `video` و`<video src>`
    يقبل الاثنين، فلا حاجة لترحيل. الحذف من المخزن أفضل جهد عبر
    `deleteStoredVideo()` كي لا يمتلئ الدلو بمقاطع بلا صاحب. `supabase-storage.sql`
    مُشغَّل (2026-09-01) والدلو موجود وعام — تُحقّق منه بأن طلب كائن غير موجود
    يردّ `Object not found` لا `Bucket not found`.

17.6 **فئة «شاهد وأجب 🎬» تُقفل السؤال خلف مقطعه** (2026-09-01). نصّ السؤال
    وإجابته — وخياراته في الأونلاين — لا تدخل الصفحة أصلاً قبل انتهاء المقطع
    (لا مجرّد `display:none`، فلا تُقرأ من مصدر الصفحة). المطابقة في
    `isWatchFirstCategory()` بعد تجريد كل ما ليس حرفاً عربياً، فتقبل الاسم
    بالإيموجي وبدونه. ثلاثة أمور تحكم السلوك ولا تُمسّ بلا سبب:

    • **`watchedQuestionKey` خارج الـ DOM.** `renderQuestionBody` تُستدعى مع
      كل بثّ حالة أونلاين؛ لو كانت حالة المشاهدة داخل الصفحة لأُعيد القفل
      بعد كل تحديث.
    • **`snapshotQuestionMedia` / `restoreQuestionMedia`.** الرندر يستبدل
      `#qbody` كاملاً فيولد عنصر وسائط جديداً من الصفر — أي مقطع لا يصل
      لنهايته أبداً في الأونلاين. نحفظ موضع التشغيل ونعيده. (يفيد الصوت
      كذلك.)
    • **المؤقّت يبدأ عند الظهور لا عند الفتح.** `openQuestion` تترك
      `questionDeadline = 0` للسؤال المقفول، و`revealWatchedQuestion` هي من
      تضبطه وتبثّه. بغيره يلتهم مقطعٌ من 30 ثانية مهلةَ الإجابة كاملة.

    زر «أظهر السؤال الآن» مخرج يدوي محلي لكل جهاز: مقطع تالف أو شبكة متعثّرة
    يجب ألّا تُجمّد السؤال. سؤال بلا مقطع في هذه الفئة لا يُقفل إطلاقاً.

17.6.1 **المؤقّت لا يظهر قبل السؤال — على كل الأجهزة** (2026-09-05، `HEAD`).
    كانت مهلة السؤال تُبثّ من صاحب القرار فيلتقطها كل جهاز ويبدأ العدّ فوراً،
    حتى جهاز ما زال على شاشة المقطع: مؤقّت يعدّ فوق سؤال غير ظاهر. ثلاثة
    إصلاحات:

    • **`startQuestionTimer` تصمت ما دام السؤال مقفولاً.** المهلة تُخزَّن كما
      وصلت (لحظة انتهاء مطلقة) بلا عرض، ويُعاد استدعاء المؤقّت عند الظهور
      فيلتقطها — فيبقى الجميع على نفس اللحظة بلا عدّ مبكّر.
    • **`watchRevealed` تُبثّ مع الحالة.** صاحب القرار حين يظهر عنده السؤال
      يفتح القفل عند الجميع، فلا يبقى أحد على المقطع والوقت يجري عليه.
    • **`isWatchFirstCategory` تُطبّع العربية** (أ إ آ ← ا، ة ← ه، ى ← ي).
      «شاهد واجب» و«شاهد وأجب» اسم واحد عند اللاعب، وكان اختلاف الهمزة وحده
      يعطّل القفل كلّه بصمت — فيجري المؤقّت من لحظة الفتح كأن الفئة عادية.

    `questionOpenedAt` تُضبط عند الظهور كذلك، وإلا حُسب طول المقطع ضمن زمن
    الإجابة في ملخّص الجولة.

17.7 **تعديل النقاط يدوياً** (2026-09-05، `HEAD`). زرّ «⚖️ تعديل النقاط» في
    ترويسة اللعبة يفتح صندوق الحَكَم: مقدار مختار (50/100/250/400) وصفّ لكل
    فريق بزرّي زيادة وخصم. النقاط تتغيّر مع كل ضغطة — لا «حفظ» في النهاية —
    وتُبثّ فوراً في الأونلاين كي لا يتأخّر الرقم على أجهزة اللاعبين.

    • **المضيف وحده في الأونلاين** (`canAdjustScores`). تعديلٌ من جهاز لاعب
      لن تقبله السحابة (`canControlGame` تكذّبه ما لم يكن دوره) فيرى رقماً
      يرجع بعد لحظة — لذلك يُخفى الزرّ عنه في `applyViewerRestrictions`.
    • **لا رصيد سالب**: الخصم يقف عند الصفر، كما في `applyBetOutcome`.

17.8 **انتهاء وقت السؤال لا يستهلك الخلية** (2026-09-05، `HEAD`). كان انتهاء
    الوقت يُعلّم الخلية مستخدَمة فيضيع السؤال. صار: الدور ينتقل والخلية تبقى
    مفتوحة، وفتحها ثانيةً يعيد **نفس** السؤال (`questionCache` مفهرَس بالخلية
    فلا يُسحب من البنك من جديد). `stateExpired` تُوسم بها الخلية وتُبثّ
    (`expired`) لتظهر منقّطة الإطار بساعة صغيرة على كل الأجهزة.

    • **لا كشف للإجابة عند انتهاء الوقت في الأونلاين.** كانت الخيارات تُبرز
      الصحيحة، وهذا يُفرغ إعادة الفتح من معناها — صارت كلها باهتة والرسالة
      «السؤال يرجع للوحة».
    • أي حسم آخر (نقاط لفريق، أو «استريح») يستهلك الخلية كالمعتاد.
    • ⚠️ **الجولة لا تنتهي وخلية مفتوحة.** سؤال يتكرّر انتهاء وقته يؤخّر شاشة
      النهاية؛ يحسمه أي إجابة (ولو خاطئة) أو «استريح».

18. **الكتابة في `game_settings` مقصورة على الإدارة.** أي `pushToCloud()` من
    جهاز لاعب عادي يفشل بخطأ RLS. لا تضِف نداءات دفع في مسارات يمرّ بها كل
    لاعب — تملأ السجلّ بلا فائدة.

19. **كيف تختبر الأونلاين بلا حسابين ولا جهازين** (الطريقة المستعملة في
    محاكاة 2026-08-02، أنجع ما جُرِّب):
    - افتح **تبويبين** على نفس العنوان — لكلٍّ ذاكرته وحالته، فهما جهازان فعلاً
    - في كل تبويب اضبط `currentRoom` و`currentPlayer` و`roomPlayers`
      بـ `geval` (راجع الملاحظة 1). **الأسماء في `roomPlayers` حقلها
      `player_name` لا `name`** — `buildTurnOrder` تقرأ `player_name`،
      وتمريرها خطأً يجعل لافتة الدور فارغة ويضيّع وقتك في مطاردة وهم.
    - **اعترض `updateRoomGameState` وحدها** (طبقة النقل) واحفظ
      `payload.state_data` في مصفوفة. لا تعترض `publishGameState` — فيها
      حارس `canControlGame()` وهو غالباً موضع العطل نفسه.
    - انقل الحالة يدوياً إلى التبويب الآخر عبر `applyRemoteGameState(state)`
    - للتقمّص: `currentPlayer` تُضبط من `turnOrder[turnIndex]` قبل كل دور
    هذا يغطّي كل شيء عدا رحلة Supabase.

---

## 5. قائمة المهام 📋

> حدّث الحالة هنا فور إنجاز أي بند.

### 🔴 حرج — قبل أي نشر عام

- [x] **1. تأمين لوحة الإدارة** — أُنجز 2026-07-27
  كان `ADMIN_PIN = '2014'` مكتوباً في `js/game.js:4` ومقروءاً لأي زائر، ولوحة
  الإدارة تكتب في السحابة، و`js/sync.js` يستبدل `CATEGORIES` بالكامل عند الجميع.
  **ما نُفِّذ:**
  - مزامنة الفئات صارت **دمجاً** عبر `mergeCategories()` → الحذف لا ينتشر أبداً
  - الدخول للوحة صار عبر **Supabase Auth** (بريد + كلمة مرور) بدل الرمز،
    والرمز يعمل فقط في التشغيل المحلي بلا سحابة حيث لا يضر أحداً
  - نفس التحقق طُبِّق على `addCustomCategory` و`quickAddAndShow`
  - `supabase-admin-security.sql`: القراءة عامة والكتابة في `game_settings`
    مقصورة على `authenticated` — الصلاحية مفروضة في قاعدة البيانات لا في المتصفح

  > ✅ **الخطوة اليدوية نُفِّذت (2026-07-27):** حساب الإدارة أُنشئ في Supabase
  > والسكربت شُغِّل. تم التحقق عبر `pg_policies`: أربع سياسات على `game_settings`
  > — قراءة `{public}` وثلاث كتابات `{authenticated}`.

- [x] **2. عودة اللاعب بعد الانقطاع** — أُنجز 2026-07-27
  كان `joinRoom` يشترط `status='waiting'`، والمضيف يحوّل الروم إلى `active` عند
  البدء → من يحدّث الصفحة أو ينقطع اتصاله يخرج نهائياً.
  **ما نُفِّذ:**
  - البحث صار بالكود فقط، والمقعد يُستعاد عبر `device_id` بنفس الفريق والنقاط وصفة المضيف
  - جلسة تُحفظ في `localStorage` (تنتهي بعد 6 ساعات) و`restoreRoomSession()`
    تُشغَّل عند تحميل الصفحة فتعيده تلقائياً
  - `fetchAndApplyGameState()` ترجعه إلى اللوحة مباشرة إن كانت الجولة جارية
  - الرفض ما زال قائماً في محله: **المطرود** لا يعود، و**لاعب جديد** لا يدخل
    وسط جولة بدأت، و**الروم المنتهية** مرفوضة

### 🟠 مهم — يخفض الاحتكاك ويزيد الانتشار

- [x] **3. رابط مباشر للروم + مشاركة واتساب** — أُنجز 2026-07-27
  كان لا بد من نطق كود من 6 خانات وكتابته يدوياً على الجوال.
  **ما نُفِّذ:**
  - زرّا **«مشاركة على واتساب»** و**«نسخ الرابط»** في شاشة إعدادات الروم
  - الرابط `?room=CODE` يفتح شاشة الدخول والكود معبّأ تلقائياً، ثم يُنظَّف
    من شريط العنوان حتى لا يتكرر عند التحديث
  - اسم اللاعب يُقترح من الجلسة السابقة إن وُجدت
  - للنسخ بديل يعمل بدون HTTPS (حيث تُمنع الحافظة)

- [x] **4. صورة معاينة الرابط + أيقونة الموقع** — أُنجز 2026-07-27
  أُضيفت وسوم `og:title` و`og:description` و`og:image` و`og:url` و`twitter:card`
  و`description` و`favicon` و`apple-touch-icon` و`theme-color` في `index.html`.
  الرابط صار يظهر في واتساب بصورة الشعار ووصف.
  - [ ] **باقٍ: نطاق خاص قصير** — `rajaxdx.github.io/raja-challenge` صعب النطق،
    وهذا مهم في لعبة تنتشر شفهياً (~50 ريال سنوياً)

- [x] **5. ضغط الشعار** — أُنجز 2026-07-27
  من **111 KB** (260×267) إلى **16 KB** (238×244) — توفير **85%**. الأبعاد تكفي
  لشاشات 2x عند أقصى عرض 122px، والشكل لم يتأثر.

- [x] **6. تصحيح الأرقام القديمة** — أُنجز 2026-07-27
  - `README.md` صار «2,160 سؤال في 36 فئة» بدل «200+ سؤال … 35 فئة»
  - عدد الفئات صار يُحسب من البيانات في `updateTotalStats()` بدل رقم ثابت في HTML
  - بريد `challange-raj.app` **غير موجود في المستودع** — لا شيء يُصحَّح

### 🟡 تحسينات ظاهرة

- [x] **7. تكبير الخطوط ومساحات اللمس** — أُنجز 2026-07-27
  رُفعت الخطوط الأصغر إلى 13px على الجوال، وزر إغلاق السؤال والشات صار **44×44**
  (الحد الموصى به عالمياً)، وأزرار الطرد والفريق والتفاعل بحد أدنى 38px.

- [x] **8. لوحة اللعب على الجوال** — أُنجز 2026-07-27
  اللوحة صارت قابلة للتمرير الأفقي بدل ضغط 6 أعمدة في شاشة ضيقة.
  الخلية على 375px: **80×58 بخط 15px** بدل 87×45 بخط 12px، والصفحة نفسها
  بلا تمرير أفقي.

  > ⚠️ **درس مهم:** `css/responsive.css` يُحمَّل **بعد** `style.css` فيتغلّب عليه
  > عند نفس الأولوية. تعديلات الجوال يجب أن تكون في `responsive.css` نفسه.
  > وأيضاً: ارفع `?v=` في `index.html` بعد أي تعديل CSS وإلا بقي الكاش القديم.

- [x] **9. استبدال `prompt` / `alert` بنوافذ مصمّمة** — أُنجز 2026-07-27
  `uiAlert` / `uiConfirm` / `uiPrompt` في `js/utils.js` بتنسيق اللعبة.
  استُبدل **65 موضعاً**: 51 `alert` و7 `confirm` و7 `prompt`.
  - `uiAlert` إشعار غير حاجب يختفي وحده بعد 4 ثوانٍ
  - `uiConfirm` / `uiPrompt` تُرجعان Promise، فالدوال المستدعية صارت `async`
  - Enter يؤكّد وEscape يلغي والنقر خارج الصندوق يلغي
  - لم يبقَ أي استدعاء أصلي في الكود (الموضع الوحيد المتبقي تعليق توضيحي)

  > السبب لم يكن الشكل فقط: بعض المتصفحات — خصوصاً iOS وداخل الإطارات —
  > تحجب `prompt()` تماماً، فكان إنشاء الروم يتوقف بلا أي رسالة.

### 🟢 لاحقاً

- [x] **10. تحليلات بسيطة** — أُنجز 2026-07-27
  جدول `app_events` يسجّل أربعة أحداث: زيارة، إنشاء روم، بدء جولة، اكتمال جولة.
  - **لا يُسجَّل أي شيء يخصّ الهوية** — لا اسم ولا رسالة ولا معرّف جهاز.
    فقط الحدث والمصدر ونوع الجهاز.
  - المصدر يُقرأ من `?src=` (للروابط التي تنشرها) أو من المُحيل
    (واتساب/تويتر/تيك توك/سناب...)
  - زيارة واحدة لكل جلسة تصفّح لا لكل تحديث
  - لوحة الإحصائيات في تبويب المزامنة: الأرقام + من أين جاؤوا + نسبة الجوال
  - القراءة مقصورة على حساب الإدارة، والكتابة مفتوحة (اللاعبون غير مسجّلين)
  - `trackEvent` لا يرمي خطأ أبداً — تم التحقق أن اللعبة تعمل كاملة والجدول غير موجود

  > ✅ **الخطوة اليدوية نُفِّذت (2026-07-27):** الجدول أُنشئ والسياستان مؤكَّدتان
  > عبر `pg_policies` — إدراج `{public}` وقراءة `{authenticated}`.
- [x] **11. زر «شارك النتيجة»** — أُنجز 2026-07-27
  زر في شاشة الفوز يبني نصاً فيه الفائز واسما الفريقين ونقاطهما ورابط اللعبة،
  ويستخدم `navigator.share` حيث تتوفر (تفتح قائمة مشاركة النظام على الجوال)
  ويسقط إلى واتساب على الكمبيوتر
- [x] **12. وسائل المساعدة** — أُنجز 2026-07-27
  كان الضغط عليها يشطبها فقط بلا أي أثر. الآن لكل واحدة وصف وأثر حقيقي:
  - **🪤 الفخ** — إن أجاب الفريق الآخر صحيحاً تُحوَّل نقاطه لصاحب الفخ
  - **🕳️ الحفرة** — زر نقاط الفريق الآخر يُعطَّل على هذا السؤال
  - **✋ استريح** — تخطٍّ بلا نقاط مع **بقاء الدور** مع نفس الفريق
  - **📞 اتصال بصديق** — مؤقّت 30 ثانية تنازلي داخل نافذة السؤال
  - **✌️ جاوب جوابين** — لافتة تُعلن الحق بإجابتين (الحكم للمضيف)

  تُستهلك مرة واحدة طوال اللعبة، ولا يمكن تفعيل أكثر من واحدة على السؤال نفسه،
  وتُبثّ ضمن حالة اللعبة فتظهر على كل الأجهزة في الأونلاين.

---

### 🐞 بلاغات المستخدم من أول جلسة لعب حقيقية (2026-08-02)

- [x] **13. الدور يقف عند من لعب** — أُنجز 2026-08-02
  «محمد جاوب، المفروض يجي دور خالد، بس ضل واقف على محمد».
  **السبب**: `finishAnsweredQuestion` تبدّل الدور ثم تبثّ الحالة، والبثّ محروس
  بـ `canControlGame()` = مضيف **أو صاحب الدور**. فور تبديل الدور يفقد المجيب
  صفة صاحب الدور، فيُمنع من بثّ التبديل نفسه — وتبقى بقية الأجهزة على الدور
  القديم للأبد. لا تظهر عند المضيف لأنه مخوّل دائماً، ولهذا بدت عشوائية.
  **الحل**: `answerPublishGrace` تمدّ الصلاحية عبر التبديل، في
  `finishAnsweredQuestion` و`award` معاً.
  **التحقق (١) — منطقي**: ست جولات متتالية بمجيب غير مضيف — الدور يتبادل
  والحالة المبثوثة تطابق الدور الجديد في كل مرة.

  **التحقق (٢) — محاكاة جهازين، 2026-08-02**: تبويبان مستقلان، كل واحد
  بحالته، ونقل الحالة بينهما كما تنقلها Supabase (اعتراض
  `updateRoomGameState` وحده — `publishGameState` وحارس `canControlGame()`
  بقيا كما هما، وهما موضع العطل).
  - بدء الجولة عند المضيف → جهاز الضيف تحوّل لشاشة اللعب، 18 خلية،
    ولافتته تقول «الدور الآن: 🟢 محمد» وليس دوره ✓
  - **المضيف يجيب صح** → عند الضيف: «🟡 خالد — دورك أنت»، النقاط 100 وصلت،
    والخلية الملعوبة صارت مستهلكة ✓
  - **الضيف (غير المضيف) يجيب خطأ** — المسار المعطوب سابقاً → الحالة التي
    بثّها تقول `turnIndex: 0` أي **محمد**، وجهاز المضيف عرض «دورك أنت» ✓
    (قبل الإصلاح كانت آخر حالة مبثوثة تقول «خالد» وتتجمّد هناك)
  - 16 سؤالاً حتى آخر خلية → النتيجة 2100 — 400، وشاشة الفوز ظهرت على
    **الجهازين** بنفس الفائز ونفس الفارق (1700) ونفس النتيجة ✓

  ⚠️ **ما لم تغطّه المحاكاة**: رحلة Supabase نفسها، والمؤقّت الحقيقي
  (2.6 ثانية) في غير أول دورين.

- [x] **14. أسئلة إجابتها لا تطابق السؤال** — أُنجز 2026-08-02
  «في أي عام…» والإجابة قرن. مسح البنك كله (2,503 سؤال) أظهر 6 حالات:
  سؤال عن عام بإجابة قرن، وخمسة أسئلة إجابتها تتقادم (`حالياً`/`منذ`) —
  منها «كم عدد السنوات منذ التأسيس» بإجابة **98 سنة** وهي خاطئة حسابياً.
  أُعيدت صياغتها كلها، ووُحِّد تاريخ تأسيس الدولة السعودية الأولى على
  **1727م** (يوم التأسيس الرسمي) بعد أن كان 1744 في موضع و1727 في ثلاثة.

- [x] **15. الصور المستوردة لا تُضاف لأي سؤال** — أُنجز 2026-08-02
  `importImages` كانت تحفظ الصور في `mr_images` **ولا سطر واحد في المشروع
  يقرأها**، ثم تعرض رسالة «يمكنك الآن استخدام أسماء الصور في الأسئلة!» — وعد
  بلا كود. **ما نُفِّذ**: حقل `image` على السؤال (data URL)، وعرضه بدل
  الإيموجي في الوضعين، وزر 🖼️ لإلصاق صورة بأي سؤال موجود، ومكتبة صور مرئية،
  وتصغير تلقائي إلى 640px/جودة 0.7 (صورة 1.7MB ← عشرات الكيلوبايتات)،
  وحارس حجم يرفض عند 4MB بدل أن يفشل الحفظ صامتاً.

- [x] **16. تسجيل الدخول يُطلب كل مرة** — أُنجز 2026-08-02
  `loadProfile` كانت تبدأ بـ `getUser()` (نداء شبكة) وتشترط نجاح قراءة صف
  `profiles`؛ أي تعثّر لحظة الفتح يمسح `currentProfile` فتظهر بوابة الدخول
  رغم أن الجلسة سليمة ومحفوظة. صارت تعتمد `getSession()` (قراءة محلية بلا
  شبكة)، وإن تعذّر جلب الصف يبقى اللاعب داخلاً بنسخة محفوظة موسومة `_stale`.
  ⚠️ `_stale` **ممنوعة من الكتابة**: `recordGameResult` ترفض الكتابة بها،
  وإلا محت سجلّ اللاعب بأصفار.

- [x] **17. ملفات البيانات لا يصلها تحديث أبداً** — أُنجز 2026-08-02
  اكتُشف أثناء إصلاح البند 14: `data/*.json` كانت تُجلب **بلا `?v=`**، فيخدمها
  المتصفح من الكاش. أي تصحيح أو إضافة في بنك الأسئلة لا يصل للاعب مهما رُفع
  إصدار السكربتات. الآن `versionedUrl()` تقرأ الإصدار من وسم `game.js` نفسه،
  فرقم `?v=` واحد يبطّل الكاش للسكربتات والتنسيقات **والبيانات** معاً.

- [x] **18. الدمج كان عاجزاً عن تصحيح أي خطأ** — أُنجز 2026-08-02
  `mergeIntoQuestionBank` كانت تتخطّى أي سؤال نصّه موجود، فتصحيح إجابة في
  الملفات يموت عند حدود المتصفح (النسخة القديمة في localStorage وSupabase).
  الآن ملفات المشروع **مرجع للإجابة**: إن اختلفت حُدِّثت. وإعادة صياغة سؤال
  تحتاج خطوة ثانية — راجع الملاحظة 15 أدناه.

- [x] **19. زر «القائمة الرئيسية» أثناء الجولة** — أُنجز 2026-08-02
  لم يكن هناك مخرج من شاشة اللعب إلا «↺ لعبة جديدة» التي تعيدك لاختيار
  الفئات وأنت ما زلت داخل الروم. أُضيف `exitToHomeConfirm()` بجانبه.
  ليس مجرّد `goToHome()`: يغلق نافذة السؤال المفتوحة (وإلا بقيت معلّقة فوق
  الشاشة الرئيسية)، و**يخرج من الروم فعلياً** عبر `leaveRoom()` في الأونلاين
  — تُلغى الاشتراكات ويُخفى الشات وتُمسح جلسة العودة التلقائية — ثم يصفّر
  حالة الجولة. نص التأكيد يختلف: في الأونلاين ينبّه أنك ستخرج من الروم.
  **التحقق**: محلي وأونلاين، إلغاء التأكيد يُبقي كل شيء، والزران على سطر
  واحد بارتفاع لمس 44px على 375px بلا تمرير أفقي.

- [x] **20. انتقال الاستضافة عند خروج المضيف** — أُنجز 2026-08-02
  خروج المضيف كان يترك الروم بلا صاحب: لا توزيع فرق، ولا بدء جولة، ولا
  تحكّم — والباقون عالقون في لوحة لا تتقدّم.
  **مساران:**
  1. **تسليم مقصود**: `passHostBeforeLeaving()` تُنادى داخل `leaveRoom()`
     **قبل** أن يعلن المضيف خروجه — وهو ما زال صاحب صلاحية. المرشّح أقدم
     لاعب نشط بـ `joined_at`، ويُحدَّث `room_players.is_host` و
     `game_rooms.host_player_id` معاً (الثاني يُقرأ عند استعادة الجلسة).
  2. **شبكة أمان**: `syncMyHostStatus()` — إن لم يكن في الروم مضيف إطلاقاً
     (انقطع المضيف أو فشل التسليم) يطالب بها **أقدم لاعب باقٍ وحده**.
     المرشّح محسوم بالترتيب فلا يطالب بها اثنان معاً.

  ⚠️ **`currentPlayer` كائن محلي منفصل عن صفّه في قاعدة البيانات.** ترقيته
  هناك لا تصل إليه وحدها، فيبقى المضيف الجديد محروماً: `isOnlineHost()`
  تكذّبه، وقيود المشاهد مطبّقة، وزر البدء مخفي. لهذا تُزامَن الصفة في
  اشتراك `room_players` اللحظي عند كل تحديث.

  **التحقق** (بجدول وهمي يحاكي `room_players` باستعلام كسول):
  ثلاثة لاعبين، خروج المضيف ينقلها لأقدم الباقين لا لأحدثهم، والمغادر يُوسم
  `left` و`is_host=false`، و`host_player_id` يتبع. وعلى جهاز المرقّى:
  `isOnlineHost()` و`canControlGame()` صارتا `true`، والإشعار يظهر **مرة
  واحدة** لا مع كل تحديث لقائمة اللاعبين، ولا يصل غير المعنيّ.

### 🔵 متبقٍ

- [x] **ربط SMTP خارجي** — أُنجز 2026-07-29 عبر Resend. الرسائل تصل فعلياً.
  ~~القديم:~~ — استعادة كلمة المرور **لا تصل فعلياً** بدونه.
  مُرسِل Supabase المجاني محدود برسائل قليلة في الساعة (أوقف اختباري برسالة
  `email rate limit exceeded`). الحل: Resend أو Brevo مجاناً، ثم
  Supabase → Settings → Authentication → SMTP Settings. ~10 دقائق.

- [ ] **نطاق قصير** — `rajaxdx.github.io/raja-challenge` صعب النطق (~50 ريال/سنة).
  **صار شرطاً لا تحسيناً:** بدونه لا يمكن توثيق نطاق في Resend، فاستعادة
  كلمة المرور لن تصل لأي لاعب غيرك، والزر يبقى مخفياً.

- [ ] **تفعيل «Confirm email»** في Supabase إن أردت تأكيد التسجيل فعلياً
  (يتطلّب SMTP أولاً وإلا عَلِق المسجّلون خارج اللعبة)

- [x] **جودة الخيارات المولّدة** — أُنجز 2026-08-02 (commit 12d7819)
  كانت المشتّتات تُختار بقرب الطول وحده، فتظهر خيارات فاضحة: «206 عظمة» أمام
  سؤال عن عصب، و«تسلا (Autopilot و Full Self-Driving)» أمام «رودولف ديزل».
  **ما نُفِّذ في `buildChoices` (`js/game.js`) — ثلاثة مسارات بالترتيب:**
  1. **مجموعات من نفس النوع** (`ANSWER_POOLS`) — 41 مجموعة: القارات، مناطق
     السعودية، المدن، الدول، الكواكب، الألوان، الأشهر، الأندية، الأنبياء،
     الشركات، البهارات، بحور الشعر، الشعراء، أجزاء السن… إن كانت الإجابة من
     إحداها سُحبت المشتّتات منها
  2. **مشتّتات رقمية** للإجابات الرقمية، مع الحفاظ على وحدة القياس والسنوات
  3. **الملاذ الأخير**: إجابات أخرى من الفئة، بمرشّحات **متدرّجة** على الشكل
     (رقم/لاتيني/طول/عدد كلمات) ثم ترتيب بأول كلمة مشتركة ثم تشابه السؤال
  - **القياس على 2,503 سؤال**: في المسار الثالث نزلت الخيارات الفاضحة من
    **53.4% إلى 3.3%**، وبلا أي سؤال يفشل في توليد خياراته (0 من 2,503)
  - تم التحقق داخل اللعبة فعلياً: فتح سؤال في جولة أونلاين يعرض 4 خيارات
    و`correctIndex` صحيح

- [ ] **خيارات مكتوبة يدوياً** بدل المولّدة — 6,480 خياراً، عمل كبير منفصل.
  صار أقل إلحاحاً بعد تحسين المولّد أعلاه، لكنه يبقى السقف الأعلى للجودة.
  **وبعد البند 45 لم يبقَ ما يستحقّها إلا 5 أسئلة** (إجابة فيها حرف لاتيني
  وفئتها عربية بالكامل) — فلو أردت أرخص خطوة، اكتب خيارات هذي الخمسة وحدها
  بدل الـ6,480.

- [x] **21. نبضة حضور (heartbeat)** — أُنجز 2026-08-03
  من يُقفل المتصفح لا يمرّ بـ `leaveRoom`، فيبقى صفّه `active` إلى الأبد،
  ومضيفٌ غادر فعلاً تظلّ الروم منسوبة إليه فلا أحد يتحكّم بها.
  **ما نُفِّذ:**
  - `supabase-presence.sql`: عمود `last_seen_at` + فهرس على
    `(room_id, status, last_seen_at)` + تعبئة الصفوف القائمة بـ `joined_at`
    (وإلا عُدّ كل من في الرومات غائباً لحظة التشغيل)
  - نبضة كل **25 ثانية**، والغياب بعد **90 ثانية** صمت
  - `startPresence` مع الاشتراك و`stopPresence` مع إلغائه — عمرها عمر
    الوجود في الروم
  - فحص دوري **مستقل عن الاشتراك اللحظي**، فتنتقل الاستضافة حتى لو انقطع البثّ
  - نبضة فورية عند العودة من الخلفية، وإلا بدا اللاعب غائباً وقد رجع
  - `demoteAbsentHosts()` تنزع الصفة عن الغائب صراحةً — بدونها يعود بعد
    انقطاع طويل فيصير في الروم مضيفان يتنازعان

  ⚠️ **لم تُعالَج بـ `beforeunload` + `leaveRoom` عمداً**: تحديث الصفحة
  يُطلقه أيضاً فيصير كل تحديث خروجاً نهائياً وتنكسر «العودة بعد الانقطاع»
  (البند 2). النبضة تفرّق بين الغياب والتحديث لأنها تقيس **الزمن** لا الحدث.

  ⚠️ **المهلة 90 ثانية سخيّة عمداً**: متصفّح الجوال يُجمّد مؤقتات التبويب في
  الخلفية، ومهلة ضيّقة تطرد لاعباً حاضراً لمجرّد أنه فتح واتساب لحظة.

  **التحقق** (جدول وهمي، سبع حالات): مضيف صامت 5 دقائق تنتقل استضافته لأقدم
  **حاضر** لا لأقدم لاعب مطلقاً؛ ومضيف حاضر لا تُسرق منه؛ وصمت 60 ثانية
  (دون المهلة) لا يكفي؛ و**العمود غير موجود → لا حكم بالغياب** فاللعبة تعمل
  قبل تشغيل السكربت؛ والمضيف العائد ينزل بهدوء ولا يستردّها؛ والمؤقتات
  تبدأ وتتوقف بلا تسريب؛ ولا نبضة والصفحة في الخلفية.

  ✅ **`supabase-presence.sql` شُغِّل (2026-08-03).** تم التحقق من قاعدة
  البيانات: العمود موجود، والصفوف القائمة اتعبّت بختم زمني لا `NULL`.
  وظهر أثر جانبي مفيد: خمسة لاعبين كانوا `active` منذ رومات مهجورة صاروا
  يُعدّون غائبين بدل أن يحتجزوا رومهم إلى الأبد.

---

- [x] **22. فحص الاتصال كان على جدول غير موجود** — أُنجز 2026-08-03
  `testSupabaseConnection()` تستعلم عن جدول `categories` — **وهو غير موجود
  إطلاقاً**. الإعدادات كلها صفوف في `game_settings`
  (`categories` / `points` / `question_bank`).
  **الأثر:** الفحص يفشل في **كل تحميل**، فيطبع تحذيراً أحمر في الكونسول
  ويضبط الحالة على «📴 وضع محلي» رغم أن الاتصال سليم تماماً. وبما أن
  `pullFromCloudOnce` تضبطها على «☁️ متزامن»، فما يراه اللاعب **سباق**:
  أيّهما ينتهي أخيراً يحسم المؤشر — ولهذا بدا عشوائياً.
  **الحل:** الفحص صار على `game_settings`.
  **التحقق:** تحميل نظيف — الكونسول بلا أي خطأ، `testSupabaseConnection()`
  ترجع `true`، والمؤشر «☁️ متزامن»، و41 فئة تصل من السحابة.

- [x] **23. جذمور `pushToCloud` مكرّر في `game.js`** — أُنجز 2026-08-03
  كان في `js/game.js` تعريف فارغ لـ`pushToCloud` نصّه «سيتم تنفيذها في
  sync.js». يعمل فقط لأن `sync.js` يُحمَّل **بعده** فيَجُبّه.
  لو انعكس ترتيب الوسمين يوماً لصارت كل عمليات الدفع تنجح **صامتة** بلا أن
  تحفظ شيئاً — وهذا أسوأ أنواع الأعطال. حُذف وبقي تحذير مكانه.

- [x] **24. اللاحقة «عليه السلام» كانت تفضح الإجابة** — أُنجز 2026-08-03
  الإجابة «يونس عليه السلام» تأتي مشتّتاتها من مجموعة الأنبياء **أسماءً
  مجرّدة**، فيبقى الخيار الصحيح وحده يحمل اللاحقة — ينكشف قبل أن يقرأ اللاعب.

  ⚠️ **المحاولة الأولى زادت الطين بلة**: أضفت الأسماء الناقصة (`آدم`، `شعيب`،
  `داوود`) إلى المجموعة ظنّاً أن المشكلة في نقصها، فارتفعت الأسئلة المفضوحة
  من **3 إلى 6 من 6** — لأن الجميع صار يطابق المجموعة فينال مشتّتات مجرّدة.
  **الدرس**: المشكلة لم تكن في المجموعة بل في **إهمال ما بعد الاسم**.

  **الحل**: `findAnswerPool` صارت ترجع `{pool, matchedLength}`، و
  `answerSuffix()` تقتطع ما تبع الاسم في الإجابة، ثم **تُلحق اللاحقة نفسها
  بالمشتّتات الثلاثة** فيستوي الأربعة.
  - أطول عنصر مطابق هو المعتمد («ذو الكفل» تسبق «ذو»)
  - إزالة التكرار بالنص المطبَّع — وإلا ظهر «آدم» و«ادم» **خيارين منفصلين
    لنفس الاسم** (وقع فعلاً وأُصلح)

  **التحقق على البنك كامل (2,570 سؤال)**: المفضوح من **11 إلى 2**، وصفر
  خيارات مكرّرة، وصفر أسئلة تفشل. الاثنتان الباقيتان إجابتهما «**النبي** شعيب
  عليه السلام» فتبدأ بكلمة خارج المجموعة وتسقط للملاذ الأخير.
  ولم تنكسر الحالات العادية: «القاهرة» تنافسها عواصم، و«206 عظمة» أرقام.

- [x] **25. «منو المشهور» صارت قصاصات عيون بالصور** — أُنجز 2026-08-03
  بطلب المستخدم: حُذفت أسئلتها النصّية كلها واستُبدلت بـ**15 صورة** من
  مجلّد `photo` عنده، والإجابة اسم الشخصية المأخوذ من اسم الملف.
  4 سهل · 7 متوسط · 4 صعب.

  **الصور**: نُسخت إلى `assets/famous/` (36 KB إجمالاً) **بأسماء لاتينية**
  لا عربية — أسماء الملفات العربية تحتاج ترميزاً في الروابط وتتكسّر على
  بعض الخوادم. والإجابة العربية في ملف الأسئلة لا في اسم الملف.
  تُشار بمسار نسبي لا `data:` — أخفّ وقابلة للتخزين المؤقت، و`questionVisual`
  تضع المسار في `src` فيعمل الاثنان.

  ⚠️ **نص كل سؤال مختلف عمداً** («من صاحب هذي العيون؟» · «لمن هذي العيون؟»
  · «احزر صاحب هذي العيون» …). السبب: `mergeIntoQuestionBank` تطابق **بنص
  السؤال**، فلو تكرّر النص لَما نجا إلا سؤال واحد من الخمسة عشر.
  وتجنّبنا التلميحات الوصفية («لاعب برتغالي») لأنها تفضح الجواب: المشتّتات
  تأتي من نفس الفئة، وفي مستوى فيه 4 أشخاص يكفي التلميح لحسمه.

  ⚠️ **الحذف احتاج مصدرين**: الملفات فيها 60 سؤالاً بينما **Supabase فيها
  72** — اثنا عشر سؤال «ما المصطلح…» بلا أصل في المشروع. أُضيف الجميع (78
  نصاً الآن) إلى `retired-questions.json`، وإلا بقيت الاثنا عشر في بنك كل
  جهاز بعد الاستبدال. **درس: عند حذف فئة قارن الملفات بالسحابة، لا بالملفات وحدها.**

  ⚠️ **الصور قصاصات عيون صغيرة** (47×34 إلى 208×92)، وكانت تُعرض بحجمها
  الأصلي — شريط لا يكاد يُرى. `max-width` وحدها لا تُكبّر، فأُعطيت `.qphoto`
  عرضاً مقصوداً `min(100%, 440px)` بارتفاع تلقائي حفاظاً على النسبة.

  **التحقق**: 15/15 صورة تُحمَّل، ولا سؤال قديم بقي، والنصوص كلها فريدة،
  وصفر أسئلة تفشل خياراتها. وفي نافذة السؤال: الصورة تُعرض **440×133**
  على سطح المكتب و**269×81** على 375px، بالنسبة محفوظة وبلا تمرير أفقي،
  والخيارات الأربعة أسماء مشاهير من نفس الفئة.

- [x] **26. الشرح الملحق بالإجابة كان يفضحها** — أُنجز 2026-08-03
  إجابات الأسئلة الحسابية تحمل حلّها معها: «36 تفاحة (12x3 =36 )».
  المشتّتات الرقمية تبدّل الرقم الأول فقط، فيبقى الشرح **نفسه حرفياً في
  الخيارات الأربعة وهو يذكر الرقم الصحيح** — يُقرأ فيُعرف الجواب بلا تفكير.
  **الحل**: `stripTrailingNote()` تُسقط ما بين قوسين في آخر الإجابة، وتُبنى
  الخيارات على الرأس وحده. **الإجابة المحفوظة تبقى كاملة** فيظهر الشرح في
  الوضع المحلي عند الكشف — تم التحقق.
  علوج من طرف الكود لا البيانات، فلم تُمَسّ فئات المستخدم.

- [x] **27. النِّسَب كانت تُمسخ بمشتّتات رقمية** — أُنجز 2026-08-03
  «من 1:15 إلى 1:18» كان يصير «من 3:15 إلى 1:18» — تركيب لا معنى له،
  والجزء الثابت يبقى شاهداً على الصحيح. `numericDistractors` صارت ترفض أي
  إجابة فيها نمط نسبة أو مجال (`\d[:/–-]\d`) فتسقط لمشتّتات الفئة.
  الآن تنافسها «المعالجة المغسولة» و«تفاعل ميلارد».

  **التحقق للبندين على البنك كامل (2,513 سؤال)**: صفر تسريب حقيقي، صفر
  خيارات مكرّرة، وصفر أسئلة تفشل. من 369 إجابة رقمية بقيت حالة واحدة
  يرصدها الكاشف وهي **إنذار كاذب** (نسبة يتكرّر فيها الرقم 1 طبيعياً،
  ومشتّتاتها مختلفة تماماً).

- [x] **28. إثراء فئات المستخدم الأربع إلى 20/20/20** — أُنجز 2026-08-03
  كانت **جغرافيا 5** و**الحساب 6** و**الانبياء 6** و**الالغاز 9** أسئلة —
  أي أن الجولة الثانية تكرّر نفس السؤال، والمشتّتات ضعيفة لصغر المجموعة.
  أُضيف **214 سؤالاً** في `data/questions-part12.json`.

  **لم يُحذف ولم يُعدَّل شيء مما كتبه المستخدم** — الدمج إضافة فقط.

  ملاحظات على الصياغة:
  - **الانبياء**: صيغة «… عليه السلام» مطّردة في كل الإجابات، فتستوي
    الخيارات ولا تتميّز الصحيحة بلاحقتها (راجع البند 24)
  - **الحساب**: الإجابات أرقام نظيفة بوحداتها بلا شرح ملحق، فتأتي
    المشتّتات الرقمية سليمة (راجع البند 26)
  - **جغرافيا**: أغلب الإجابات تقع في `ANSWER_POOLS` (عواصم، دول، قارات)
    فتنال مشتّتات من نوعها
  - أسئلة الأنبياء من المتفق عليه في القرآن والسنة، ومسائل الحساب
    تُحقّق حسابياً (204 مربعاً في رقعة الشطرنج = مجموع ك² من 1 إلى 8،
    و950 سنة لنوح، و6 سور بأسماء أنبياء: يونس وهود ويوسف وإبراهيم ومحمد ونوح)

  **التحقق**: الفئات الأربع **20/20/20** بالضبط، صفر نصوص مكرّرة، وصفر
  أسئلة تفشل خياراتها. والبنك كله صار **2,727 سؤالاً** بصفر فشل.

---

### ✨ تحسينات اللعب (2026-08-03) — بطلب المستخدم، أربعة بالترتيب

- [x] **29. مؤقّت للسؤال** — أُنجز 2026-08-03
  المؤقّت الوحيد كان 30 ثانية لوسيلة «اتصال بصديق»؛ السؤال العادي يبقى مفتوحاً
  بلا حدّ، ففريق يفكّر خمس دقائق والبقية ينتظرون.
  **الاختيار**: بدون · 20 · 30 · 45 ثانية، في شاشة الفئات ويُحفظ في `mr_qtimer`.

  ⚠️ **نبثّ لحظة الانتهاء (طابع زمني) لا الثواني المتبقية**: بثّ عدّاد يجعل
  كل جهاز يرى رقماً مختلفاً بمقدار تأخّر الشبكة. الطابع الزمني يجعل الجميع
  يحسبون من نقطة واحدة — تم التحقق: الضيف يستقبل الحالة فيعرض نفس الرقم.

  **عند الانتهاء**: محلياً `award(null)` — بلا نقاط والدور ينتقل. وأونلاين
  تُحتسب إجابة خاطئة مع عرض الصحيحة ورسالة «⏰ انتهى الوقت على فلان».
  الحسم مقصور على صاحب الدور أو المضيف، والاستدعاء المزدوج غير ضارّ لأن
  الدوال تخرج فوراً إذا أُغلق السؤال.

  ⚠️ **`renderTimerPicker` تُنادى من `renderCatGrid` لا من `goToCategories`**:
  للأخيرة **مدخلان** (محلي وأونلاين) فيُنسى أحدهما، وكلاهما يمرّ بالأولى.
  وفي الطريق تبيّن أن **`goToCategories` معرّفة مرتين** — في `utils.js`
  و`game.js` — والثانية تجُبّ الأولى. عُدّلت الميتة أولاً بلا أثر، ووُضع
  تحذير مكانها. (نفس فخّ `pushToCloud`، راجع البند 23.)

  **التحقق**: العدّاد ينقص فعلياً، وينبض أحمر في آخر 5 ثوانٍ، ويُفرَّغ عند
  الانتقال لسؤال بلا مؤقّت (كان يبقى عدّاد السؤال السابق)، والدور ينتقل
  والخانة تُستهلك ولا تُمنح نقاط.

- [x] **30. الأسئلة كانت تتكرّر بين الجولات** — أُنجز 2026-08-03
  `pickFromBank` كان اختياراً عشوائياً محضاً **بلا ذاكرة**، فالسؤال نفسه يعود
  في الجولة التالية. مع 20 سؤالاً لكل خانة و18 خانة: الجولة الثانية فيها
  ~1 مكرّر والخامسة ~3-4. لعائلة تلعب كل ليلة في رمضان هذا محسوس.
  **الحل**: `mr_seen_questions` في `localStorage` تحفظ نصوص ما عُرض لكل
  (فئة/مستوى)، والاختيار من غير المعروض.
  - **عند استهلاك الفئة كاملة تُصفَّر خانتها** وتبدأ دورة جديدة — فلا ننفد
    من الأسئلة أبداً ولا تتوقّف اللعبة
  - سقف `SEEN_MAX_CELLS = 400` خانة يمنع تضخّم التخزين مع مرور الشهور

  **القياس**: 52 سحبة متتالية من فئة فيها 52 سؤالاً → **صفر تكرار** وتغطية
  كاملة، مقابل **21 مكرّراً** بالعشوائي المحض. وثلاث جولات كاملة متتالية
  (54 سؤالاً): **صفر تكرار** مقابل 4.

- [x] **31. تُثبَّت على الجوال وتعمل بلا إنترنت** — أُنجز 2026-08-03
  `manifest.json` + `sw.js`. اللعبة تُضاف لشاشة الجوال وتفتح بلا متصفح،
  والوضع المحلي يعمل بلا شبكة — وهو أكثر مكان تُلعب فيه (استراحة، مزرعة).

  ⚠️ **الشبكة أولاً ثم الكاش، لا العكس.** المشروع يعتمد كلياً على `?v=`
  لإيصال التعديلات (الملاحظة 8)، و«الكاش أولاً» كان سيُبطلها: يبقى اللاعب
  على نسخة JS قديمة **إلى الأبد بلا رسالة خطأ** ويستحيل تشخيصها. بالشبكة
  أولاً يأخذ المتصل الأحدث دائماً، والكاش لا يُستعمل إلا عند الانقطاع.
  **تم التحقق عملياً**: غُيّر محتوى ملف على القرص فجاءت القراءة التالية
  بالمحتوى الجديد لا المخزّن.

  ⚠️ **نداءات Supabase غير معترَضة إطلاقاً** — مزامنة وحالة لعب لحظية،
  وتقديم نسخة مخزّنة منها أسوأ من الفشل الصريح.

  - `SHELL` تشمل **صور «منو المشهور» الـ15**: الفئة كلها صور فبدونها تنكسر
  - رقم الكاش يُرفع عند تغيير `SHELL`، و`activate` يمسح ما سواه
  - فشل تسجيل عامل الخدمة لا يمنع اللعب — النداء داخل `catch`

  **التحقق بإيقاف الخادم فعلياً**: الصفحة أُعيد تحميلها وحمّلت، و**40 فئة
  و2,726 سؤالاً** وصلت، وفُتح سؤال ومُنحت نقاط وانتقل الدور — كله بلا خادم.
  والملف غير المخزَّن يفشل صراحةً بدل أن يُقدَّم شيء خاطئ.

  ⚠️ **كان قيداً وزال بالبند 64**: يوم كُتب هذا كانت البوابة على كل لعب،
  فمن لم يسجّل من قبل يقف عندها بلا إنترنت ولا يستطيع التسجيل. الآن اللعب
  المحلي بلا حساب، فأول مرة بلا شبكة تشتغل. والأونلاين وحده يبقى محتاجاً
  حساباً — وهو محتاج شبكة أصلاً.

- [x] **32. إحصاءات حسب الفئة** — أُنجز 2026-08-03
  الملف الشخصي كان «لعبت/فزت/نقاط» فقط. أُضيف قسم يعرض **أقوى الفئات
  وأضعفها** بنسبة مئوية وشريط، والبيانات كانت موجودة أصلاً بلا استعمال.

  ⚠️ **العنوان «أداء هذا الجهاز» لا «أداؤك»** — مقصود: في الوضع المحلي
  يتشارك الفريقان جهازاً واحداً فلا تُنسب الإجابة إلى شخص. تسميتها «أداؤك»
  تُوهم بدقّة لا وجود لها.

  - الاحتساب في `submitAnswer` (أونلاين، على من أجاب فعلاً عبر `isMyTurn`)
    وفي `award` (محلي فقط، حتى لا يُحتسب مرّتين)
  - **«استريح» لا تُحتسب** — تخطٍّ مقصود لا محاولة. وانتهاء الوقت يُحتسب خطأً.
  - **حدّ أدنى 3 محاولات** لظهور الفئة: نسبة مبنية على محاولة واحدة (0% أو
    100%) تُضلّل أكثر مما تفيد
  - زر لمسح الإحصاءات

  ⚠️ **القسمة إلى «أقوى/أضعف» لا تحدث إلا بأربع فئات فأكثر**: بفئتين كانت
  الثانية — ولو 0% — تُعرض تحت «💪 الأقوى» (وقع فعلاً وأُصلح). دونها يُعرض
  قسم واحد «📊 حسب الفئة».

  **التحقق**: فئتان → قسم واحد؛ ستّ → قسمان مرتّبان صحيحاً؛ فئة بمحاولتين
  لا تظهر؛ زر المسح يفرّغها؛ وعلى 375px بلا تمرير أفقي وارتفاع لمس 40px.

- [x] **32. أداء الجهاز حسب الفئة** — أُنجز 2026-08-03
  الملف الشخصي كان فيه «لعبت/فزت/نقاط» فقط. أُضيف قسم يعرض **أقوى ثلاث فئات
  وأضعفها** بنسبة مئوية وشريط تقدّم، في `mr_cat_stats`.

  ⚠️ **العنوان «أداء هذا الجهاز» لا «أداؤك» — مقصود.** في الوضع المحلي
  يتشارك الفريقان جهازاً واحداً فلا تُنسب الإجابة إلى شخص. تسميتها «أداؤك»
  تُوهم بدقّة لا وجود لها.

  - **لا تُحتسب** «استريح» (تخطٍّ مقصود لا محاولة) — تم التحقق
  - في الأونلاين الاحتساب في `submitAnswer` بشرط `isMyTurn()`، فإجابة لاعب
    آخر لا تُسجَّل على جهازنا — تم التحقق
  - وفي المحلي في `award()` بشرط `!isOnlineGame()` منعاً للازدواج
  - انتهاء الوقت يُسجَّل خطأً (البند 29)
  - **حدّ أدنى 3 محاولات** ليظهر: نسبة مبنية على محاولة واحدة (0% أو 100%)
    تُضلّل أكثر مما تفيد — تم التحقق أن فئة بمحاولتين مخفية وتظهر عند الثالثة
  - زر «مسح هذه الإحصاءات» بتأكيد

  **التحقق**: محلي 3 صح ثم خطأ → 3/4 = 75%؛ وأونلاين 2 صح و1 خطأ → 2/3؛
  والعرض يرتّب تنازلياً بشريط مطابق للنسبة؛ وعلى 375px بلا تمرير أفقي
  وارتفاع لمس 40px.

- [x] **33. شعارات عالمية بالصور** — أُنجز 2026-08-03
  من مجلد `logo` عند المستخدم: **17 شعاراً** (5 سهل · 8 متوسط · 4 صعب) في
  `assets/logos/` بأسماء لاتينية، و`data/questions-part13.json`.

  **الحذف طُلب لاحقاً ونُفِّذ**: الفئة صارت **صوراً بحتة 5/8/4** بعد سحب
  **69 سؤالاً نصّياً**. (أُضيفت أولاً بلا حذف لأن الحذف لم يُطلب حينها —
  الإضافة قابلة للتراجع والحذف لا.)

  ⚠️ **الحذف احتاج المصدرين مرة أخرى**: الملف والسحابة يحملان نفس الـ69
  (جُمع 129 نصّاً، الفريد منها 69). وأُضيفت حماية في السكربت تمنع سحب أي
  نص من أسئلة الصور الجديدة بالخطأ. قائمة السحب صارت **147 نصّاً**.
  الفئتان الأخريان «شعارات» و«شعارات سعودية» لم تُمسّا — تم التحقق.

  ⚠️ `sa logo` **لم يُضف بناءً على طلب المستخدم** — ما زال يعدّله.
  ملفاته جاهزة (10 شعارات سعودية) متى أراد.

  - نص كل سؤال مختلف عمداً (الدمج يطابق بالنص — راجع البند 25)
  - أُضيفت الصور و`part13` إلى `SHELL` في `sw.js` ورُفع الكاش إلى `raja-v3`
  - صُحّحت أخطاء إملائية في أسماء الملفات عند كتابة الإجابات:
    الانقليزي ← الإنجليزي · نيتفلكس ← نتفليكس · تيسلا ← تسلا · نيفيديا ← إنفيديا

  **التحقق**: 17/17 صورة تُحمَّل، صفر نصوص مكرّرة، صفر أسئلة تفشل خياراتها.
  وفي نافذة السؤال: شعار ديزني يُعرض 440×164 بالنسبة محفوظة، وخياراته
  علامات تجارية من نفس الفئة.

  ⚠️ **شعار ويندوز 41×38 بكسل فقط** — أصغر بكثير من البقية، وسيظهر مشوّشاً
  عند التكبير. يُستحسن استبداله بنسخة أوضح بنفس الاسم.

- [x] **35. الحذف لم يكن ينتشر، والمزامنة تعلّق** — أُنجز 2026-08-03
  شكوى المستخدم: «تعلّق، وما تنحذف من كل النسخ، ولما أحدّث الصفحة ألقى
  الفئات المحذوفة».

  **السبب الجذري — الحذف بلا تمثيل:** السحابة تخزّن «الموجود» فقط، وكل جهاز
  **يدمج ولا يستبدل**. فغياب الفئة من السحابة لا يعني «محذوفة» بل «لا معلومة
  عنها»، فيُعيدها أي جهاز عنده نسخة قديمة — وتعود لك أنت عند أول تحديث.
  البند 34 عالجها بقائمة **ثابتة في ملف**، وهذا لا يكفي: كل فئة تُحذف
  مستقبلاً تحتاج تعديل ملف ورفعاً.

  **الحل**: صفّ `retired_categories` في `game_settings`.
  - `deleteCategory` تنشر الاسم في السحابة **قبل** الدفع، فيصل كل جهاز
  - كل جهاز يقرأه في `pullFromCloudOnce` **قبل الدمج** — قراءته بعده تعني
    أن الفئة دخلت ثم خرجت وقد تُحفظ بينهما
  - `unretireCategory` ترفع السحب عند إعادة إضافة الاسم نفسه، وإلا اختفت
    الفئة فور إضافتها وبدا أن الإضافة لم تنجح
  - قائمة الملف تبقى أرضية ثابتة، والاثنتان تتّحدان
  - رسالة الحذف صارت تفرّق: «حُذفت من كل الأجهزة» أو تحذير أن النشر فشل

  **سبب التعليق**: `pushToCloud` كانت **ثلاث `await` متتابعة** (فئات ثم نقاط
  ثم بنك 190 KB) — زمن الشبكة مضروب في ثلاثة. والأسوأ: **نداءات Supabase بلا
  مهلة افتراضية**، فتعثّر رحلة واحدة يترك الوعد معلّقاً للأبد والمؤشر عالقاً
  على «🔄 جاري المزامنة...» بلا خطأ ولا نجاح.
  **الحل**: `upsert` واحد بالصفوف الثلاثة + `withTimeout` بسقف 15 ثانية على
  الدفع والسحب معاً.

  **التحقق**: رحلة **واحدة** فعلية بثلاثة صفوف (بعدّ الرحلات باعتراض الطبقة)؛
  والمهلة تُطلق خطأً صريحاً؛ وفشل الدفع بلا صلاحية يُظهر «⚠️ خطأ بالمزامنة»
  بدل التعليق؛ ووصول حذف من جهاز آخر يُسقط الفئة محلياً، وإعادة الدمج لا
  تُرجعها، ورفع السحب يُرجعها **بأسئلتها الستين**.

- [x] **34. الفئات المحذوفة كانت تعود من ملفات المشروع** — أُنجز 2026-08-03
  حذف المستخدم 15 فئة من لوحة الإدارة، والسحابة صارت **25 فئة**. لكن
  ملفات المشروع كانت تُرجعها كلها عند أول تحميل، فيرى 40.

  **طريقان للعودة، سُدّا معاً:**
  1. `mergeIntoQuestionBank` تُعيد زرع أسئلة الفئة من الملفات، ثم
     `syncCategoriesWithBank` تُعيد اسمها للقائمة
  2. `mergeCategories` تُعيدها من السحابة لو دفعها جهاز قديم لم يُحدَّث

  **الحل**: `data/retired-categories.json` + `retireCategories()` تُسقط
  الفئة من **القائمة والبنك معاً** في كل تحميل، وحارس في `syncCategoriesWithBank`
  وآخر في `mergeCategories`.

  ⚠️ **الإسقاط بعد الدمج لا قبله** — الدمج هو ما يُعيد الزرع، فالإسقاط
  قبله لا يُجدي.

  ⚠️ **لم تُحذف أي أسئلة**: الـ900 سؤال باقية في ملفات `data/`. حذف اسم
  الفئة من `retired-categories.json` يُرجعها كاملة. الحذف قرار قابل للتراجع.

  📌 **كلما حذف المستخدم فئة من لوحة الإدارة، أضِف اسمها إلى
  `data/retired-categories.json`** — وإلا عادت عند أول تحميل.

  **التحقق**: 25 فئة بالضبط، صفر زائدة وصفر ناقصة، والبنك 25 فئة و1,752
  سؤالاً. وزرعُ فئة محذوفة يدوياً ثم إعادة الدمج يُسقطها، ومزامنة السحابة
  لا تُعيدها، وأسئلتها ما زالت في الملفات.

### 📌 اعتماد الفئات والشعارات السعودية + كشف (2026-08-04) — بطلب المستخدم

- [x] **36. الفئات الحيّة صارت هي الأصلية في الكود** — أُنجز 2026-08-04 (commit 7e52f55)
  `DEFAULT_CATEGORIES` كانت 35 فئة من زمن البداية، **سبع عشرة منها مسحوبة
  فعلياً**. فالجهاز الجديد كان يبدأ بقائمة ميتة ثم تُصحَّح عند أول مزامنة —
  يرى فئات ليست في اللعبة، ولو تعثّرت الشبكة بقي عليها.
  صارت **24 فئة** منسوخة حرفياً من صفّ `categories` في `game_settings` بعد
  أن استقرّ المستخدم عليها، ومنها الخمس التي أضافها بنفسه.
  - `سيارات` و`تكنولوجيا` كانتا مسحوبتين في **السحابة وحدها** — أُضيفتا إلى
    `data/retired-categories.json` (الأرضية الثابتة)، وإلا أعادتهما ملفات
    المشروع لأي جهاز يفتح اللعبة قبل وصول قائمة السحابة
  - أيقونات فئات المستخدم تُركت `✨` كما هي في السحابة: أسماؤها تحمل رمزها
    داخلها («جغرافيا 🌎») فوضع الرمز أيقونةً يُظهره مرّتين

  ⚠️ **«ولا كلمة 🤫» بلا سؤال واحد** وضُمّت بطلب صريح. تم التحقق أنها **لا
  تكسر شيئاً**: فتح خانتها يعرض «ما فيه سؤال محفوظ لهذه الفئة بعد 🙂» بلا خطأ
  ولا تجمّد، والخانة لا تُستهلك. لكنها تظهر للاعبين كخيار فارغ.

- [x] **37. «شعارات سعودية» صارت صوراً بحتة** — أُنجز 2026-08-04 (commit 7e52f55)
  **21 شعاراً** (7 سهل · 7 متوسط · 7 صعب) من مجلد `sa logo` عند المستخدم،
  في `assets/sa-logos/` بأسماء لاتينية و`data/questions-part14.json`.
  (كانت مؤجّلة في البند 33 لأن المستخدم ما زال يعدّل المجلد.)

  ⚠️ **الحذف احتاج المصدرين للمرة الثالثة**: الملفات فيها 60 نصّاً والسحابة
  69 (65 فريداً)، فسُحب **65 نصّاً** لا 60. الاعتماد على الملفات وحدها كان
  سيُبقي خمسة أسئلة نصّية حيّة على أجهزة اللاعبين. قائمة السحب: 147 ← 212.
  والسكربت يحمي نصوص أسئلة الصور الجديدة صراحةً من السحب بالخطأ، ويرفض سحب
  أي نص تستعمله فئة أخرى (`retiredTexts` عالمية لا مقصورة على فئة).

  - نص كل سؤال مختلف عمداً (الدمج يطابق بالنص — البند 25)
  - **بلا تلميح لنوع الجهة** («بنك»/«وزارة»): المشتّتات من نفس الفئة، وفيها
    ستّ وزارات وأربعة بنوك، فالتلميح وحده كان يحسم الجواب. الصيغة المعتمدة
    «جهة» في الجميع
  - `المراعي` (1448×1086) و`سبكيم` (1600×750) صُغِّرتا إلى 440px —
    المجلد من 250 KB إلى **130 KB**
  - أُضيفت الصور و`part14` إلى `SHELL` في `sw.js` ورُفع الكاش إلى `raja-v5`

  ⚠️ **`معادن` 57×35 بكسل فقط** — يُعرض بتكبير **4.7×** على الجوال و**7.7×**
  على سطح المكتب، فسيبدو مشوّشاً. نفس حالة `windows.jpg` في البند 33.
  يُستحسن استبداله بنسخة أوضح بنفس الاسم.

- [x] **38. الفئات المسحوبة كانت تُدمج 1,020 سؤالاً كل تحميل ثم تُرمى**
  — أُنجز 2026-08-04 (commit 7e52f55)، اكتُشف أثناء الكشف.
  `mergeIntoQuestionBank` تدمج كل ما في الملفات، و`retireCategories()` تحذف
  الفئات المسحوبة **بعدها**. فسبع عشرة فئة مسحوبة × 60 = **1,020 سؤالاً**
  تُبنى في الذاكرة في كل تحميل صفحة لتُحذف بعد سطور.
  والأثر الأوضح: `added` كان يعدّها، فيُطبع «📚 تم تحميل 1020 سؤال جديد» في
  الكونسول **كل مرة** — رقم لا يقابله شيء في اللعبة ويُضلّل أي تشخيص لاحق.
  **الحل**: الفئة المسحوبة لا تُزرع أصلاً. `retireCategories()` باقية في
  محلها لتنظيف ما جاء من `localStorage` أو السحابة قبل وصول القائمة.
  **التحقق**: قبل الإصلاح سطران في كل تحميل ثانيهما 1,020 دائماً؛ بعده
  **لا سطر إطلاقاً** في التحميل المستقر، وبنفس النتيجة: 24 فئة و1,576 سؤالاً.

- [x] **39. ثلاثة أسئلة كانت في مستويين معاً** — أُنجز 2026-08-04 (commit 94e1d18)
  نفس النص ونفس الإجابة في 250 و400 من الفئة نفسها، فيمكن أن يأتي السؤال
  مرتين في الجولة الواحدة: «الطبق الحساوي» و«الحلى الشعبي» في مطاعم السعودية،
  و«قلوب الأخطبوط» في عالم الحيوان (نسختها الثانية في السحابة لا في الملفات).

  ⚠️ **لا يكفي حذف النسخة الزائدة من الملفات**: النسخة السحابية تبقى على
  أجهزة اللاعبين. والسحب يطابق **بالنص** فيقتل النسختين معاً — فالعلاج
  خطوتان: سحب النص، ثم إعادة السؤال **مرة واحدة** بصياغة جديدة في
  `data/questions-part15.json`، مع سؤال بديل للمستوى الذي فقد سؤاله.

  وفي الطريق ظهر عيبان في المسحوبَين نفسيهما:
  - «الحلى المصنوع من **الدقيق المحمّص بالسمن**» إجابته «المحلبية» —
    والمحلبية من الحليب والنشا. أُبقيت الإجابة كما كتبها المستخدم وصُحّح
    **السؤال** ليطابقها، وأُضيف «الحنيني» سؤالاً مستقلاً لوصف الدقيق والتمر
  - «الطبق الحساوي» إجابته «مجبوس أو مرقوق» — إجابتان في واحدة، و«المرقوق»
    إجابة سؤال آخر في الفئة نفسها و«المجبوس» جزء من ثالث. سُحب ولم يُعَد،
    وحلّ محلّه سؤالان نظيفان

  **التحقق**: صفر نصوص مكرّرة في البنك كله (كانت 3)، والنصوص الثلاثة اختفت
  فعلاً، والبدائل الخمسة وصلت بخياراتها، و**1,575 سؤالاً بصفر فشل وصفر
  خيارات مكرّرة**. العدّ بعد التغيير: مطاعم السعودية 22/22/22 وعالم الحيوان
  22/22/21 — كلها فوق الحدّ.

- [x] **40. سقف لتكبير الصور الصغيرة** — أُنجز 2026-08-04
  `.qphoto` تعطي كل صورة عرضاً مقصوداً 440px، وهذا صحيح للصورة الكبيرة
  وخاطئ للصغيرة: «معادن» 57×35 كان يُعرض بتكبير **7.7×** فتبدو حوافه
  ضبابية، ومثله شعار ويندوز 41×38 (المرصود في البند 33).
  **الحل**: `capPhotoUpscale` تضبط `max-width` بـ`naturalWidth × 3`.

  ⚠️ **لا يُعالَج في CSS**: الحدّ يعتمد على أبعاد الصورة الأصلية، و
  `naturalWidth` لا تُعرف قبل التحميل — فالضبط في `onload` على كل صورة.
  و⚠️ **`max-width` لا `width`**: العرض المقصود يبقى كما هو ولا يُقصّ إلا
  عند اللزوم، والصورة الكبيرة لا يمسّها شيء، وعلى الجوال يبقى `min(100%,…)`
  هو الحاكم فلا تفيض الصغيرة عن الشاشة.

  **التحقق على الصور الـ53 عند 1280px**: لا صورة تتجاوز **3.0×** (كان
  الأقصى 10.7×)، و**27 صورة لم تتغيّر** إطلاقاً (عرضها الأصلي ≥ 147px).
  وفي جولة حقيقية من 18 سؤالاً: `onload` يعمل فعلاً — كل صورة عليها
  `max-width` مضبوط، وصفر أخطاء، والجولة تصل شاشة الفوز.

  ⚠️ **الثمن**: أصغر ثلاث صور صارت صغيرة على سطح المكتب —
  ويندوز **123px** وموسوليني **141px** ومعادن **171px** بدل 440.
  المقايضة مقصودة (وضوح مقابل حجم)، ورفع المعامل إلى 4× يوسّعها
  (ويندوز 164 · معادن 228) بضبابية أعلى. المعامل في `MAX_PHOTO_UPSCALE`.
  **الأصل يبقى: استبدال الصور الثلاث بنسخ أوضح يُغني عن المقايضة كلها.**

#### 🔎 نتيجة الكشف (2026-08-04) — تشغيل فعلي لا قراءة كود

| الفحص | النتيجة |
|---|---|
| توليد الخيارات على **1,575 سؤالاً** | **صفر فشل · صفر خيارات مكرّرة · صفر تسريب** |
| تحميل الصور (53 صورة: 15 مشاهير + 17 شعاراً عالمياً + 21 سعودياً) | **53/53 تُحمَّل** |
| جولة محلية كاملة 18 سؤالاً حتى شاشة الفوز | ✅ النقاط والدور والخانات المستهلكة وشاشة الفائز |
| كشف الإجابة داخل السؤال | ✅ النص يتبدّل إلى الإجابة ويرجع |
| أخطاء الكونسول في تحميل نظيف | **صفر** خطأ وصفر تحذير |
| الجوال 375px | بلا تمرير أفقي، والصور بنسبتها محفوظة |
| فئة «ولا كلمة 🤫» الفارغة | تتدهور بلطف — رسالة بلا خطأ |
| مؤشر المزامنة | ☁️ متزامن |

**ما لم يُغطَّ**: الأونلاين بجهازين حقيقيين عبر Supabase (البوابة تطلب حساباً
ولا تُدخَل كلمات مرور في الاختبار الآلي)، ولقطات الشاشة (لوحة المتصفح لم تكن
معروضة — كل الأرقام أعلاه قياسات `offsetWidth`/`naturalWidth` فعلية).

⚠️ **الثغرة الأمنية في جداول الرومات ما زالت مفتوحة** — أُعيد التحقق فعلياً
بمفتاح `anon` بلا تسجيل دخول: `game_rooms` و`room_players` و`room_chat`
و`room_game_state` **ترجع صفوفاً فعلية** (رسائل شات بأسماء أصحابها).
و`profiles` و`room_invites` و`app_events` ترجع **صفراً** — أي أن الخلل
محصور في جداول الرومات كما هو موصوف أسفل هذا الملف.

### 🔎 فحص الفئات المضافة من المستخدم (2026-08-03) — تشخيص بلا تعديل

خمس فئات في Supabase ليست في ملفات المشروع ولا في `DEFAULT_CATEGORIES`.
**لم يُعدَّل فيها شيء** — هذا تقرير فقط.

| الفئة | 100/250/400 | الحالة |
|---|---|---|
| الالغاز 🧩 | 3/4/2 | الأسلم — إجابات الألغاز مشتّتات جيدة لبعضها |
| الانبياء 🌙 | 2/2/2 | ⚠️ «عليه السلام» تفضح الإجابة |
| الحساب 🧮 | 2/2/2 | ⚠️ الشرح داخل الإجابة يفضحها |
| جغرافيا 🌎 | 2/2/1 | خانة 400 بسؤال واحد |
| ~~ب~~ | ~~1/0/0~~ | ❌ **غير موجودة في السحابة** — راجع التصحيح أسفله |

**1. ~~«ب» فئة تجريبية~~ — ❌ تصحيح (2026-08-03):**
**«ب» غير موجودة في السحابة إطلاقاً** — لا في `categories` ولا في
`question_bank`. السحابة فيها 40 فئة نظيفة.

كانت موجودة في `localStorage` عند متصفّح الفحص وحده، فبَنيتُ التقرير على
النسخة المحلية وحسبتها الحقيقة.
**الدرس المنهجي: عند فحص البيانات ارجع إلى `game_settings` في السحابة، لا
إلى `CATEGORIES` و`QBANK` في المتصفح** — الثانية قد تحمل بقايا لا وجود لها.

⚠️ **وهذا إثبات عملي أن الحذف لا ينتشر** (الملاحظة 3): `mergeCategories`
تدمج ولا تحذف، فالفئة المحذوفة من السحابة تبقى في كل جهاز حمّلها من قبل.
**والخطر أن أي دفع لاحق من ذلك الجهاز يُعيدها للسحابة وللجميع** — وهذا
ينطبق على أي فئة تُحذف، لا على «ب» وحدها.

**2. الحساب 🧮 — الشرح يفضح الإجابة في 5 من 6.** الإجابة تحوي الحل كاملاً،
والمولّد يبدّل الرقم الأول فقط فيبقى الشرح **نفسه في الخيارات الأربعة**
وهو يذكر الرقم الصحيح:
`36 تفاحة (12x3 =36 )` · `41 تفاحة (12x3 =36 )` · `28 تفاحة (12x3 =36 )`
وكذلك `العدد هو 5 (لأن 3 × 3 = 9…)` والصحيح `العدد هو 3`.
✅ **عولج 2026-08-03 من طرف الكود** (البند 26) — بيانات المستخدم لم تُمَسّ.

**3. الانبياء 🌙 — «عليه السلام» تفضح في 3 من 6.** السبب دقيق:
`findAnswerPool` تطابق **بداية** الإجابة، فـ«يونس عليه السلام» و«هود» و«موسى»
موجودة في مجموعة الأنبياء فتأتي مشتّتاتها أسماءً مجرّدة — والصحيح وحده يحمل
اللاحقة. أما «ادم» و«داوود» و«شعيب» فليست في المجموعة (المجموعة فيها `داود`
لا `داوود`، ولا آدم ولا شعيب)، فتسقط للفئة نفسها حيث الجميع يحملون اللاحقة
فتستوي الخيارات.
✅ **عولج 2026-08-03 من طرف الكود وحده** (البند 24) — فئات المستخدم لم تُمَسّ.

**4. ~~الشُّح عام~~** ✅ **عولج 2026-08-03** (البند 28): كانت 5–9 أسئلة
للفئة مقابل 60 في فئات المشروع، فتتكرّر في الجولة الثانية ومشتّتاتها ضعيفة
لصغر المجموعة. صارت كلها **20/20/20**.

### 🔐 ثغرات أمنية مفتوحة

- [x] **جداول الرومات مفتوحة للجميع** — ✅ أُغلقت 2026-08-04 (راجع البند 41).
  في `supabase-setup.sql` كل السياسات على `game_rooms` و`room_players` و
  `room_chat` و`room_game_state` هي `USING (true)` للقراءة والإضافة والتعديل
  والحذف. ومفتاح `anon` منشور في `js/supabase-config.js` (وهذا طبيعي)،
  فأي شخص يفتح أدوات المطوّر يستطيع:
  - قراءة **كل** الرومات وكل رسائل الشات لكل اللاعبين
  - تنصيب نفسه مضيفاً على أي روم (`is_host = true`)
  - طرد أي لاعب (`status = 'left'`) أو تغيير فريقه أو نقاطه
  - تزوير حالة اللعبة كاملة في `room_game_state`
  - **حذف** أي روم أو لاعب أو رسالة

  الفرق أن `profiles` و`room_invites` و`game_settings` و`app_events` **مقفلة
  فعلاً** ومُتحقَّق منها — الخلل محصور في جداول الرومات وحدها.

  **الاتجاه الصحيح**: ربط الصف بصاحبه (`auth.uid()`) بدل `player_id` نصّي،
  وقصر التعديل على صاحب الصف، وجعل ما يخصّ الاستضافة والطرد يمرّ بدالة
  `SECURITY DEFINER` تفحص أن المنادي هو المضيف — كما فُعل في `invite_friend()`.
  ⚠️ عمل غير هيّن: يمسّ `createRoom` و`joinRoom` و`assignPlayerToTeam`
  و`kickPlayer` و`transferHostTo` معاً.

  ✅ **الكود نُشر والسكربتان شُغِّلا (2026-08-04)** — راجع البند 41.

- [x] **41. إغلاق ثغرة الرومات — الكود** — أُنجز 2026-08-04
  **⚠️ لم يُشغَّل أي سكربت على قاعدة البيانات، والثغرة ما زالت مفتوحة فعلياً
  حتى تُشغَّل.** ما أُنجز هو الكود والسكربتات وترتيب النشر.

  **الترتيب إلزامي — أي خلط يكسر الأونلاين:**
  1. `supabase-rooms-security-1.sql` — **إضافة محضة** (عمودان و8 دوال).
     اللعبة المنشورة تبقى تعمل بعده كما هي.
  2. نشر الكود الجديد (`git push`) والتأكد من وصوله.
  3. إغلاق الرومات المفتوحة من لوحة الإدارة (🧹 تنظيف الرومات).
  4. `supabase-rooms-security-2.sql` — **هذا الذي يُغلق الثغرة**.

  **لماذا مرحلتان**: الكود الجديد ينادي دوالّ لا وجود لها قبل السكربت 1،
  والسكربت 2 يمنع التحديثات المباشرة التي يعتمد عليها الكود القديم. فلا
  بدّ من نافذة يعمل فيها الاثنان معاً.

  ⚠️ **الصفوف القائمة `user_id = NULL`** ولا سبيل لنسبتها لأصحابها بأثر
  رجعي — لذلك تُغلق الرومات المفتوحة في الخطوة 3، وإلا صار من فيها غير
  قادر على تعديل صفّه.

  **ما يتغيّر في الكود:**
  - الدخول بالكود عبر `find_room_by_code()` بدل قراءة `game_rooms` مباشرة
    (القراءة صارت للأعضاء، والداخل ليس عضواً بعد — حلقة مفرغة). وفائدة
    ثانية: **لا أحد يستطيع تعداد الرومات**، من معه الكود وحده يدخل
  - توزيع الفريق والطرد ونقل الاستضافة والنقاط: دوالّ `SECURITY DEFINER`
    تفحص المنادي، بدل تحديثات مباشرة على صفوف الآخرين
  - `demoteAbsentHosts` **حُذفت** — نزع الصفة صار داخل `claim_room_host()`
    في نفس معاملة المطالبة. كان نداءين منفصلين قد ينجح أحدهما فتبقى الروم
    بمضيفَين أو بلا مضيف
  - `leaveRoom` ما عادت تكتب `is_host: false` — العمود ممنوع على اللاعب،
    وإدراجه كان سيجعل كل خروج يفشل

  ⚠️ **ثلاثة مواضع كانت ستبقى مفتوحة لولا الانتباه لها:**
  1. **صلاحيات الأعمدة لا RLS**: RLS تحرس الصفوف لا الأعمدة. بـ«عدّل صفّك»
     وحدها يكتب أي عضو `is_host = true` في صفّه فيصير مضيفاً، أو
     `score = 9999`. الحلّ `REVOKE UPDATE` ثم `GRANT UPDATE (أعمدة بعينها)`.
  2. **والإدخال أيضاً**: صلاحيات الأعمدة في السكربت تحرس `UPDATE` وحده،
     فكان بالإمكان الدخول بروم بكودها مع `is_host = true` من أول لحظة.
     سُدَّت بشرط في سياسة الإدخال: الصفة لا تُقبل إلا لمن هو نفسه المضيف
     المعلن في `game_rooms.host_player_id` — أي منشئ الروم وحده.
  3. **`SECURITY DEFINER` بلا `search_path` مثبّت** تسمح بزرع دالة بنفس
     الاسم في مخطّط أسبق فتُنفَّذ بصلاحية المالك. كلها `SET search_path`.

  ⚠️ **ما بقي مفتوحاً عمداً** (وليس سهواً): النقاط وحالة اللعبة **لأي عضو
  في الروم** لا للمضيف وحده. السبب أن صاحب الدور يبثّ الحالة وقد لا يكون
  مضيفاً (البند 13)، وقصرها على المضيف يُجمّد الدور عند من لعب. النموذج
  التهديدي هنا **الغريب** لا زميلك: من دخل بالكود جالس معك.

  **ما تم التحقق منه فعلاً:**
  - أسماء الدوالّ الثمانية ووسائطها في SQL تطابق نداءات `rpc()` في الكود
    حرفياً — قورنت آلياً، صفر اختلاف
  - باعتراض طبقة النقل: الأفعال الخمسة تمرّ بالدوالّ بوسائط صحيحة، ولم
    يبقَ أي تحديث مباشر على صفوف الآخرين
  - الدخول بالكود يترجم أعمدة `out_*` لأسمائها الثمانية، واللاعب الجديد
    يُدرَج بـ`is_host: false` كما تشترط السياسة
  - الوضع المحلي لم يتأثر: جولة 18 سؤالاً حتى شاشة الفوز بصفر أخطاء

  ✅ **شُغِّل السكربتان (2026-08-04).** التحقق من القاعدة: صفر صلاحية لـ`anon`،
  و15 سياسة جديدة، وصفر سياسة مفتوحة باقية، وأعمدة `room_players` القابلة
  للتحديث هي الأربعة المقصودة وحدها. و`find_room_by_code` نُوديت من الموقع
  المنشور فردّت بلا خطأ.
  ⚠️ **يبقى غير مختبر بيد إنسان**: جولة أونلاين بجهازين حقيقيين بعد الشدّ.

  📌 وفي آخر السكربت 2 دفعة «العودة» مُعطَّلة بتعليق تُرجع الحال كما كان
  إن ظهر عطل لاحقاً.

  ⚠️ **العدد 15 لا 16**: كتبتُ 16 في تعليق السكربت أول مرة وهو خطأ عدّ
  (5 رومات · 3 لاعبين · 3 شات · 4 حالة = 15). صُحِّح في الملف.

- [x] **42. زر الحظر (Ban) في لوحة الإدارة** — أُنجز 2026-08-15 (commit f8a65c1)
  حظر لاعب يمنعه هو **وجهازه** من الدخول أو إنشاء حساب جديد. بطلب المستخدم.

  ✅ **الكود منشور و`supabase-bans.sql` مُشغَّل (2026-08-15).** التحقق من
  القاعدة: الدوال الستّ موجودة، وعمود `covers_devices` موجود (أي أن تصحيح
  خيار «احظر أجهزته» وصل). الميزة حيّة فعلياً لا مكتوبة فقط.

  📌 لو نُقلت لمشروع Supabase آخر: بلا تشغيل السكربت يظهر الزر ويعمل الفحص
  لكنه يردّ «غير محظور» دائماً (نفشل مفتوحين عمداً — راجع رأس `js/bans.js`).

  **الطبقات الثلاث وقوّتها ليست واحدة — وهذا جوهر الميزة:**
  1. **الحساب** (`user_id`): مفروض في **قاعدة البيانات** لا في المتصفح —
     شرط `NOT is_banned_now()` في سياسات إدخال `game_rooms` و`room_players`
     و`room_chat`. لا يُلتَفّ عليه بأدوات المطوّر إطلاقاً.
  2. **معرّف الجهاز** (`mr_device_id`): يمنع الحساب الجديد من نفس الجهاز.
     يكسره مسح بيانات الموقع.
  3. **بصمة الجهاز**: نوع الجهاز والشاشة والمتصفح والمنطقة الزمنية ورسم
     canvas مجموعة في SHA-256. **لا تُخزَّن في localStorage عمداً** —
     فائدتها كلها أنها تبقى بعد مسحه وفي النافذة الخاصة.

  **جدول `device_accounts` هو ذاكرة الحظر**: بدونه تحظر الحساب فيعود
  صاحبه بحساب جديد من نفس الجهاز، أو تحظر الجهاز فيعود بحسابه من جهاز
  آخر. يسجّل «من لعب من أين» فيمتدّ الحظر على السلسلة كلها.

  ⚠️ **تصحيح بعد أول تشغيل للسكربت (2026-08-15)**: خيار «احظر أجهزته» كان
  **كاذباً** — قاعدة السلسلة في `check_ban` تحجب الجهاز متى كان صاحبه محظوراً
  سواء أطفأت الخيار أم لا، فمن أراد حظر شخص دون أن يمسّ الجهاز الذي يشاركه
  إخوته لم يكن يجد سبيلاً. أُضيف عمود `bans.covers_devices` وصار شرطاً في
  السلسلة. **يتطلّب إعادة تشغيل الدفعات 2 و3 و5** (الملف كله قابل لإعادة
  التشغيل بأمان). وإعادة الحظر بنطاق أوسع صارت **توسّعه فعلاً** بدل أن
  تُهمل بصمت لوجود حظر قائم.

  ⚠️ **الحظر لا يمسك الجهاز إلا بعد أن يُسجَّل**: `device_accounts` يمتلئ من
  `register_device()` عند كل فتح/دخول بالكود الجديد. فقبل نشر الكود — أو
  للاعب لم يفتح اللعبة بعده — الحظر يقع على **الحساب وحده**، وتقول رسالة
  اللوحة ذلك صراحةً («لم يُسجَّل له جهاز بعد»).

  ⚠️ **البصمة وحدها هي التي قد تُخطئ**: جهازان متطابقان تماماً (نفس
  الموديل والنظام والمتصفح والمنطقة) قد يعطيان البصمة نفسها فيُحظر بريء.
  لذلك هي **خيار** في نافذة الحظر (مفعّل افتراضياً مع تحذير مكتوب)،
  ورفعها سطراً واحداً من قائمة المحظورين.

  ⚠️ **`bans.user_id` بلا مفتاح أجنبي عمداً**: مع `ON DELETE CASCADE` كان
  حذف الحساب يمحو سجلّ حظره — أي أن الحذف يرفع الحظر، عكس المقصود.

  ⚠️ **سياسة الشات استُبدلت بنفس الاسم** (`chat_insert_self`) لا أُضيفت
  باسم جديد: سياسات الإدخال المتعدّدة تُجمع بـ`OR`، فالقديمة كانت ستبقى
  تسمح للمحظور والحظر لا يفعل شيئاً.

  **ما تم التحقق منه محلياً** (بمحاكاة ردّ القاعدة):
  - شاشة الحظر تظهر بالسبب، بلا مخرج، والجلسة تُنهى، وترس الإدارة يُخفى
  - صفوف اللوحة الثلاثة: محظور (أحمر + «رفع الحظر») · عادي («⛔ حظر») ·
    إدمن (بلا زر حظر أصلاً، مطابقةً لرفض القاعدة حظر الإدمن)
  - قائمة المحظورين تعرض الأنواع الثلاثة (حساب/جهاز/بصمة) وترفع كلاً منها
  - غياب الدوال من Supabase يمرّ بتحذير في الطرفية واللعبة تعمل طبيعياً
  - الجوال 375px: لا فيض أفقي

  **ما لم يُختبر**: الحظر الحقيقي بعد تشغيل السكربت، وأن المحظور فعلاً لا
  يُنشئ روماً — يحتاج جهازين وحساباً حقيقياً.


- [x] **43. سقف اللوحة 6 فئات + سؤال المراهنة الأخير** — أُنجز 2026-08-15 (commit f4fe75e)
  بطلب المستخدم: «سؤال المراهنة يكون ألف نقطة + يكون اختياري + الجولة حد فئاتها 6».

  **سقف الفئات**: كان الاختيار مفتوحاً و`startGame` تقسّمه كل 6 فئات جولةً
  مستقلة، فالجلسة بلا نهاية واضحة. صار `MAX_CATS = 6` — لوحة واحدة، 18 سؤالاً.
  (تبويبات الجولات تختفي وحدها: `renderTabs` تخرج عند `rounds.length <= 1`.)

  **المراهنة**: بعد آخر خلية، كل فريق يراهن حتى **1000** على سؤال **صعب**
  من إحدى فئات اللوحة. صحّ يربحها، غلط يخسرها. مفتاح تشغيلها في شاشة الفئات
  محفوظ في `mr_bet`.

  ⚠️ **السقف الحقيقي = الأقل من 1000 ومن رصيد الفريق.** بلا شرط الرصيد
  يراهن فريقٌ بلا نقاط بألف: لا شيء يخسره وكل شيء يكسبه. ولهذا لا حاجة
  لتصفير النتيجة السالبة أصلاً.

  ⚠️ **المراهنة سرّية حتى يقفل الفريقان** — حتى في الوضع المحلي. الفريقان
  على جهاز واحد، فرقم الأول لو بقي معروضاً لاختار الثاني رقمه وهو يعرف
  كم يكفيه بالضبط.

  ⚠️ **السؤال يُنتقى مرة واحدة على جهاز واحد ويُبثّ**. `pickFromBank` عشوائية
  وذاكرة «ما عُرض» محلية، فلو انتقاه كل جهاز لاختلف السؤال بين اللاعبين.

  ⚠️ **`mergeBetState` دمج لا استبدال، وهي جوهر المزامنة هنا.** فريقان
  يقفلان في اللحظة نفسها: كلٌّ يبثّ حالةً لا تعرف قفل الآخر، وآخر بثّ يفوز
  فيضيع قفلٌ ويتجمّد الجميع في انتظاره. الدمج يأخذ كل قفل وكل إجابة من أي
  طرف، ومن عرف أكثر ممّا وصله أعاد البثّ. و`betPublishGrace` تفتح البثّ لأي
  لاعب في هذه المرحلة — لا دور فيها أصلاً.

  **التحقق (محلي)**: 6 فئات، 18 خلية، شاشة المراهنة، كشف، حكم، تطبيق
  ‎+750/−1000، وشاشة الفوز مطابقة. وفريق برصيد 300 لا يُعرض له إلا 0 و250.
  والمفتاح مطفأ ← شاشة الفوز مباشرة بلا حالة مراهنة. والفئة السابعة مرفوضة.

  **التحقق (جهازان)**: إطار من نفس المصدر كجهاز ثانٍ، باعتراض طبقة النقل
  وحدها (`updateRoomGameState`) كما في البند 13 — نفس السؤال ونفس الخيارات
  الأربعة على الجهازين، والضيف لا يملك إلا فريقه، وسباق القفل تقارب بإعادة
  بثّ واحدة من كل طرف، والنتيجة والنقاط وشاشة الفوز متطابقة، وصفر أخطاء.

  📌 الإطار من نفس المصدر أنجع من تبويبين: `iframe.contentWindow.eval(...)`
  يصل لمتغيّرات `let` في نطاق السكربت (راجع الملاحظة 1)، والنقل بينهما
  استدعاء مباشر بلا تنسيق يدوي.

- [x] **44. إحساس اللعبة: زحف النقاط والكونفيتي** — أُنجز 2026-08-15 (commit 50f1026)
  النقاط كانت تُبدَّل بكتابة نصّ صامتة — وهي الحدث الوحيد الذي يهمّ اللاعبين.
  صارت تزحف بنبضة، وشاشة الفوز تعدّ المجموعين من الصفر تحت كونفيتي، ونتيجة
  المراهنة تزحف من الرصيد **قبل** الرهان فيُرى أثر الرهان نفسه. والخيار
  الخاطئ يهتزّ والصحيح ينبض. الأصوات كانت مميّزة أصلاً فلم تُمَسّ.

  ⚠️ **`animateNumber` لا تثق بـ`requestAnimationFrame` وحدها.** المتصفح
  يوقفها في التبويب المخفيّ، فمن يعطي نقاطاً ثم يفتح واتساب يعود لرقم
  متجمّد وسط الزحف — أو لأصفار في شاشة الفوز، وهي أسوأ لحظة. مؤقّت يفرض
  القيمة النهائية مهما جرى. **وهذا ليس فرضاً**: هو بعينه ما وقع في الفحص
  الآلي (الملاحظة 10: اللوحة مخفية فلا تتقدّم الحركة) — ظهرت النتائج أصفاراً
  حتى أُضيف الحارس.

  ⚠️ `.confetti-layer` لها `pointer-events: none` — بدونها حجبت زرّي
  «شارك النتيجة» و«لعبة جديدة» تحتها.

  و`prefers-reduced-motion` يكتب الرقم فوراً ويلغي الكونفيتي كاملاً.

- [x] **45. الإجابة كانت تُعرف بشكلها لا بمعناها** — أُنجز 2026-08-15 (commit e90abab)
  بقي تسريبان بعد تحسين المولّد (2026-08-02)، وكلاهما بنيوي لا مسألة كلمات:

  1. **الشرح الملحق لا يحمله إلا الصحيح**: «صلم (Salm)» وحده بين ثلاثة
     عربية، و«نهر النيل (ويُنازعه الأمازون)» وحده الطويل. `stripTrailingNote`
     كانت موجودة لكنها تُستدعى في المسار الرقمي وحده. صارت تمرّ على الصحيح
     **وعلى المجموعة**، والتفريد يقع على النص المقطوع — وإلا دخل «جبل السودة»
     و«جبل السودة (3133 متر)» خيارين لشيء واحد. **الإجابة المحفوظة كما هي**
     فيظهر الشرح عند الكشف محلياً (تم التحقق).
  2. **مستويات الفئة كانت تُجمع عند «أقل من 12» فقط**: مستوى مليء بالأرقام
     يكفي العدد ولا يكفي **الشكل**، فوقفت «أرامكو السعودية» أمام «حوالي 50%»
     و«بئر الدمام رقم 7». الجمع صار غير مشروط: اختلاف المستوى لا يراه اللاعب،
     واختلاف الشكل يفضح الجواب.

  ⚠️ **جُرِّب التوسيع إلى الفئات الأخرى طلباً للشكل — ورُدّ.** خيارات
  «هذا شعار أي قناة؟» صارت: OSN وفصيلة دم ووحدة فلكية واسم علمي لجمل.
  الشكل استوى والمعنى انهار. **الفئة الواحدة سقف مقصود.**

  **القياس على البنك كامل (1,452 سؤالاً، صفر يفشل)**: تنافر الأرقام من
  **5 إلى 0**، والطول الشاذّ من **7 إلى 2** (وكلاهما بريء)، وصفر خيارات
  مكرّرة، و**73 إجابة** صارت تُعرض في الخيارات بلا شرحها.

  **الباقي 5 فقط**: إجابات فيها حرف لاتيني في فئات عربية بالكامل
  («الحمض النووي DNA»، «فصيلة O سالب»، «الوحدة الفلكية AU»،
  «Camelus dromedarius»، «OSN»). لا تُحلّ إلا بخيارات مكتوبة يدوياً —
  فهذا البند الباقي أدناه هو محلّها.


- [x] **46. ملخّص نهاية الجولة** — أُنجز 2026-08-15 (commit f068c49)
  شاشة الفوز كانت كأساً واسمين ورقمين — ولا كلمة عمّا جرى في الثمانية عشر
  سؤالاً. صار يُجمع `roundLog` سطراً لكل خلية (من، أي فريق، صح/خطأ، النقاط،
  الزمن)، ويُبنى منه: أفضل لاعب · أسرع إجابة · أصعب فئة · نقاط ضاعت · جدول
  بالدقة لكل لاعب · وسطر المراهنة.

  ⚠️ **الوضع المحلي للفرق لا للأفراد، ومكتوب على الشاشة.** الفريقان على جهاز
  واحد و`award(team)` لا تحمل اسم شخص — واختراع «أفضل لاعب» هناك كذب.
  نفس مبدأ «أداء هذا الجهاز» في `recordCategoryResult`.

  ⚠️ **بطاقة «أطول سلسلة» لا تظهر في الأونلاين أبداً** — ولا يُظنّ أنها عطل:
  `buildTurnOrder` تتناوب بين الفريقين بالبناء، فاستحال أن يصيب فريق مرتين
  متتاليتين. بقيت للمحلي، وأُضيفت **«أصعب فئة»** التي تعمل في الوضعين.

  ⚠️ **«أسرع إجابة» تتجاهل ما دون نصف ثانية** — أقلّ من ذلك ليس سرعة بشر،
  و«أسرع إجابة: 0.0 ثانية» تُقرأ عطلاً. (ظهرت فعلاً في أول فحص آلي.)

  ⚠️ **تمييز العدد بالعربية**: «6 سؤالاً» و«5 إجابة صحيحة» خطأ — العدد 3–10
  يُميَّز بالجمع. `qCount` و`aCount` تتكفّلان بذلك وبالمثنّى.

  📌 **السجلّ يُبثّ ضمن الحالة** فيُبنى الملخّص نفسه على كل الأجهزة، ولا
  يُؤخذ الوارد إلا إن كان **أطول أو مساوياً** للمحلي: هو لا ينمو إلا عند من
  يجيب، فالأقصر يعني وصولاً متأخّراً لا تحديثاً.

  **التحقق**: جولة محلية تعرض صفَّي الفريقين بدقة 77%/40% وسطر مراهنة بعلامته،
  وجولة أونلاين بأربعة لاعبين ترتّبهم وتسمّي البطل، ومراهنة 750 قُصّت إلى 450
  لفريق رصيده 450، والبطاقات تظهر وتختفي بشروطها، وجوال 375px بلا فيض أفقي
  ولا اسم مقصوص.

- [x] **47. فئتا الأعلام: «أعلام عربية» و«أعلام العالم»** — أُنجز 2026-08-31 (commit 1c81544)
  بطلب المستخدم، من مجلّدَي «AR FLAG» و«WORLD Flags» على سطح مكتبه. 34 علماً:
  عربية 8/3/4 وعالمية 7/6/6. الصور في `assets/flags-ar/` و`assets/flags-world/`،
  والأسئلة في `data/questions-part16.json`، والفئتان في `DEFAULT_CATEGORIES`.

  📌 **التقسيم إلى سهل/متوسط/صعب هو تقسيم المستخدم نفسه** — مجلّداته الثلاثة
  كما هي، لم نُعِد توزيع أي علم.

  ⚠️ **تُحقّق من كل علم بصرياً قبل الكتابة.** أسماء الملفات هي تسمية المستخدم
  لا مصدراً موثوقاً، وخطأ واحد في المطابقة يُنتج سؤالاً إجابته غلط لا يكتشفه
  أحد. بُنيت ورقة تباين من كل مستوى وقُرئت صورةً: 34/34 مطابق. أخطرها زوجان
  يسهل خلطهما — كازاخستان/قيرغيزستان، وساحل العاج/إيرلندا (الترتيب معكوس).

  ⚠️ **الإجابات بالأسماء القصيرة الشائعة لا الرسمية**: «السعودية» لا «المملكة
  العربية السعودية»، و«أمريكا» لا «الولايات المتحدة». `buildChoices` يختار
  المشتّتات بتقارب الشكل والطول، فاسم واحد طويل بين قصار **يفضح نفسه** —
  نفس عيب البند 45.

  ⚠️ **«ويلز» ليست دولة** بل بلد داخل المملكة المتحدة، فسؤالها وحده صيغته
  «لأي **بلد** يعود هذا العلم؟» لا «أي دولة».

  📌 **نص كل سؤال مختلف داخل الفئة**: الدمج يطابق بالنص، فالمكرّر لا ينجو منه
  إلا واحد — 19 صياغة مختلفة لسؤال واحد في المعنى.

  📌 **الصور 250px عرضاً عند المصدر** وتُعرض بـ440px. سقف البند 40 (×3) يمنع
  ما هو أسوأ، لكن النعومة حدُّ المادة الواردة لا عطل في الكود.

  **التحقق**: بعد التحميل الفعلي في اللعبة — الفئتان في `CATEGORIES` بأيقونتيهما،
  والأعداد 8/3/4 و7/6/6 كما هي، و34/34 صورة تُرجِع 200، والصورة تُحمّل وتُعرض
  فعلاً (250×167 ← 440px). و`buildChoices` على **1,360 سحبة** (34 سؤالاً ×
  40 بذرة) بلا `null` واحد وبلا زوج خيارات يحتوي أحدهما الآخر — أي بلا
  «سلطنة عُمان» أمام «عُمان».

- [x] **48. المشتّتات كانت تأتي من مجموعة خاطئة** — أُنجز 2026-08-31
  ظهر أثناء التحضير لفئة الأعلام القديمة: «الاتحاد السوفيتي» كانت خياراته
  **«الشباب السوفيتي» و«الأهلي السوفيتي» و«الرائد السوفيتي»**. السبب أن
  `findAnswerPool` تقبل الاسم متبوعاً بـ**أي** كلمة، فطابق «الاتحاد» مجموعة
  الأندية السعودية، ثم `answerSuffix` تُلحق « السوفيتي» بالجميع فتخرج أسماء
  لا وجود لها.

  ⚠️ **العيب لم يكن في الأعلام بل في اللعبة كلها**، وكان صامتاً: القياس على
  البنك الحيّ وجد **37 إجابة** تُطابق مجموعة خاطئة. أوضحها «إسحاق نيوتن»
  و«إبراهيم ناجي» يأخذان مشتّتاتهما من **مجموعة الأنبياء**، و«الحوت الأزرق»
  من الحيوانات بـ«حوت»، وأمثال مثل «العين بصيرة واليد قصيرة» من أعضاء الجسم.

  **الإصلاح**: الكلمة التالية للاسم إمّا تصفه (لقب أو شرح بين قوسين) أو تجعله
  اسماً آخر بالكامل — ولا ثالث. فصارت المطابقة تقبل القوسين وقائمة ألقاب
  (`NAME_HONORIFICS`) فقط. «يونس عليه السلام» تبقى، و«نوح السوفيتي» تسقط.

  📌 **الألقاب تُطبَّع قبل المقارنة**: «صلى» تصير «صلي» في `normalizeAnswer`،
  فمقارنة اللقب بنصّه الخام لا تُطابق شيئاً.

  **التحقق**: فرق آلي بين المنطقين على كل إجابة في البنك (1,486): تغيّرت 37،
  **كلها من مجموعة خاطئة إلى لا-مجموعة**، ولا واحدة فقدت مجموعة صحيحة ولا
  انتقلت بين مجموعتين. وبعده: 1,486 سؤالاً تبني خياراتها بلا `null` واحد،
  والأنبياء ما زالوا يحملون اللقب في الخيارات الأربعة، وفئتا الأعلام نظيفتان
  على 1,360 سحبة. «الحوت الأزرق» صارت تنافسها «الكنغر» و«الفيل الأفريقي».

- [x] **49. فئة «أعلام قديمة»** — أُنجز 2026-08-31
  من مجلّد «old flag» عند المستخدم. 11 علماً: 3/4/4. الصور في
  `assets/flags-old/`، والأسئلة في `data/questions-part17.json`.

  ⚠️ **عَلَم عُمان استُبعد أولاً بالخطأ ثم أُعيد**: `عمان.webp` أبيض بخطّ
  أحمر يقرأ «نصر من الله وفتح قريب»، فقُرئ شعاراً بلا حقل علم. والصواب أن
  **الأبيض هو حقل العلم** — كما أن أعلام الشهادة حقلها لون ونصّها خط.
  صحّحه المستخدم فأُعيد. الدرس: صغر الصورة وبساطة الحقل ليسا دليل نقص.

  📌 **الإجابة اسم واحد بلا قوسين وبلا شرطة مائلة** — سُئل عنها المستخدم
  صراحةً («الحالي/السابق» أم الحالي وحده) وجُرِّبت الصيغتان فعلياً:
  الشرطة تبقى حرفياً في الخيار فيتميّز الصحيح بشكله (البند 45)، والقوسان
  يُلحقان بالخيارات الأربعة فتخرج «قطر (العلم الأخضر 1977–2011)».
  والقاعدة: الاسم القديم إن سقطت الدولة، والحالي إن بقيت وتغيّر علمها فقط.

  📌 **قبول الاسمين يخصّ الوضع المحلي وحده** — هناك المضيف هو الحكم. أما
  الأونلاين فأربعة أزرار: الاسم المكتوب هو الزر الصحيح ولا شيء «يُقبل».

  📌 **هذه الفئة هي التي كشفت البند 48**: «الاتحاد السوفيتي» كانت خياراته
  أندية سعودية. لولا الإصلاح لَما صلحت الفئة.

  ⚠️ **صورتان ضعيفتان**: `iran.webp` 120×69 و`bani-khalid.webp` 250×141
  (خطّها غير مقروء بهذا الحجم — جُرِّب قلبها أفقياً فلم يتحسّن، فالسبب
  الدقة لا انعكاس). سقف البند 40 يمنع تكبيرهما أكثر من ×3.

  **التحقق**: الفئة في `CATEGORIES`، الأعداد 3/4/3، 10/10 صورة تُرجِع 200
  وتُحمّل فعلاً، وبناء الخيارات على **400 سحبة** بلا `null` وبلا زوج ملتبس.
  و«الاتحاد السوفيتي» صارت تنافسها «الدولة الأموية» و«إمارة بني خالد».

- [x] **50. حذف فئة «الذكاء الاصطناعي»** — أُنجز 2026-08-31، بطلب المستخدم
  أُسقطت من `DEFAULT_CATEGORIES` **و** أُضيف اسمها إلى
  `data/retired-categories.json` (صارت 18). الخطوة الثانية ليست زائدة:
  بدونها تُعيدها `syncCategoriesWithBank` من أسئلة الملفات عند أول تحميل.
  أسئلتها باقية في `data/` — فحذف الاسم من قائمة المسحوبات يُرجعها كاملة.

  📌 **كانت محذوفة من السحابة أصلاً** فلم يتغيّر ما يراه اللاعبون؛ الحذف
  هنا يجعلها محذوفة في الكود نفسه لا معتمِدة على بيانات السحابة.

  📌 **قائمة المسحوبات وقت التشغيل = الملف + السحابة.** فحصها كشف أربعة
  مدخلات سحابية: `h` (زبالة)، «الذكاء الاصطناعي»، «مطاعم السعودية»،
  و«اعلام عربية 🏳️» — فئة قديمة للمستخدم بلا همزة وبإيموجي، لا تطابق
  «أعلام عربية» الجديدة فلم تتأثر. **مطابقة الاسم حرفية، والفرق يحمي هنا
  ويؤذي في مكان آخر.**

  ⚠️ **تصحيح**: لا حاجة إلى «مزامنة» بعد إضافة فئة أو أسئلة. الدفع للسحابة
  مقصور على الإدارة، وكل جهاز يدمج ملفات المشروع عند كل تحميل
  (`syncBundledQuestionBank`) — راجع تعليق `js/game.js` عند «لا ندفع
  التصحيح للسحابة». قيل للمستخدم خلاف ذلك أولاً وكان خطأً.

- [x] **51. زر إضافة صوت للسؤال** — أُنجز 2026-09-01، بطلب المستخدم
  حقل `🔊 صوت السؤال (اختياري)` في نموذج إضافة سؤال بلوحة الإدارة، وزر `🔊`
  على كل سؤال في القائمة لإلصاق/استبدال صوت (و`🔇` لحذفه) — بنفس نمط الصور.
  الصوت يُخزَّن في حقل `audio` على السؤال كـ data URL، ويُعرض مشغّلاً
  (`<audio controls>`) داخل نافذة السؤال في الوضعين: المحلي (`renderQuestionBody`)
  والأونلاين ذي الخيارات (`renderChoices`).

  📌 **الصوت لا يُصغَّر في المتصفح كالصورة** — لا مكافئ لـ `downscaleImageFile`.
  فالحارس الوحيد هو رفض الملف قبل قراءته: `readAudioFile` في `js/utils.js`
  ترفض فوق **400KB** (≈50 ثانية بجودة 64kbps). بدون هذا الحدّ يمتلئ
  localStorage (~5MB) ويفشل حفظ البنك **صامتاً** فيضيع كلّه — نفس الخطر
  الذي يحرسه `saveBankWithImages()` عند 4MB، وهي تحرس الصوت أيضاً.

  📌 مشغّل المتصفح الأصلي أبيض ناصع يقطع الجوّ الداكن، فأُعطي `color-scheme: dark`
  في `.qaudio audio` بدل إعادة بناء مشغّل مخصّص.

  الملفات: `index.html` (الحقل) + `js/utils.js` (`readAudioFile`) +
  `js/admin.js` (المعاينة، الإلصاق، الحذف، الحفظ مع السؤال) +
  `js/game.js` (`questionAudio`) + `css/style.css`.
  رُفع `?v=` إلى `20260901a` وكاش الـ Service Worker إلى `raja-v8` حتى تصل
  الملفات الجديدة للأجهزة المثبِّتة للّعبة.

- [x] **52. زر إضافة فيديو للسؤال** — أُنجز 2026-09-01، بطلب المستخدم
  نفس نمط الصوت تماماً: حقل `🎬 فيديو السؤال (اختياري)` في نموذج الإضافة،
  وزر `🎬` على كل سؤال في القائمة (و`⛔` لحذفه)، وحقل `video` على السؤال
  كـ data URL، ومشغّل `<video controls playsinline>` داخل نافذة السؤال في
  الوضعين المحلي والأونلاين.

  📌 **الحدّ 1MB — أضيق من الصوت لأن الفيديو أثقل بمراتب.** `readVideoFile`
  في `js/utils.js` ترفض قبل القراءة. 1MB ≈ 15–20 ثانية بجودة 480p. البنك كله
  (نصّاً وصوراً وصوتاً وفيديو) يُحفظ في localStorage (~5MB) ويُدفع كاملاً إلى
  Supabase عند كل تعديل، فمقطعان أو ثلاثة يستهلكان الميزانية كلها.
  **للمقاطع الكثيرة أو الطويلة الحلّ هو Supabase Storage ورابط بدل data URL** —
  وهو نفس ما قيل عن الصور في البند 17.

  ⚠️ **هذا الحدّ لم يعد ساري المفعول** — نُفِّذ حلّ Storage فعلاً في نفس اليوم
  وارتفع السقف إلى 50MB. راجع البند **17.5**. ما يبقى من هذا البند هو الواجهة
  (الحقل والأزرار والمشغّل)، أما `readVideoFile` بحدّها 1MB فصارت مسار
  الطوارئ بلا شبكة لا المسار الأساسي.

  📌 مع الفيديو يُحذف مربّع الإيموجي فوقه (`questionVisual` تُرجع فراغاً):
  المقطع نفسه هو الصورة، والمربّع يزاحمه على ارتفاع الشاشة بلا فائدة.
  الصوت يختلف — لا شيء يُرى معه فيبقى الإيموجي.

  الملفات: `index.html` + `js/utils.js` + `js/admin.js` + `js/game.js` +
  `css/style.css`. رُفع `?v=` إلى `20260901b` والكاش إلى `raja-v9`.

- [x] **53. الوسائط كانت تُبتلع بصمت عند النقر السريع** — أُنجز 2026-09-01
  بلاغ المستخدم: «الفيديو ما انضاف». السبب: `previewNewQuestion*` **غير
  متزامنة** (FileReader) و`addBankQuestion` كانت **متزامنة** تقرأ
  `pendingQuestion*` مباشرة. بين اختيار الملف وانتهاء قراءته تبقى فارغة —
  فمن يضغط «إضافة» في تلك الفجوة يُضاف سؤاله **بلا وسائط وبلا رسالة خطأ**.
  قِيس حيّاً: `pendingQuestionVideo === null` مباشرة بعد `onchange`.

  📌 **العلّة كانت في الصورة والصوت أيضاً منذ البداية** — الفيديو فقط أبطأ
  ما يُقرأ فكشفها. أُصلحت الثلاثة معاً.

  **ما نُفِّذ**: `inflightMedia` يحتفظ بوعد القراءة، و`addBankQuestion` صارت
  `async` تنتظره (`allSettled` — الملف المرفوض أبلغ عن نفسه ولا يمنع بقية
  الوسائط)، وزر الإضافة يُعطَّل ويصير «⏳ جاري قراءة الملف…» أثناءها،
  وخانة المعاينة تُظهر «جاري القراءة» بدل أن تبدو فارغة بلا سبب.

  الملفات: `js/admin.js` + `index.html` (`id="addQuestionBtn"`).
  رُفع `?v=` إلى `20260901c` والكاش إلى `raja-v10`.

- [x] **54. اعتماد التصميم الجديد «3a»** — أُنجز 2026-09-01
  المستخدم أرسل `tahadi-raja-standalone.html` (ملف تصميم مجمَّع فيه **٨ نماذج**
  في ثلاث جولات) واختار النموذج **3a**: أسود دافئ وذهبي، عنوان اللعبة وحده
  بخط **Reem Kufi** وكل ما عداه **Cairo**.

  📌 **قيم اللوحة مستخرَجة من ملف التصميم نفسه لا مقدَّرة بالعين**: فُكّ ضغط
  الحمولة (gzip+base64 داخل الملف) واستُخرج قسم 3a وحُصرت ألوانه.
  الخلفية `#141110`، الأسطح `#1A1615`/`#1F1A18`/`#241F1D`، الحدود `#332C29`،
  الذهبي `#C48D3A` والفاتح `#E8C37A`، النص `#F0E9DF`، الباهت `#9C9081`.

  **المنجز**:
  - `:root` كاملاً + إزالة كل الأخضر المكتوب يدوياً خارج المتغيرات
    (خلايا اللوحة كانت `#0E2B20` وأخواتها، وألوان الفريقين rgba صريحة)
  - الخط: Reem Kufi للعنوان فقط، Cairo لكل شي — و**حُذف Tajawal** تماماً
  - الشاشة الرئيسية: عنوان كوفي كبير، سطر تعريفي بأرقام حيّة، زر ذهبي واحد،
    شبكة ٢×٢ (إحصائياتي/الأصدقاء/الرومات/عن اللعبة)، وبطاقة «آخر لعبة»
  - `showScreen` صار يكتب `document.body.dataset.screen`، وبه تُخفى الترويسة
    والشعار ونقشة السدو والتذييل على الرئيسية وحدها (البديل `:has()` يسقط
    صامتاً على المتصفحات الأقدم)
  - `saveLastGame`/`renderLastGame`: سجلّ نتيجة واحد محلي — لم يكن للّعبة
    سجلّ نتائج أصلاً، والبطاقة في التصميم تحتاجه

  - **لوحة اللعب**: كل فئة كرت فيه اسمها وأيقونتها و**٦ خلايا (٣×٢)**،
    والخلية المستخدَمة نقطة باهتة لا علامة صحّ صارخة
  - **نافذة السؤال**: سطح داكن، نقاط ذهبية كبيرة، صندوق إجابة ذهبي،
    زر الفريق الأول ذهبي مصمت والثاني داكن، و«بدون نقاط» نصّ باهت

  ⚠️ **رُجِع عن الست خلايا** — راجع البند 56. اللوحة الآن ٣ خلايا كما كانت،
  والكروت وشكلها باقية.
  **ما تغيّر في المنطق**: `slot` (0–5) هو الخلية و`levelOfSlot` يشتقّ منها
  المستوى (0–2)، فبقيت النقاط والصعوبة وكل حساباتهما كما هي.
  📌 مفتاح المخزون صار بالخلية لا بالمستوى — بدونه تعرض خلّيتا المستوى
  الواحد **نفس السؤال**. (`pickFromBank` يتكفّل باختلافهما فعلياً عبر سجلّ
  المعروض.)
  📌 `normalizeUsedState` تمدّ حالة قادمة من جهاز على نسخة أقدم (٣ خلايا)
  إلى ٦ بدل أن تنكسر اللوحة عليه، و`slot` يُبثّ ضمن حالة السؤال المفتوح.

  📌 **ألوان `canvasSignature` في `js/bans.js` لا تُلمس**: هي بذرة بصمة
  الجهاز لا ألوان واجهة — تغييرها يُسقط كل الحظورات القائمة. أُضيف تحذير.

  الشاشات الأخرى (الفئات، الرومات، النهاية، الإدارة) ورثت اللوحة والخطوط
  ولم يُعَد تركيبها — **ملف التصميم لا يرسمها**. فُحصت السبع شاشات برمجياً:
  لا أثر لأي لون أخضر من اللوحة القديمة.

  `?v=20260901h` والكاش `raja-v15`.

- [x] **55. اسم اللعبة كان يتبدّل شكله بين الشاشات** — أُنجز 2026-09-01
  بلاغ المستخدم: «لما اجي هنا خط العنوان يتغير + باللعبه + عند اختيار الفئات».
  السبب: عنوان الرئيسية `.hero-title` صار **Reem Kufi** مع اعتماد تصميم 3a،
  بينما عنوان الترويسة `.brand` بقي **Cairo 900 بتوهّج ذهبي** — فيتبدّل شكل
  الاسم كلما انتقل اللاعب من الرئيسية إلى الإعداد أو الفئات أو الرومات.

  📌 وكانت ألوان الكلمتين **معكوسة** بين الاثنين: في الترويسة «تحدي» ذهبي
  و«رجا» عاجي، وفي الرئيسية العكس. وُحّد الاثنان على شكل الرئيسية.

  وبطلبه أيضاً: **حواف ذهبية للأزرار**. الأزرار الثانوية وأزرار الشبح
  و`reset-btn` وشرائح وسائل المساعدة كانت حدودها `rgba(240,233,223,…)` —
  عاجية باهتة تذوب في الأسود الدافئ فيبدو الزر بلا حدّ. صارت كلها ذهبية.

  `?v=20260901i` والكاش `raja-v16`.

- [x] **56. الرجوع إلى ٣ خلايا لكل فئة** — أُنجز 2026-09-01، بطلب المستخدم
  «ارجع نظام النقاط القديم: 100 مرة وحدة، 250 مرة وحدة، 400 مرة وحدة».
  الست خلايا (البند 54) ضاعفت طول الجولة، فرُجِع عنها.

  📌 **التغيير سطران فقط**: `CELLS_PER_CAT = 3` و`levelOfSlot = slot => slot`.
  تجريد «slot» أُبقي عمداً رغم أنه صار مطابقاً للمستوى — مفتاح مخزون الأسئلة
  وحالة السؤال المبثوثة في الأونلاين كلها مبنية عليه، وحذفه تفكيك بلا مقابل.
  ولو رجعنا يوماً لأكثر من سؤال في المستوى يكفي تعديل هذين السطرين.

  📌 `normalizeUsedState` تتكفّل بالاتجاه المعاكس أيضاً: جهاز بقي على نسخة
  الست خلايا يبثّ ٦، فتُقصّ إلى ٣ بدل أن تنكسر اللوحة.

  **شكل الكروت باقٍ** كما في تصميم 3a — تغيّر عدد الخلايا لا هيئتها، فصار
  الكرت صفاً واحداً من ثلاث خلايا.

  `?v=20260901j` والكاش `raja-v17`.

- [x] **57. نقشة السدو صارت سدواً فعلاً** — أُنجز 2026-09-01، بطلب المستخدم
  أرسل صورة نسيج سدو حقيقي وطلب أن يشبهه الشريط العلوي «بس عدّل ألوانها».
  الشريط القديم كان `repeating-linear-gradient` قطرياً متقاطعاً — شبكة
  معيّنات بلا معنى، لا تشبه السدو في شيء.

  **ما نُفِّذ**: بلاطة SVG تتكرّر أفقياً بعناصر النسيج المعروفة — معيّن مركزي
  عنابي بحدّ ذهبي وقلب عاجي، مثلثات جانبية، عمودان رفيعان، وخطّان يحدّان
  الشريط. والشريط الرفيع تحت الترويسة صار صفّ مثلثات ذهبية وعنابية.

  📌 **SVG لا تدرّجات CSS**: أشكال السدو حادّة الحواف، والتدرّجات تعطيها
  حوافّ مسنّنة عند التصغير إلى ارتفاع 26px.

  📌 **الألوان بُدّلت عمداً**: النسيج الأصلي أحمر وأبيض وأسود صريح، فاستُبدل
  بذهبي وعنابي وعاجي على أسود دافئ — الأحمر الصريح كان سيقطع جوّ الشاشة.

  📌 **الشريط يظهر في كل الشاشات** — أُظهر على الرئيسية ولوحة اللعب بطلب
  المستخدم بعد أن كان مخفياً فيهما (تصميم 3a بلا ترويسة، البند 54). أمّا
  الشريط الرفيع الثاني فيبقى مخفياً هناك: الترويسة في الشاشتين سطر رفيع،
  فشريطان يحصرانه يجعلانه حشواً بين نقشتين.

  `?v=20260901l` والكاش `raja-v19`.

- [x] **58. فئة «خرائط الدول»** — أُنجز 2026-09-03، بطلب المستخدم
  من مجلّد `MAPS` عند المستخدم (سهل/متوسط/صعب بتقسيمه هو). **17 خريطة:
  6/5/6**. الصور في `assets/maps/` بأسماء لاتينية، والأسئلة في
  `data/questions-part18.json`.

  الخرائط كلها من نفس الطراز: الدولة مظلّلة بالأخضر الغامق على خريطة
  إقليمية رمادية، فالسؤال «ما الدولة المظلّلة؟» والإجابة اسمها وحده.

  📌 **الأسماء منسوخة حرفاً بحرف من «أعلام العالم»** (أمريكا · بريطانيا ·
  الصين · روسيا · جورجيا · فيتنام · كوستاريكا): الاسمان المختلفان لدولة
  واحدة يخرجان معاً في الخيارات الأربعة فيتناقضان أمام اللاعب.

  📌 **الإجابة بلا قوسين وبلا شرطة مائلة** — نفس قاعدة البندين 45 و49.

  ⚠️ **صورتان قُصّتا**: `costa-rica.jpg` كانت 762×566 والدولة فيها لا تتجاوز
  عشرة بكسلات عند عرض 440px (البند 40)، فقُصّت على أمريكا الوسطى؛
  و`south-korea.jpg` كانت 456×334 والشبه جزيرة عند حافة الإطار العليا،
  فقُصّت على كوريا واليابان. القصّ لا التكبير: سقف البند 40 يمنع تجاوز ×3.

  📌 **الصور أُضيفت إلى `SHELL` في `sw.js`** والكاش صار `raja-v20` — الفئة
  كلها صور، فبدون التخزين تنكسر تماماً بلا إنترنت (كما فُعل مع «منو المشهور»
  والشعارات). ⚠️ وكشف هذا أن فئات الأعلام الثلاث كانت خارج `SHELL` أصلاً —
  أُصلح في **البند 59**.

  **التحقق** (تشغيل فعلي على `localhost:8150`): الفئة في `CATEGORIES`
  (صارت 29)، الأعداد 6/5/6، **17/17 صورة تُحمَّل فعلاً** وأصغرها 263px
  عرضاً فلا تصطدم بسقف ×3، وبناء الخيارات على **680 سحبة**: صفر فشل،
  صفر تكرار، وكل خيار اسم دولة — «كوستاريكا» تنافسها «نيوزيلندا»
  و«أذربيجان» و«إثيوبيا»، و«السعودية» تأخذ مشتّتاتها من مجموعة الدول
  العربية. ⚠️ **لم تُجرَّب باليد داخل جولة**: المتصفح المُختبَر مسجَّل
  خروجاً والدخول يحتاج حساب المستخدم.

  `?v=20260903a` والكاش `raja-v20`.

- [x] **59. فئات الأعلام كانت تنكسر بلا إنترنت** — أُنجز 2026-09-03، بطلب المستخدم
  رُصد أثناء البند 58: `SHELL` في `sw.js` كانت تحفظ صور «منو المشهور»
  والشعارات، ولا تحفظ **ولا علماً واحداً** — ولا ملفَّي أسئلتها
  (`data/questions-part16.json` و`part17`). فالفئات الثلاث «أعلام عربية»
  و«أعلام العالم» و«أعلام قديمة» تفتح بلا إنترنت **بلا صور وبلا أسئلة**:
  الفئة موجودة في القائمة والخانة تُفتح على فراغ.

  ⚠️ **النقص كان صامتاً**: السياسة «الشبكة أولاً» (رأس `sw.js`) تعني أن
  المتصل يأخذ كل شيء من الشبكة دائماً — فالعيب لا يظهر إلا في اللحظة
  الوحيدة التي وُجد التخزين من أجلها. ومن كتب البندين 47 و49 أضاف الصور
  والملفات ولم يمرّ على `sw.js`.

  **ما نُفِّذ**: أُضيفت الـ45 صورة (`flags-ar` 15 · `flags-world` 15 ·
  `flags-old` 15) وملفّا الأسئلة، والكاش صار `raja-v21`.

  📌 **الفحص الذي رصده يُعاد آلياً**: تُجمع كل قيم `image` من ملفات `data/`
  وتُقارن بنصّ `SHELL`. أي فئة صور جديدة تُضاف مستقبلاً تُكشف بنفس السطر
  بدل أن تنتظر جلسة بلا إنترنت.

  **التحقق** (تشغيل فعلي على `localhost:8150`): `SHELL` صارت **153 مدخلاً**
  بلا مكرّر وكلها موجودة على القرص، والمتصفح خزّنها فعلاً — `raja-v21`
  فيها 153 ملفاً منها 45 علماً و17 خريطة وملفات الأسئلة الثلاثة، ولا كاش
  قديم بقي (`activate` مسح `raja-v20`). و**صفر صورة** في بنك الأسئلة كلّه
  خارج `SHELL` الآن (كانت 45).

- [x] **60. فئة «صور قديمة»** — أُنجز 2026-09-03، بطلب المستخدم
  من مجلّد `OLD PHOTO` عند المستخدم. **5 صور: 2/2/1**. الصور في
  `assets/old-photos/` والأسئلة في `data/questions-part19.json`.

  ⚠️ **المستوى الصعب فيه سؤال واحد**، فخانة 400 لهذه الفئة تُعيد نفس الصورة
  في كل جولة. المجلّد وصل بستّ صور وحُذفت واحدة (أدناه). الحلّ صور أخرى لا
  كود.

  📌 **الفئة تخلط دولاً ومدناً** كما وصلت (روسيا · مكة · أبوظبي · هيروشيما ·
  لندن)، فنصّ السؤال هو الذي يفصل: «في أي دولة» للدولة و«أي مدينة» للمدينة.

  ⚠️ **العلامة المائية تكشف الإجابة**: صورة أبوظبي كان عليها
  «UAEHISTORY.COM» — تقول الدولة قبل أن ينظر اللاعب. قُصّ أسفلها 10٪
  وحُوّلت إلى JPEG (كانت PNG بـ998KB).

  ⚠️ **صورة «ايطاليا.jpg» استُبعدت بقرار المستخدم**: لا تبدو إيطالية — نساء
  بالحايك المغاربي، وسيتروين 2CV و Traction، وناشر البطاقة «Dany» جزائري —
  وعليها علامتان مائيتان، ودقّتها 550×363 بلا لافتة مقروءة يُتحقّق منها.
  سُئل المستخدم ولم يُخمَّن (درس البند 49: الاستبعاد بالظنّ أخطأ مرّة).

  📌 **أُضيف اسمان إلى `ANSWER_POOLS`**: «أبوظبي» إلى مدن العرب و«هيروشيما»
  إلى مدن العالم. بدونهما كانت الاثنتان تسقطان إلى الملاذ الأخير فتأخذان
  مشتّتاتهما من الفئة نفسها — وفيها **روسيا**، فيقف اللاعب أمام سؤال «أي
  مدينة؟» وأحد خياراته دولة، فيُسقطها بلا تفكير وتصير ثلاثة. ومعهما
  «ما عاصمة الإمارات؟» (كان في الملاذ الأخير) صار يأخذ عواصم عربية،
  و«ما عاصمة اليابان؟» صارت «هيروشيما» أحد مشتّتاته — وهو أفضل مشتّت ممكن.

  **التحقق** (تشغيل فعلي على `localhost:8150`): فرق آلي على **1,547 إجابة**
  قبل التعديل وبعده: تغيّرت **19**، منها 3 من لا-مجموعة إلى مجموعة
  (أبوظبي مرّتين وهيروشيما) و16 بقيت في مجموعتها واتّسعت بواحد،
  و**لا واحدة فقدت مجموعة**. وبناء الخيارات على البنك كله — **4,641 سحبة**:
  صفر فشل، صفر تكرار، ولا خيار ناقص. و5/5 صور تُحمَّل، والكاش `raja-v22`
  فيه 159 مدخلاً بلا مكرّر.

- [x] **61. الفئات الناقصة: أربع نصّية أُكملت، وسؤالان كانا بلا خيارات**
  أُنجز 2026-09-05، بطلب المستخدم (commit c49d8a8)

  **الجرد أولاً**: عُدّت الفئات بعد الدمج الفعلي (بإسقاط المسحوب فئةً ونصّاً،
  وبحذف المكرّر بنصّ السؤال كما يفعل `mergeIntoQuestionBank`) لا بجمع أطوال
  الملفات — الجمع الساذج يعطي رقماً أكبر من الواقع. النتيجة: **12 فئة تحت
  20/20/20**، منها **4 نصّية** و8 معتمدة على صور.

  **ما أُكمل** (26 سؤالاً في `data/questions-part12.json`، كلها إلى 20/20/20):
  جغرافيا 🌎 (+5) · الالغاز 🧩 (+9) · الحساب 🧮 (+6) · الانبياء 🌙 (+6).

  **وفئة خامسة كانت خارج المستودع أصلاً**: «📱 تطبيقات ومنصات» موجودة عند
  المستخدم في Supabase فقط ولم تكن في أي ملف — كانت **8/6/8**. أُضيف
  `data/questions-part20.json` بـ38 سؤالاً (وسُجّل في `QBANK_FILES`)، والدمج
  بالاسم أوصلها إليها فصارت 20/20/20 **بلا لمس السحابة**: أسئلة المستخدم
  الأصلية باقية كما هي جنب الجديدة.

  ⚠️ **الاسم يبدأ بإيموجي 📱 وهو جزء من الاسم لا أيقونة**: اختلاف حرف واحد
  يُنشئ فئة ثانية فارغة بدل أن يُكمل هذه. تُحقّق من الاسم بـ `Object.keys(QBANK)`
  لا بالنظر إلى الشاشة.

  **وسؤالان كانا يُعرضان بلا خيارات أصلاً في الأونلاين** — عيب حقيقي كشفه
  الجرد: `buildChoices` تحتاج ثلاثة مشتّتات، فإن لم تُطابق الإجابة أي مجموعة
  في `ANSWER_POOLS` سقطت إلى مشتّتات الفئة نفسها — وفي مستوى فيه ثلاثة أسئلة
  لا تكفي. فأُصلحت المجموعتان لا الأسئلة:

  - **«فلسطين»** كانت خارج مجموعة الدول العربية، ومعها الصومال وجيبوتي
    وموريتانيا وجزر القمر وسلطنة عُمان. أُضيفت كلها، فصار سؤال علم فلسطين
    يأخذ مشتّتاته من الدول العربية، وأعلام الفئة الصعبة معه.
  - **«الاتحاد السوفيتي»** لا تُشبه أي دولة قائمة، وبين «السعودية» و«إيطاليا»
    تُعرف بأنها الوحيدة الزائلة. أُضيفت مجموعة كيانات تاريخية.
    ⚠️ بصيغة «الدولة الأموية» كاملةً: المطابقة تُسقط «ال» من أول الإجابة فقط،
    فمجموعة الدول الموجودة («الأموية»، «العباسية»…) لا تطابقها.

  **التحقق** (تشغيل فعلي على `localhost:8150` مع بيانات السحابة الحيّة):
  بناء الخيارات على البنك كله — **1,611 سحبة: صفر فشل، صفر خيار مكرّر،
  ولا سحبة أقل من أربعة خيارات** (كانت سحبتان تفشلان قبل التعديل). و«فلسطين»
  صارت خياراتها {المغرب، العراق، فلسطين، جيبوتي} و«الاتحاد السوفيتي»
  {الاتحاد السوفيتي، إمارة بني خالد، يوغوسلافيا، تشيكوسلوفاكيا}.
  الفئات الكاملة **19 من 29**، والبنك **1,611 سؤالاً**.

  📌 **الباقي عشر فئات، وكلها موقوفة على وسائط لا على كود**: تسع تحتاج صوراً
  أو مقاطع من المستخدم (صور قديمة 2/2/1 · أعلام عربية 8/3/4 · أعلام قديمة
  3/4/4 · خرائط الدول 6/5/6 · منو المشهور 4/7/4 · شاهد وأجب 🎬 1/3/2 ·
  أعلام العالم 7/6/6 · شعارات سعودية 7/7/7 · شعارات عالمية 5/8/4) — وكل
  صورة في `assets/` مستعملة فعلاً، فلا يوجد رصيد غير مستغلّ. والعاشرة
  «ولا كلمة 🤫» **1/1/1 — أسوأ فئة في اللعبة، تُعيد نفسها كل جولة** ولا
  تحتاج وسائط إطلاقاً، لكن نصّ سؤالها واحد في المستويات الثلاث
  («خل الي يمثل الكلمه يشوف الجواب ويمثله») والدمج يطابق بنصّ السؤال —
  فإضافة عشرين كلمة بنفس النصّ تُسقط تسعة عشر منها **وتدهس إجابة الموجود**.
  تحتاج قراراً من المستخدم في صياغة النصّ قبل أي إضافة.

- [x] **الإيموجي كان يفضح الإجابة** — أُنجز 2026-09-05 (commit 0c2acc9)
  بلاغ المستخدم: «فيه فئات فيها أسئلة يظهر الإيموجي وهو الحل»، ومثاله سؤال
  الجمل في «عالم الحيوان». `questionVisual()` تعرض `item.emoji` **مع نصّ
  السؤال** لا مع الإجابة، فأي إيموجي يسمّي الإجابة يُلغي السؤال.
  - **مسحٌ آليّ** على 2,259 سؤالاً بلا صورة، بجدول إيموجي←كلمة عربية،
    مع استبعاد ما تذكره صياغة السؤال أصلاً: **128 سؤالاً** الإيموجي فيها
    هو الحل. صُحّح منها **87** يدوياً بإيموجي يلمّح لموضوع السؤال لا لجوابه
    (🦁 «ملك الغابة» ← 👑، 🐋 «أكبر حيوان» ← 🌍، 🇷🇺 «أكبر دولة» ← 🗺️ …).
  - **فئة «الالغاز 🧩» كلها**: صورة الشيء هي حلّ اللغز دائماً — 60 لغزاً
    وُحِّدت على 🧩. (25 فقط كشفها المسح، والبقية كشفتها القراءة اليدوية:
    🍶 الزجاجة، 🔊 الصدى، 🪑 الطاولة، 🧽 الإسفنجة…)
  - **الخمسة عشر الباقية مقصودة**: إجابتها عدد («كم عدد المستشفيات؟» 🏥)
    أو كلمتها في نصّ السؤال أصلاً — فلا تفضح شيئاً.
  - **سؤال الجمل نفسه**: «يخزّن الدهون في سنامه» كان في `medium` وهو سهل،
    وإجابته «الجمل» مكرّرة مع سؤال `easy` في الفئة ذاتها. استُبدل بـ«كم عدد
    حجرات معدة الجمل؟ / ثلاث حجرات»، والنصّ القديم أُضيف إلى
    `data/retired-questions.json` وإلا بقي على أجهزة اللاعبين وفي Supabase.
  - **رقم الإصدار في `index.html` رُفع** إلى `20260905b` — بدونه يخدم
    المتصفح `data/*.json` من الكاش ولا يصل التصحيح للاعب (`versionedUrl`).
  - تم التحقق داخل اللعبة: `QBANK` بعد الدمج يعرض الإيموجي الجديد،
    والسؤال القديم اختفى، ونافذة السؤال تعرض 👑 مع «ملك الغابة».

- [x] **62. «ولا كلمة 🤫» من 1/1/1 إلى 20/20/20 — وفئات التمثيل لا تُعرض بخيارات**
  أُنجز 2026-09-05، بطلب المستخدم
  (الأسئلة في c73a11d · الإعفاء من الخيارات في fe12151 · التسجيل في QBANK_FILES في 6d7d3f3)

  كانت **أسوأ فئة في اللعبة**: سؤال واحد لكل مستوى، فتُعيد نفسها كل جولة.
  وموجودة عند المستخدم في Supabase فقط ولم تكن في أي ملف. أُضيف
  `data/questions-part21.json` بستّين كلمة (مهن وحيوانات وأفعال للسهل،
  أمثال شعبية للمتوسط، تعابير أطول للصعب).

  ⚠️ **نصّ السؤال مرقّم (1..20) بقرار المستخدم**: الفئة نصّ سؤالها واحد في
  المستويات الثلاث («خل الي يمثل الكلمه يشوف الجواب ويمثله») والدمج يطابق
  بنصّ السؤال — فإضافة عشرين كلمة بنفس النصّ **تُسقط تسعة عشر منها وتدهس
  إجابة السؤال الموجود**. الترقيم داخل المستوى فيتكرّر بين المستويات، وهذا
  سليم لأن المطابقة داخل المستوى لا عبره.

  ⚠️ **والأهمّ — بناء الخيارات كان سيُلغي الفئة**: الإجابة هنا هي الكلمة
  المطلوب تمثيلها، فعرضها ضمن أربعة أزرار يجعل الفريق **يقرأها بدل أن
  يخمّنها من الحركة**. ولم تكن الفئة سليمة قبل اليوم بالتصميم بل بالمصادفة:
  `buildChoices` تحتاج ثلاثة مشتّتات، وبسؤال واحد لكل مستوى كانت تفشل
  وتُعيد `null` فتُعرض بلا خيارات. أوّل ما اكتملت إلى عشرين **نجح البناء
  وانكشفت الإجابة**. فأُضيفت `NO_CHOICE_CATEGORIES` و`isActingCategory()`
  في `js/game.js`، و`buildChoices` تخرج فوراً لهذه الفئات.

  📌 المطابقة **بالنصّ العربي بعد تجريد الإيموجي**، لأن الاسم يحمل 🤫 وقد
  يتغيّر. والإيموجي في كل الأسئلة 🤫 واحد عمداً: إيموجي معبّر يكشف الإجابة
  قبل التمثيل.

  📌 **أسئلة المستخدم الثلاث باقية بلا مساس**، فالفئة عنده 21/21/21 وعلى
  جهاز نظيف 20/20/20.

  **التحقق** (تشغيل فعلي على `localhost:8150` بالبيانات الحيّة): الفئة
  21/21/21، و**63 محاولة بناء خيارات لها: صفر نجاح** (أي لا تُعرض بخيارات
  أبداً)، وباقي البنك **1,608 سحبة: صفر فشل** — أي أن الاستثناء لم يمسّ
  فئة أخرى. والأجوبة الثلاث الأصلية موجودة كما هي. الفئات الكاملة صارت
  **20 من 29**، والباقي تسع كلها موقوفة على وسائط من المستخدم.

- [x] **63. «ولا كلمة» بلا مؤقّت + سؤال المراهنة من فئة خارج اللوحة**
  أُنجز 2026-09-05، بطلب المستخدم

  **(أ) فئة التمثيل بلا مؤقّت.** المؤقّت في «ولا كلمة» لا يقطع الإجابة بل
  التمثيل نفسه: اللاعب يقرأ الكلمة ثم يمثّلها بلا كلام، وعشرون ثانية تنتهي
  قبل أن يبدأ فريقه بالتخمين. صارت `questionDeadline = 0` لهذه الفئة مهما
  كان إعداد المؤقّت — في `openQuestion` وفي `revealWatchedQuestion`.

  📌 **أعيد استعمال `isActingCategory()` نفسها** التي تُعفي الفئة من بناء
  الخيارات (البند 62) بدل قائمة ثانية: لو أُضيفت فئة تمثيل غداً وُصِفت مرّة
  واحدة، ولا تتفرّق القائمتان فتظهر فئة بخيارات بلا مؤقّت أو العكس.

  📌 **الصفر يكفي في الأونلاين أيضاً**: المهلة تُبثّ طابعاً زمنياً من جهاز
  صاحب القرار، وباقي الأجهزة تقرأ `state.questionDeadline` كما هو — فصفرٌ
  عنده صفرٌ عند الجميع بلا تغيير في البثّ.

  **(ب) سؤال المراهنة من فئة لم تُختَر في الجولة.** كان يُنتقى من فئات
  اللوحة نفسها، وبعد ثمانية عشر سؤالاً صار الفريقان يعرفان مزاج كل فئة —
  فتفقد الخاتمة مفاجأتها. صار `pickBetQuestion` يستبعد كل فئة ظهرت في أي
  جولة (`rounds` كلها لا `rounds[activeRound]` وحدها).

  📌 **فئات اللوحة تبقى احتياطاً أخيراً**: لو ما أسعفت أي فئة خارجية (بنك
  ناقص أو كل الفئات مختارة) نرجع إليها بدل أن تسقط المراهنة كلها.

  📌 **استُبعدت أسئلة الفيديو والصوت من المراهنة**: شاشة المراهنة تعرض النصّ
  والصورة فقط (`renderBetQuestion` تنادي `questionVisual` بلا مشغّل)، وسؤالٌ
  مقطعُه هو محتواه كان سيصل بلا محتوى. لم يظهر قبل اليوم لأن الاستبعاد
  الجديد وسّع البِركة إلى كل الفئات.

  **التحقق** (تشغيل فعلي بالبيانات الحيّة): بمؤقّت 20 ثانية، «ولا كلمة»
  `questionDeadline = 0` وخانة العدّاد فارغة، وفئة عادية في نفس اللوحة
  تعرض `⏱️ 20`. و**20 سحبة لسؤال المراهنة: صفر منها من فئات اللوحة**
  (13 فئة مختلفة). بلا أخطاء في الـ console.

- [x] **64. اللعب المحلي بلا حساب — البوابة صارت للأونلاين وحده**
  أُنجز 2026-09-05، بطلب المستخدم

  كانت `REQUIRE_ACCOUNT` توقف **كل** لعب عند شاشة الدخول: تفتح اللعبة فترى
  «ادخل بحسابك» ولا مخرج منها. وهذا ثقيل في الحالة التي بُنيت لها اللعبة —
  جهاز واحد في مجلس وفريقان حوله؛ من يوقف المجلس ليُنشئ بريداً وكلمة مرور؟
  والحساب هناك لا يحمي شيئاً ولا يربط بأحد: اللاعبون في نفس الغرفة والنقاط
  تنتهي بانتهاء الجلسة.

  صارت `REQUIRE_ACCOUNT_FOR_ONLINE`، والبوابة تُسأل عند مدخل كل شاشة تحتاج
  هويّة لا عند الإقلاع: `goToRooms` و`goToFriends` و`goToProfile` تنادي
  `requireAccount(reason)` فتحوّل الضيف إلى شاشة الدخول **برسالة تشرح لماذا**
  بدل أن يصطدم بها بلا سياق. واللوحة المحلية لا تمرّ من هناك إطلاقاً.

  📌 **الأونلاين يبقى محتاجاً حساباً، وهذا شرط أمني لا تفضيل**: سياسات RLS
  في `supabase-rooms-security-1/2.sql` مبنية على أن كل لاعب في روم له
  `auth.uid()`، و`anon` مسحوبة الصلاحيات من جداول الرومات. فتح الأونلاين
  للضيوف يكسر ذلك كلّه — ولذلك حُدِّث التعليق في الملفين ليشير إلى الاسم
  الجديد، فلا يظنّ قارئ أن الرايةَ التي زالت هي التي كانت تحميه.

  📌 **رابط الروم لضيف**: `handleRoomLinkOnLoad` كانت تستهلك الكود من
  `sessionStorage` ثم تُحوّله للدخول، فيسجّل ويجد نفسه في الرئيسية بلا روم.
  صار الكود يُعاد إلى مخزنه قبل التحويل، و`handleAuthSubmit` يلتقطه بعد
  الدخول. وجلسة روم قديمة لا تُستعاد لضيف أصلاً.

  📌 **مخرج ومدخل ظاهران**: شاشة الدخول صار لها زر «رجوع» (لم تعد بوابة)،
  والترويسة تعرض للضيف زر «دخول» مكان اسم الحساب — وإلا لا يجد الراغب في
  التسجيل بابه. والخروج من الحساب يعود للرئيسية لا لشاشة الدخول.

  **التحقق** (جولة كاملة كضيف بلا أي تسجيل): الصفحة تفتح على الرئيسية،
  و«محلي» يمشي إلى النهاية — 18 سؤالاً + المراهنة + شاشة الفائز، بلا خطأ
  واحد في الـ console. و«أونلاين» و«الرومات» و«الأصدقاء» و«إحصائياتي»
  كلها تحوّل لشاشة الدخول برسالة كلٍّ منها، وزر الرجوع يعيد للرئيسية.
  ورقم الإصدار رُفع إلى `20260905c` وكاش الـ service worker إلى `raja-v25`،
  وإلا خدم المتصفح النسخة القديمة وبقيت البوابة عند اللاعبين.

- [x] **65. شاشة «عن اللعبة» — شرح كامل بأربعة أقسام ولقطات حقيقية**
  — أُنجز 2026-09-08 (commit 0da1589)، بطلب المستخدم

  زر «عن اللعبة» في الرئيسية كان يفتح رسالة منبثقة من أربعة أسطر. صار يفتح
  شاشة كاملة `screen-about` بأربعة أقسام يتنقّل بينها بتبويبات:
  **1) طريقة اللعب** بسبع خطوات، **2) الأدوات المستخدمة**، **3) اللغات
  البرمجية**، **4) توزيع الفئات والأسئلة**.

  **الملفات**: `js/about.js` (جديد)، وقسم `screen-about` في `index.html`،
  وتنسيقاته في `css/style.css` + `css/responsive.css`، و`assets/help/*.webp`.

  📌 **قسم البنك كلّه يُحسب لحظياً من `QBANK` و`CATEGORIES`** — لا رقم فئات
  ولا أسئلة مكتوب نصّاً في HTML. أي رقم يُكتب بيدٍ هنا يصير كذباً بعد أول
  سؤال يُضاف، والجدول (فئة × سهل/متوسط/صعب) يُبنى عند كل فتح. وكذلك وسائل
  المساعدة تُقرأ من `LIFELINES` في `game.js` لا تُنسخ نصّاً.

  📌 **اللقطات السبع لقطات حقيقية من اللعبة** لا رسوماً: التُقطت بتشغيل
  اللعبة نفسها في Chrome بلا واجهة على حالات مُعدّة (لوحة في منتصف جولة،
  سؤال مفتوح، مراهنة أقفلها فريق واحد)، ثم قُصّت منها الترويسة المكرّرة
  وحُوّلت إلى WebP بعرض 900 — **94 كيلوبايت للسبع مجتمعة**.

  📌 **الصور `loading="lazy"` ولم تُضَف إلى `SHELL` في `sw.js`**: لا تُحمَّل
  إلا لمن يفتح الشرح فعلاً، ويلتقطها الكاش اللحظي بعد أول عرض. إضافتها إلى
  `SHELL` كانت ستُثقل تثبيت التطبيق على من لن يفتح الشاشة أصلاً.

  📌 **`showAbout` حُذفت من `js/utils.js`** ولم يبقَ مكانها إلا تعليق يشير
  إلى `js/about.js` — الاسم واحد ولا يجوز أن يعرّفه ملفان.

  📌 **`unicode-bidi: isolate` على `#screen-about code`**: أسماء الملفات
  اللاتينية داخل نصّ عربي كانت تلتصق علامات ترقيمها بالطرف الخطأ
  («responsive.cssو style.css») فيُقرأ السطر مقلوباً وإن كان مكتوباً صحيحاً.

  📌 **قسم اللغات يعرض النسبة المئوية** (commit b857f8a، طلب لاحق في الجلسة نفسها): النسبة
  هي ما يُقارن بين اللغات بنظرة واحدة، وعدد الأسطر صار سطراً مكتوماً بجانبها.
  وعند إضافتها تبيّن أن الأرقام الأولى (585 / 4,880 / نحو 9,300) **أبطلها
  تعديلُ هذه الشاشة نفسه** — فأُعيد العدّ: 836 / 5,333 / 9,354 / 2,201،
  مجموعها 17,724 سطراً، والنسب 4.7 / 30.1 / 52.8 / 12.4 بالمئة، وعرض كل
  شريط صار نسبته حرفياً. وصُحّح «أحد عشر سكربت SQL» إلى **اثني عشر** (عُدَّت
  الملفات فعلياً). أرقام هذا القسم وحدها في الشاشة مكتوبة بيد — لا يمكن عدّ
  أسطر المصدر من المتصفح — فوُضِع فوقها في `index.html` تنبيهٌ بأمر إعادة
  العدّ. ورُفع الإصدار إلى `20260908b` والكاش إلى `raja-v27`.

  📌 **الأرقام غربية لا هندية**: `toLocaleString('ar-EG')` (ميراث الرسالة
  القديمة) كانت تُخرج «١٬٦٧١» بينما اللوحة والنقاط والتذييل كلها أرقام
  غربية — فصارت `en-US`.

  **التحقق** (تشغيل فعلي على سيرفر محلي): الأقسام الأربعة تُعرض بلا خطأ
  واحد في الـ console، والجدول يبني 29 صفاً ومجموعه 1,671 سؤالاً مطابقاً
  لما يعرضه التذييل، والوسائل الخمس تُقرأ من `LIFELINES`، واللقطات السبع
  تظهر. وعلى عرض 375px (جوال) لا تمرير أفقي للصفحة إطلاقاً — الجدول وحده
  يمرّر داخل صندوقه. ورقم الإصدار رُفع إلى `20260908a` وكاش الـ service
  worker إلى `raja-v26`، وإلا خدم المتصفح النسخة القديمة بلا الشاشة الجديدة.

- [x] **66. «شاهد وأجب 🎬» تُسحب مؤقتاً — وأسئلتها تُنقَذ إلى المشروع أولاً**
  — أُنجز 2026-09-08 (commit 8027308)، بطلب المستخدم

  الفئة كانت **1 / 3 / 2** (ستّة أسئلة)، وأي جولة تختارها تكرّر نفس السؤال
  على نفس اللاعبين. وإكمالها متعذّر: كل سؤال فيها مربوط بمقطع بعينه
  («وش لون قميص الشارح؟»، «كم مزهرية ظهرت خلف المتحدث؟») — لا تُكتب أسئلته
  بالمعرفة بل بمشاهدة المقطع، والمقاطع نفسها غير متاحة.

  📌 **أسئلتها كانت في Supabase وحدها** — لا نسخة منها في `data/` إطلاقاً.
  السحب قبل الحفظ كان سيجعل رجوعها رهينة صفٍّ واحد في السحابة. فنُقلت أولاً
  إلى `data/questions-part22.json` (سُجّل في `QBANK_FILES` وفي `SHELL`)،
  ثم أُضيف الاسم — **بإيموجيه حرفياً** — إلى `data/retired-categories.json`.

  📌 **الإيموجي في الاسم ليس تزييناً**: `retireCategories` تطابق نصّياً
  (`retiredCategoryNames.has(name.trim())`) بلا التوحيد الذي تفعله
  `isWatchFirstCategory`. «شاهد وأجب» بلا 🎬 لا تُسقط شيئاً.

  📌 **ملف السحب وحده يكفي رغم أن السحابة ما زالت تحمل الفئة**:
  `retireCategories()` تعمل **بعد** كل دمج — بعد دمج ملفات المشروع في
  `syncBundledQuestionBank`، وبعد دمج السحابة في `pullFromCloudOnce` — فما
  يعيده الدمج يُسقطه الإسقاط. لا حاجة لصفّ `retired_categories` في السحابة
  (وهو يحتاج جلسة إدمن أصلاً).

  **البدائل التي فُحصت ورُفضت** (قبل اختيار السحب):
  - **مقاطع من صور المشروع** بـ `ffmpeg` (عرض متتابع، 55KB للمقطع): جُرّبت
    فعلياً ونجحت — بما فيها أن **المسار النسبي `assets/watch/x.mp4` يشتغل**
    فلا حاجة لرفع Supabase إطلاقاً. لكن طعمها واحد («كم عدّ؟ وش الثالث؟»).
  - **التجوال داخل صورة**: جُرّب على `old-photos/hiroshima.jpg` وسقط —
    الصورة رتيبة بلا تفاصيل تُبنى عليها أسئلة، والصور القديمة خمس فقط.
  - **صور `assets/famous/` غير صالحة لأي مقطع**: عرضها **110 بكسل** وسيطاً
    (وأصغرها 47)، و`sa-logos` وسيطها 140. الصالح للتكبير إلى 480p هو
    الأعلام (250) والخرائط (510) والصور القديمة فقط.

  **التحقق** (تحميل نظيف ثلاث مرات بمسح localStorage وعامل الخدمة):
  الفئة اختفت من القائمة ومن البنك معاً (29 ← **28** فئة، 1,671 ← **1,665**
  سؤالاً — بالضبط الستّة)، وشاشة «عن اللعبة» والتذييل يعرضان الرقم الجديد.
  و**اختُبر الرجوع فعلياً**: بحذف الاسم من ملف السحب عادت الفئة كاملة
  (1/3/2) بروابط مقاطعها سليمة، ثم أُعيد السحب. الإصدار `20260908c`
  والكاش `raja-v28`.

  **للإرجاع لاحقاً**: احذف `"شاهد وأجب 🎬"` من `data/retired-categories.json`
  وارفع رقم الإصدار — وترجع بأسئلتها من `part22`.

- [x] **67. بلاغ عن سؤال + تقييم الجولة + إعلان الألعاب الأخرى**
  — أُنجز 2026-09-09 (commit 5c95e19)، بطلب المستخدم

  ثلاث إضافات دفعةً واحدة، اثنتان منها ملاحظات تصعد للسحابة والثالثة روابط
  ثابتة. الملفات: `js/feedback.js` (جديد)، `supabase-feedback.sql` (جديد)،
  وأقسام في `index.html` وتنسيقاتها.

  **(أ) بلاغ عن سؤال.** البنك تجاوز 1,600 سؤال كثير منها كُتب بسرعة، ومن
  يكتشف إجابة خاطئة لم يكن عنده أي طريقة ليخبرك — فيتكرّر الخطأ على كل من
  يلعب بعده. زرّ 🚩 في نافذة السؤال ← خمسة أسباب جاهزة وملاحظة اختيارية ←
  جدول `question_reports` ← تبويب **🚩 الملاحظات** في لوحة الإدارة (عولج /
  أعِد فتحه / حذف).

  📌 **البلاغ لا يضيع بلا إنترنت.** أكثر اللعب محلي والوضع المحلي يعمل بلا
  شبكة، فبلاغ يُرسَل مباشرةً أو لا يُرسل أبداً كان سيضيّع أغلب البلاغات.
  يُحفظ في طابور `mr_pending_reports` (سقفه 40) ويُفرَّغ عند أول تحميل تتوفر
  فيه السحابة. **والمسح بعد نجاح الإدراج لا قبله** — لو مسحنا أولاً وفشل
  الرفع ضاع البلاغ، وهو ما جاء الطابور ليمنعه.

  📌 **الزرّ ملاصق لاسم الفئة لا في طرف الشريط.** وُضع أولاً في طرف
  `.topline` فوقع فوق زرّ الإغلاق ✕ المطلق في زاوية البطاقة — رُصد بالقياس
  (تقاطع المستطيلين) لا بالنظر. وأُلغي `filter: grayscale(1)` معه: مع شفافية
  0.35 كان العَلَم يُقرأ لطخةً رماديةً لا زرّاً.

  📌 **البلاغ خارج شريط الحكم** (`corners`) عمداً: ذاك للمضيف، والبلاغ يجب
  أن يصل إليه أي لاعب.

  **(ب) تقييم الجولة.** خمس نجوم في شاشة النهاية ← `game_ratings` مع الوضع
  (محلي/أونلاين) وعدد الأسئلة المُلعَبة. تقييم ≤3 نجوم يُتبَع بسؤال «وش
  الناقص؟» — نجمتان بلا سبب لا تفيدان. ومن قيّم جولةً لا يُسأل عنها ثانية
  (`mr_rated_games` ببصمة محلية بحتة لا تُرسل مع التقييم).

  **(ج) إعلان الألعاب الأخرى.** بطاقتان في شاشة النهاية — **صراع الحروف**
  و**توب تن** — ورابط بوابة **نكست ليفل**. العناوين والروابط قُرئت من
  المواقع الحيّة نفسها لا من الذاكرة (اسم لعبة الحروف المنشور «صراع الحروف»
  لا «حروف مع رجا»)، والثلاثة تُرجع 200.

  📌 **روابط ثابتة في HTML بلا أي JavaScript**: قائمة تُجلب من السحابة كانت
  ستضيف طلباً وتفشل بلا إنترنت، والألعاب لا تتغيّر كل يوم. وموضعه **بعد**
  أزرار الجولة: من يريد لعبة جديدة يجدها أولاً.

  ⚠️ **خطوة يدوية معلّقة**: `supabase-feedback.sql` **لم يُشغَّل بعد**.
  حتى يُشغَّل، البلاغات تتراكم في الطابور المحلي والتقييمات تُهمَل بصمت،
  وتبويب الملاحظات يعرض «تأكّد أن السكربت شُغِّل». **شغّله على دفعتين**
  (الفاصل معلَّم داخل الملف) كما تنصّ الملاحظة 11.

  **التحقق** (تشغيل فعلي، والجدولان غير موجودين بعد فمسار الفشل هو ما
  اختُبر): البلاغ يُحفظ كاملاً في الطابور (فئة، مستوى، سؤال، إجابة، سبب،
  ملاحظة)، و**نجا من تحميل فاشل** — أعيد التحميل ففشل الرفع وبقي البلاغ
  مكانه. والتقييم يشكر ولا يعاود السؤال بعد إعادة الرسم. واللوحة تعرض رسالة
  السكربت في الصندوقين معاً (كان أحدهما يبقى على «اضغط للتحميل» فيوهم أنه لم
  يحاول). وعلى 375px لا تمرير أفقي، والبطاقتان تنطبقان عموداً واحداً، وهدف
  لمس الزرّ 46×40. ولا خطأ واحد غير مُلتقَط في الـ console — نداءات السحابة
  الفاشلة كلها مبتلعة واللعب مستمر. الإصدار `20260909a` والكاش `raja-v29`.

- [x] **68. اثنتا عشرة فئة جديدة — أسماء محجوزة بلا أسئلة بعد**
  — أُنجز 2026-09-10 (commit 59fcfe0)، بطلب المستخدم

  المستخدم أرسل قائمة بأسماء فئات وطلب إضافتها **دون أسئلة**: يريد الأسماء
  محجوزة وظاهرة على أجهزته الآن، ويملأ البنك على مراحل. أُضيفت إلى
  `DEFAULT_CATEGORIES` في `js/game.js`:

  حروف مبعثرة 🔤، صحح الخطأ ✏️، القرآن الكريم 📗، شعارات دول 🛡️،
  نشيد وطني 🎼، معالم السعودية 🕌، اسم اللاعب الأول 🪪، تسريحة لاعب 💇،
  كرة قدم ⚽، الكرة السعودية 🏆، شعارات أندية ومنتخبات 🏟️، ألعاب إلكترونية 🎮.

  الأسماء كُتبت بالإملاء الصحيح لا كما وردت في القائمة (حروف مبعثر**ة**،
  القر**آ**ن، **أ**ندية، **أ**لعاب **إ**لكترونية) — لا بنك مرتبطاً بها بعد
  فالاسم حرّ الآن، وبعد أول سؤال يصير مفتاحاً لا يُمسّ.

  📌 **الإضافة إلى `DEFAULT_CATEGORIES` وحدها لم تكن تكفي** — وهذا هو
  التعديل الحقيقي في هذا البند. تلك القائمة كانت **أرضيةَ أول تشغيل فقط**:
  بعده تُقرأ الفئات من `mr_categories` في localStorage، فأي فئة تُضاف
  للمشروع لا تصل إلى جهازٍ لعب مرّة واحدة من قبل — أي إلى أجهزة المستخدم
  كلها. و`syncCategoriesWithBank` لا تُنقذها لأنها تُعيد ما له **أسئلة** في
  البنك، وهذه بلا أسئلة بالتعريف. أُضيفت `mergeDefaultCategories()` تُنادى
  في `syncBundledQuestionBank` عند كل تحميل: تدمج ولا تحذف، فصارت ملفات
  المشروع مصدر **الإضافة** على كل جهاز.

  📌 **والحذف يبقى سيّد الموقف**: الدالة تتخطّى `retiredCategoryNames`،
  و`retireCategories()` تعمل بعدها كطبقة ثانية — فلا تعود فئة سحبها
  المستخدم من لوحة الإدارة ولو كانت في `DEFAULT_CATEGORIES`. (رُصد عملياً:
  «مطاعم السعودية» ما زالت في القائمة الافتراضية ومسحوبة في السحابة، فدُمجت
  ثم أُسقطت في نفس التحميل.)

  **التحقق** (تشغيل فعلي على `localhost:8150`): زُرع في localStorage جهازٌ
  «قديم» بثلاث فئات محفوظة فقط ثم أُعيد التحميل — الاثنتا عشرة كلها وصلت
  (41 فئة بلا تكرار واحد)، وحُفظت في `mr_categories`، ورُسمت البطاقات
  باثنتي عشرة أيقونة صحيحة. واختيار ست فئات جديدة وبدء جولة: اللوحة تبني
  18 خلية، وفتح خلية يعرض **نموذج «إضافة سؤال سريع»** («ما فيه سؤال محفوظ
  لهذه الفئة بعد 🙂») لا خطأً — فهي قابلة للاختيار وغير جاهزة للعب حتى
  يُكتب لها سؤال. ولا خطأ واحد في الـ console. الإصدار `20260910a` والكاش
  `raja-v30`.

  📌 **ورُفعت إلى السحابة أيضاً** (2026-09-10، بطلب المستخدم): صفّ
  `categories` في `game_settings` كان 29 فئة ولا واحدة منها فيه. دُمجت
  الاثنتا عشرة **إضافةً بحتة** — قُرئ الصفّ واحتُفظ بنسخته قبل الكتابة، ولم
  تُحذف فئة ولا تكرّرت واحدة (29 ← 41)، وفُحصت أسماء المسحوبات الـ31 في
  السحابة فلم تكن أيٌّ منها فيها. ليست ضرورية للظهور — `mergeDefaultCategories`
  تكفي — لكنها تجعل السحابة والملفات تقولان الشيء نفسه، فلا يرى جهازٌ قائمةً
  ويرى غيره أخرى.

  **تحقق ثانٍ بعد الرفع**: مُحيت `mr_categories` من جهاز الاختبار وأُعيد
  التحميل (جهاز نظيف يقرأ من السحابة والملفات معاً) — 41 فئة، الاثنتا عشرة
  كلها، بلا تكرار وبلا خطأ في الـ console.

  📌 **متبقٍ**: الأسئلة. وحتى تُكتب، من يختار إحداها في جولة حقيقية يقف
  أمام نموذج إضافة — يُستحسن عدم اختيارها في جلسة مع الأهل.

- [x] **69. تعبئة الفئات الجديدة — 540 سؤالاً في تسع فئات**
  — أُنجز 2026-09-10 (commit 1480386)، بطلب المستخدم

  الفئات الاثنتا عشرة من البند 68 كانت أسماءً بلا أسئلة. عُبِّئت منها **تسع**
  بمعيار المشروع 20/20/20 — 540 سؤالاً في ثلاثة ملفات جديدة:
  `data/questions-part23.json` (حروف مبعثرة، صحح الخطأ)،
  `part24` (القرآن الكريم، نشيد وطني، معالم السعودية)،
  `part25` (اسم اللاعب الأول، كرة قدم، الكرة السعودية، ألعاب إلكترونية).

  📌 **والثلاث الباقية تنتظر صور المستخدم** بقراره: «شعارات دول»، «شعارات
  أندية ومنتخبات»، «تسريحة لاعب» — طبيعتها مصوّرة، وصور المشروع كلها ملفات
  محلية في `assets/` من مجلّداته (كفئتَي الأعلام)، ولا تُولَّد من عندنا. تبقى
  أسماؤها محجوزة، وفتح خلية فيها يعرض نموذج الإضافة السريعة كما في البند 68.

  📌 **حروف التبعثر مُولَّدة بسكربت من الإجابة نفسها** لا مكتوبة بيد: يستحيل
  أن ينقص حرف أو يزيد، والبذرة من الكلمة فلا يتغيّر السؤال بين تحميل وتحميل.
  فُحصت الستون كلها بمقارنة الحروف المعروضة بحروف الإجابة — تطابق تام.

  📌 **خمس كلمات أُسقطت من «حروف مبعثرة» عمداً** (قلم، كتاب، شمس، قمر، بحر):
  المستخدم كان أضافها بنفسه في بنك السحابة بصيغة سؤال مختلفة. ذاكرة عدم
  التكرار (`SEEN_KEY`) تعمل بنص السؤال لا بالإجابة، فلو بقيت لظهرت الكلمة
  نفسها بسؤالين. صارت الفئة 25 سؤالاً سهلاً بلا جواب مكرّر واحد.

  📌 **القرآن الكريم — ما استُبعد أهم مما أُدرج**: اقتُصر على ما لا خلاف فيه
  (أعداد وأسماء وترتيب وألقاب مشهورة)، وأعداد الآيات كلها بالعدّ الكوفي
  المعمول به في مصحف المدينة. تُجنّبت المسائل المختلف فيها — عدد سجدات
  التلاوة، آخر ما نزل، أول سورة نزلت كاملة — وكل ما يقوم على حديث ضعيف،
  والألقاب صيغت بلفظ «تُلقّب» لأنها ألقاب لا أسماء رسمية.

  📌 **«نشيد وطني» بلا اقتباس كلمات**: الأسئلة عن الأناشيد لا منها — كاتب
  وملحّن وتاريخ وأرقام قياسية. كلمات الأناشيد محفوظة الحقوق فلا تُعاد هنا.

  📌 **لا معلومة متغيّرة في فئتي الكرة**: لا بطل حالي ولا انتقالات ولا أرقام
  موسم جارٍ — تلك تقدُم بعد أشهر فيصير السؤال خطأً في جلسة عائلية. ولا شيء
  عن مونديال 2026 لأنه بعد حدّ معرفة من كتب الأسئلة. وأُسقطت ثلاثة أسئلة
  كُتبت ثم لم تصمد للتحقق (مدينة نادي الفيصلي، لقب «فخر الجنوب»، عدد فرق
  دوري روشن) واستُبدلت بوقائع ثابتة.

  📌 **الملفات الثلاثة أُضيفت إلى `SHELL` في `sw.js`** لا إلى `QBANK_FILES`
  وحدها — بدون ذلك تُحمَّل الفئات الجديدة أونلاين فقط وتغيب في الوضع المحلي
  بلا شبكة.

  **التحقق**: (1) سكربت فحص على البنك كله — 20/20/20 في التسع، ولا نصّ سؤال
  مكرّر داخل فئة+مستوى في **البنك كله** (3,049 سؤالاً في 57 فئة)، ولا سؤال
  بإجابتين، ولا حقل فارغ. (2) تشغيل فعلي: التسع تظهر 20/20/20 داخل اللعبة،
  وجولة من ست فئات جديدة فُتحت فيها ست خلايا على المستويات الثلاثة فظهر نصّ
  حقيقي في كل مرة، وكشف الإجابة يعمل. ولا خطأ واحد في الـ console.
  الإصدار `20260910c` والكاش `raja-v32`.

  ⚠️ **درس تكرّر**: تعديل ملف بيانات **بلا رفع `?v=`** لا يظهر أبداً — عُدِّلت
  «حروف مبعثرة» فبقيت النسخة القديمة تُخدَّم من الكاش حتى رُفع الإصدار.

- [x] **70. «نشيد وطني» صارت فئة صوتية بحتة**
  — أُنجز 2026-09-10 (commit 4e3bd05)، بطلب المستخدم

  المستخدم قرّر أن يضع مقاطع الأناشيد بنفسه: الفئة تصير «اسمع المقطع وخمّن
  الدولة». الصوت **مدعوم في اللعبة أصلاً** — حقل `audio` في السؤال يرسم
  `<audio controls>` (بُني لفئة «صوت المشهور» المسحوبة)، فلم يُكتب سطر عرض
  واحد. جُرّب فعلياً بنغمة مولَّدة: المشغّل يظهر داخل بطاقة السؤال ويُشغّل.

  📌 **الستون النصّية لم تُرمَ**: نُقلت بنصوصها كما هي إلى فئة جديدة
  **«معلومات الأناشيد» 🎵** (كاتب، ملحّن، تواريخ، أرقام قياسية). النقل تغييرُ
  اسمِ مفتاحٍ في `data/questions-part24.json` لا إعادة كتابة.

  📌 **`AUDIO_ONLY_CATEGORIES` — ولماذا لم تكفِ الملفات**: البنك يعيش في
  `mr_bank` بـlocalStorage وفي `question_bank` بالسحابة، والدمج **يضيف ولا
  يحذف**. فمن حمّل الصفحة قبل التغيير كانت الستون ستبقى عنده داخل «نشيد
  وطني» إلى الأبد فتصير الفئة مختلطة عنده وحده — وهو ما رفضه المستخدم.
  `enforceAudioOnlyCategories()` تُسقط من الفئة كل سؤال بلا `audio` عند كل
  تحميل، وتُنادى في `syncBundledQuestionBank` وبعد سحب السحابة في `sync.js`.

  📌 **ولا تصلح `retired-questions.json` هنا**: إسقاطها بالنصّ يعمل على **كل**
  الفئات، والنصوص نفسها هي التي انتقلت إلى الفئة الجديدة — فكانت ستُمحى من
  الاثنتين معاً. القاعدة مربوطة بالفئة لا بالنصّ.

  📌 **أثر جانبي مقصود**: سؤال يُضاف بنموذج «الإضافة السريعة» في فئة صوتية
  بلا مقطع يُسقَط عند التحميل التالي.

  📌 **باق في عامل الخدمة ظهر مع الصوت**: `if (fresh && fresh.ok)` كانت تُخزّن
  **206** أيضاً — جواب طلب Range الذي يرسله المتصفح عند سحب شريط الصوت —
  و`cache.put` ترفض أي استجابة جزئية، فكان كل سحب يخلّف رفضاً غير مُلتقَط.
  صارت `fresh.status === 200`.

  📌 **`assets/anthems/` جاهز** وفيه `اقرأني.txt` بالقواعد: أسماء لاتينية
  (`saudi.mp3`)، صيغة mp3، طول 10-20 ثانية (المؤقّت 20/30/45 فمقطع دقيقة يأكل
  وقت الإجابة)، وأقل من 500KB لكل ملف — لأن `SHELL` في `sw.js` تُنزَّل على
  جوال كل لاعب ليعمل الوضع المحلي بلا إنترنت.

  **التحقق** (تشغيل فعلي على جهاز زُرع فيه الوضع القديم عمداً: بنك محفوظ فيه
  «نشيد وطني» بثلاثة أسئلة نصّية وواحد صوتي): بعد التحميل بقي **الصوتي وحده**
  (0/0/1) و**حُفظ التنظيف في localStorage** لا في الذاكرة فقط، و«معلومات
  الأناشيد» ظهرت 20/20/20 وانضمّت لقائمة الفئات. ولا تصادم نصّ واحد في البنك
  كله (3,049 سؤالاً في 57 فئة)، ولا خطأ في الـ console.
  الإصدار `20260910d` والكاش `raja-v33`.

  ⏳ **متبقٍ**: مقاطع المستخدم. حتى تصل، «نشيد وطني» فئة فارغة تعرض نموذج
  الإضافة السريعة — كالثلاث المصوّرة في البند 69.

- [x] **71. زرّ إخفاء الفئة + علامة «قريباً» على الفارغة**
  — أُنجز 2026-09-10 (commit cf17c18)، بطلب المستخدم

  **(أ) الإخفاء.** زرّ ✕ في زاوية كل بطاقة فئة يخفيها عن **كل الأجهزة**
  (باختيار المستخدم)، وشريط «🙈 فئات مخفية» تحت الشبكة يرجعها بضغطة.

  📌 **لا آلية موازية**: يستعمل نفس أنبوب `retired_categories` الذي يستعمله
  الحذف في لوحة الإدارة (`publishRetiredCategory` / `unretireCategory`).
  الفرق أن هذا معلن القابلية للتراجع بدل حذف يبدو نهائياً.

  📌 **ولا تضيع الأسئلة**: `retireCategories()` تحذف الفئة من البنك، لكن
  أسئلتها تعيش في ملفات المشروع وفي `question_bank` بالسحابة، فترجع كاملة
  عند الإظهار. جُرّب على «كرة قدم»: 60 سؤالاً ← إخفاء (اختفت من الشبكة ومن
  البنك) ← إرجاع ← 60 سؤالاً كاملة.

  📌 **الزرّ للإدمن وحده ولا مطالبة لغيره**: `authenticateAdmin()` مع السحابة
  تمرّ صامتةً لمن هو إدمن وترفض غيره برسالة — فزرٌّ يراه الجميع ويُنكر على
  أكثرهم إزعاجٌ بلا فائدة. `probeAdminOnce()` تتحقّق مرّة واحدة بلا أي
  مطالبة (جلسة + `is_admin`) وتعيد الرسم إن ثبتت الصلاحية.

  📌 **بلا سحابة لا إخفاء**: لو فشل الرفع لا نُخفي محلياً ونسكت —
  `fetchRetiredCategories` تعيد بناء القائمة كل تحميل، فالإخفاء المحلي البحت
  يتبخّر بلا أن يفهم أحد لماذا. نقول: «تعذّر الوصول للسحابة» ولا نُخفي.

  **(ب) ثلاث طبقات ترشيح لشريط الإرجاع** — كلها ظهرت في الاختبار لا في التصميم:
  1. **المسحوب في ملف المشروع لا يُعرض** (`fileRetiredNames`): إرجاعه وعدٌ
     كاذب، لأن `data/retired-categories.json` يعيد سحبه عند أول تحميل.
     أسقط 19 اسماً.
  2. **الاسم الذي لا يقابله فئة لا يُعرض** (`knownCategoryNames` من ملفات
     المشروع + بنك السحابة): صفّ `retired_categories` في قاعدة المستخدم فيه
     **23 اسماً مهملاً** من كتابة خاطئة قديمة — `"0"`..`"21"` و`"h"`. لا أثر
     لها على اللعب (تُخفي فئات غير موجودة) لكنها كانت ستملأ الشريط بثلاث
     وعشرين شريحة بلا معنى. **ولم نعدّل بيانات المستخدم** — الترشيح في العرض.
  3. الترتيب أبجدي وبلا تكرار.

  النتيجة: من 31 اسماً في السحابة إلى **شريحة واحدة** قابلة للإرجاع فعلاً.

  **(ج) «قريباً» على الفئة الفارغة.** كانت تُختار عادية فيقع اللاعب وسط
  الجلسة على نموذج «أضف سؤالاً» — وهو مخرج المطوّر لا مخرج اللاعب. صارت
  باهتة بعلامة «قريباً» ولا تُختار، مع رسالة تشرح. تنطبق تلقائياً على كل فئة
  بلا أسئلة، فترجع للاختيار وحدها أول ما تصلها أسئلة — لا قائمة يدوية تُنسى.

  **التحقق** (تشغيل فعلي): 42 بطاقة، و«قريباً» على الأربع الفارغة وحدها
  (نشيد وطني، شعارات دول، تسريحة لاعب، شعارات أندية ومنتخبات). والضغط عليها
  لا يختارها ويعرض الرسالة، بينما فئة عادية تُختار طبيعياً. ولغير الإدمن **لا
  زرّ ✕ ولا شريط مخفيّات إطلاقاً** (صفر زرّ). وللإدمن 42 زرّاً. ودورة إخفاء
  وإرجاع كاملة على «كرة قدم» رجعت بستّين سؤالاً واختفت شريحتها.
  و**بالقياس لا بالنظر**: زرّ ✕ في يمين البطاقة (848–870 داخل 714–876)
  وعلامة ✓ في يسارها (722–740) — **لا تقاطع** بينهما والبطاقة مختارة.
  ولا خطأ واحد في الـ console. الإصدار `20260910g` والكاش `raja-v36`.

  ⚠️ **ما لم يُختبر**: مسار `probeAdminOnce` الحقيقي يحتاج حساب إدارة مسجّلاً
  (اختُبر بمحاكاة الصلاحية). ولم تُكتب في السحابة ولا حرف أثناء الاختبار —
  دورة الإخفاء والإرجاع جرت بدوالّ محاكاة، فلم تُخفَ فئة على أحد.

  📌 **معلّق للمستخدم**: تنظيف الأسماء المهملة الـ23 من صفّ
  `retired_categories` — تحسين لا ضرورة، والواجهة تتجاهلها الآن.

- [x] **72. «شعارات دول» — 22 شعاراً من مجلّد المستخدم**
  — أُنجز 2026-09-11 (commit PENDING)، بطلب المستخدم

  أولى الفئات المصوّرة الثلاث من البند 69. المصدر مجلّد «contry logos» عند
  المستخدم، والتقسيم إلى سهل/متوسط/صعب **تقسيمه هو**: 9/6/7. الصور نُسخت إلى
  `assets/country-emblems/` بأسماء لاتينية كفئتَي الأعلام، والإجابة العربية في
  `data/questions-part26.json` لا في اسم الملف.

  📌 **كل شعار فُحص بالعين قبل كتابة إجابته** — الـ22 مطابقة لأسمائها. شعار
  سوريا هو الجديد (العُقاب الذهبي بثلاث نجوم)، والإجابة «سوريا» صحيحة به.

  📌 **أقل من معيار 20/20/20 كفئات الأعلام**: العدد عدد صور المستخدم. اللوحة
  تسحب سؤالاً واحداً لكل مستوى في الجولة، فـ9/6/7 تكفي عدة جولات قبل التكرار.

  📌 **ثلاثة شعارات تكتب اسم دولتها**: فيتنام («VIỆT NAM» — في المتوسط)،
  والبرازيل («REPÚBLICA FEDERATIVA DO BRASIL») وإيطاليا («REPVBBLICA
  ITALIANA») — وكلاهما في الصعب. تُركت كما قسّمها المستخدم، ونُبّه إليها.

  📌 **الصور الـ22 في `SHELL`** (1.6MB) مع ملف الأسئلة — الفئة كلها صور،
  فبدونها تنكسر تماماً بلا إنترنت، كصور «منو المشهور».

  **التحقق** (تشغيل فعلي بعد مسح عامل الخدمة والكاش): الإصدار الجديد يُحمَّل،
  والفئة 9/6/7، و**زالت عنها علامة «قريباً»** تلقائياً (البند 71)، والصور
  22/22 تُحمَّل، وجولة من الفئة فتحت 100 و250 و400 فظهر الشعار ونصّ سؤاله في
  كل مرة. ولا تصادم نصّ في البنك كله (3,071 سؤالاً في 58 فئة)، ولا صورة مفقودة،
  ولا خطأ في الـ console. الإصدار `20260910h` والكاش `raja-v37`.

- [ ] **رمز الإدارة `2014` مكتوب في الكود** (`ADMIN_PIN` في `js/game.js`).
  يعمل فقط في التشغيل المحلي بلا سحابة حيث لا يضرّ أحداً — لكنه يبقى
  مكشوفاً لمن يقرأ المصدر. الحماية الحقيقية في قاعدة البيانات (`is_admin()`).

---

---

## 5.5 خلاصة الجاهزية

**اللعبة منشورة وتعمل.** كل commit يُنشر تلقائياً على GitHub Pages.

**لا يوجد عمل برمجي معلّق.** المتبقي كله إما خارج الكود (نطاق) أو تحسين
اختياري.

**العائق الوحيد أمام النشر الواسع:** لا نطاق ← لا توثيق في Resend ← استعادة
كلمة المرور لا تصل لأي لاعب غير المالك ← الزر مخفي. اللاعب الذي ينسى كلمته
يعلق، ويُحل يدوياً من SQL Editor.

**أول جلسة لعب حقيقية جرت في 2026-08-02** وكشفت ستة عيوب (البنود 13–18)،
منها اثنان لم يكونا ليظهرا في أي اختبار آلي: وقوف الدور عند من لعب (لا يظهر
إلا بجهازين ولاعب غير مضيف)، وأن تحديثات بنك الأسئلة لا تصل للاعبين أصلاً
بسبب الكاش. كلها أُصلحت.

**جولة كاملة جرت في محاكاة جهازين (2026-08-02)** — من البدء إلى شاشة الفوز،
18 سؤالاً، بإجابات صحيحة وخاطئة، ومجيب مضيف ومجيب غير مضيف. كل شيء تطابق
على الجهازين. التفاصيل في البند 13.

**ما زال غير مختبر بيد إنسان:** جولة على جهازين حقيقيين عبر Supabase (رحلة
الشبكة نفسها هي الوحيدة التي لم تغطّها المحاكاة)، وإلصاق الصور من جوال حقيقي.

---

## 6. الخطة الزمنية

| الموعد | الهدف |
|---|---|
| **قبل النشر** | البندان 1 و 2 (حرجان) |
| **نشر صامت** | تجربة مع العائلة والأصدقاء ومراقبة أين يتعثرون |
| **اليوم الوطني (23 سبتمبر)** | جاهز: السعودية، أغاني وطنية، المناطق الخمس، شعارات سعودية، لهجات |
| **رمضان (مطلع 2027)** | الموسم الأكبر — اجتماع عائلي كل ليلة |

---

## 7. كيف تُحدَّث هذه الخطة

عند إنجاز أي بند:
1. غيّر `[ ]` إلى `[x]` وأضف: `— أُنجز 2026-07-27 (commit abc1234)`
2. انقل ما استُجدّ من ملاحظات إلى القسم 3 أو 4
3. غيّر تاريخ «آخر تحديث» في أعلى الملف
4. أضف أي مهمة جديدة في مكانها حسب الأولوية
